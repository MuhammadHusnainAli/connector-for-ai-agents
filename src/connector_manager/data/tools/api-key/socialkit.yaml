# Socialkit tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.socialkit.dev/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: socialkit
display_name: Socialkit
docs_url: https://api.socialkit.dev/openapi.json
generated: true
generated_from: https://api.socialkit.dev/openapi.json
tools:
  get_facebook_channel_stats:
    title: Facebook Channel Stats
    description: Facebook Channel Stats Get stats and metadata for a Facebook page. Calls GET /facebook/channel-stats
      on the provider's API.
    category: facebook
    request:
      method: GET
      path: /facebook/channel-stats
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Page stats retrieved successfully
      type: object
    read_only: true
  create_facebook_channel_stats:
    title: Facebook Channel Stats
    description: Facebook Channel Stats Get stats and metadata for a Facebook page. Calls POST /facebook/channel-stats
      on the provider's API.
    category: facebook
    request:
      method: POST
      path: /facebook/channel-stats
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Page stats retrieved successfully
      type: object
  get_facebook_comments:
    title: Facebook Comments
    description: Facebook Comments Fetch top comments from a Facebook video or post. Calls GET /facebook/comments
      on the provider's API.
    category: facebook
    request:
      method: GET
      path: /facebook/comments
      query:
        url: ${url}
        limit: ${limit}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      limit:
        type: integer
        description: Maximum number of results to return
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Comments retrieved successfully
      type: object
    read_only: true
  create_facebook_comments:
    title: Facebook Comments
    description: Facebook Comments Fetch top comments from a Facebook video or post. Calls POST /facebook/comments
      on the provider's API.
    category: facebook
    request:
      method: POST
      path: /facebook/comments
      query:
        url: ${url}
        limit: ${limit}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      limit:
        type: integer
        description: Maximum number of results to return
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Comments retrieved successfully
      type: object
  get_facebook_stats:
    title: Facebook Stats
    description: Facebook Stats Get engagement metrics and metadata for a Facebook video or post. Calls
      GET /facebook/stats on the provider's API.
    category: facebook
    request:
      method: GET
      path: /facebook/stats
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Post stats retrieved successfully
      type: object
    read_only: true
  create_facebook_stats:
    title: Facebook Stats
    description: Facebook Stats Get engagement metrics and metadata for a Facebook video or post. Calls
      POST /facebook/stats on the provider's API.
    category: facebook
    request:
      method: POST
      path: /facebook/stats
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Post stats retrieved successfully
      type: object
  get_facebook_summarize:
    title: Facebook Summarize
    description: Facebook Summarize Summarize a Facebook video using AI. Calls GET /facebook/summarize
      on the provider's API.
    category: facebook
    request:
      method: GET
      path: /facebook/summarize
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Summary generated successfully
      type: object
    read_only: true
  create_facebook_summarize:
    title: Facebook Summarize
    description: Facebook Summarize Summarize a Facebook video using AI. Calls POST /facebook/summarize
      on the provider's API.
    category: facebook
    request:
      method: POST
      path: /facebook/summarize
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Summary generated successfully
      type: object
  get_facebook_transcript:
    title: Facebook Transcript
    description: Facebook Transcript Extract the spoken transcript from a Facebook video. Calls GET /facebook/transcript
      on the provider's API.
    category: facebook
    request:
      method: GET
      path: /facebook/transcript
      query:
        url: ${url}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Transcript extracted successfully
      type: object
    read_only: true
  get_instagram_channel_posts:
    title: Instagram Channel Posts
    description: Instagram Channel Posts Get recent posts from an Instagram profile's feed. Calls GET
      /instagram/channel-posts on the provider's API.
    category: instagram
    request:
      method: GET
      path: /instagram/channel-posts
      query:
        url: ${url}
        limit: ${limit}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      limit:
        type: integer
        description: Maximum number of results to return
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Channel posts retrieved successfully
      type: object
    read_only: true
  create_instagram_channel_posts:
    title: Instagram Channel Posts
    description: Instagram Channel Posts Get recent posts from an Instagram profile's feed. Calls POST
      /instagram/channel-posts on the provider's API.
    category: instagram
    request:
      method: POST
      path: /instagram/channel-posts
      query:
        url: ${url}
        limit: ${limit}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      limit:
        type: integer
        description: Maximum number of results to return
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Channel posts retrieved successfully
      type: object
  get_instagram_channel_reels:
    title: Instagram Channel Reels
    description: Instagram Channel Reels Get recent reels from an Instagram profile's reels tab. Calls
      GET /instagram/channel-reels on the provider's API.
    category: instagram
    request:
      method: GET
      path: /instagram/channel-reels
      query:
        url: ${url}
        limit: ${limit}
        access_key: ${access_key}
    input:
      url:
        type: string
        description: Full URL of the resource
        optional: true
      limit:
        type: integer
        description: Maximum number of results to return
        optional: true
      access_key:
        type: string
        description: Project access key (also accepted as x-access-key header)
        optional: true
    output:
      description: Channel reels retrieved successfully
      type: object
    read_only: true
