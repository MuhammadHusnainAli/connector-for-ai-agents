# Altoviz tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.altoviz.com/openapi.yaml
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: altoviz
display_name: Altoviz
docs_url: https://api.altoviz.com/openapi.yaml
generated: true
generated_from: https://api.altoviz.com/openapi.yaml
tools:
  get_hello_get:
    title: Say hello to test api key
    description: Say hello to test api key Calls GET /Hello on the provider's API.
    category: hello
    request:
      method: GET
      path: /Hello
      query:
        api-version: ${api_version}
    input:
      api_version:
        type: string
        description: The api-version parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_classifications_list:
    title: List all classifications
    description: List all classifications Calls GET /v1/Classifications on the provider's API.
    category: classifications
    request:
      method: GET
      path: /v1/Classifications
      query:
        type: ${type}
    input:
      type:
        type: string
        description: Classification Type
        optional: true
        enum:
        - Sale
        - Expense
        - Other
    output:
      description: OK
      type: object
    read_only: true
  get_colleagues_list:
    title: List colleagues
    description: List colleagues Calls GET /v1/Colleagues on the provider's API.
    category: colleagues
    request:
      method: GET
      path: /v1/Colleagues
      query:
        PageIndex: ${page_index}
        PageSize: ${page_size}
        OrderBy: ${order_by}
        query: ${query}
    input:
      page_index:
        type: integer
        description: The PageIndex parameter.
        optional: true
      page_size:
        type: integer
        description: The PageSize parameter.
        optional: true
      order_by:
        type: string
        description: The OrderBy parameter.
        optional: true
      query:
        type: string
        description: query (full text search)
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  post_colleagues_post:
    title: Create a new colleague
    description: Create a new colleague Calls POST /v1/Colleagues on the provider's API.
    category: colleagues
    request:
      method: POST
      path: /v1/Colleagues
      body:
        cellPhone: ${cell_phone}
        email: ${email}
        firstName: ${first_name}
        homecareServiceNumber: ${homecare_service_number}
        id: ${id}
        initialPartnerBalance: ${initial_partner_balance}
        internalId: ${internal_id}
        isPartner: ${is_partner}
        lastName: ${last_name}
        metadatas: ${metadatas}
        name: ${name}
        number: ${number}
    input:
      cell_phone:
        type: string
        description: Cell phone number
        optional: true
      email:
        type: string
        description: Email
        optional: true
      first_name:
        type: string
        description: First name
        optional: true
      homecare_service_number:
        type: string
        description: Homecare service number associated with the colleague
        optional: true
      id:
        type: integer
        description: Colleague Id
        optional: true
      initial_partner_balance:
        type: number
        description: Initial balance for the partner
        optional: true
      internal_id:
        type: string
        description: Internal identifier for the colleague
        optional: true
      is_partner:
        type: boolean
        description: Indicates if the colleague is a partner
        optional: true
      last_name:
        type: string
        description: Last name
        optional: true
      metadatas:
        type: object
        description: Metadata associated with the colleague
        optional: true
      name:
        type: string
        description: Colleague Name
        optional: true
      number:
        type: string
        description: Colleague Number
        optional: true
    output:
      description: OK
      type: object
  get_contacts_list:
    title: List Contacts
    description: List Contacts Calls GET /v1/Contacts on the provider's API.
    category: contacts
    request:
      method: GET
      path: /v1/Contacts
      query:
        PageIndex: ${page_index}
        PageSize: ${page_size}
        OrderBy: ${order_by}
        query: ${query}
    input:
      page_index:
        type: integer
        description: The PageIndex parameter.
        optional: true
      page_size:
        type: integer
        description: The PageSize parameter.
        optional: true
      order_by:
        type: string
        description: The OrderBy parameter.
        optional: true
      query:
        type: string
        description: query (full text search)
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  post_contacts_post:
    title: Create a new Contact
    description: Create a new Contact Calls POST /v1/Contacts on the provider's API.
    category: contacts
    request:
      method: POST
      path: /v1/Contacts
      body:
        cellPhone: ${cell_phone}
        companyName: ${company_name}
        displayName: ${display_name}
        email: ${email}
        firstName: ${first_name}
        function: ${function}
        id: ${id}
        internalId: ${internal_id}
        invertedDisplayName: ${inverted_display_name}
        lastName: ${last_name}
        phone: ${phone}
        service: ${service}
    input:
      cell_phone:
        type: string
        description: The cellPhone field of the request body.
        optional: true
      company_name:
        type: string
        description: The companyName field of the request body.
        optional: true
      display_name:
        type: string
        description: The displayName field of the request body.
        optional: true
      email:
        type: string
        description: The email field of the request body.
        optional: true
      first_name:
        type: string
        description: The firstName field of the request body.
        optional: true
      function:
        type: string
        description: The function field of the request body.
        optional: true
      id:
        type: integer
        description: Contact ID
        optional: true
      internal_id:
        type: string
        description: ID used in your system
        optional: true
      inverted_display_name:
        type: string
        description: The invertedDisplayName field of the request body.
        optional: true
      last_name:
        type: string
        description: The lastName field of the request body.
        optional: true
      phone:
        type: string
        description: The phone field of the request body.
        optional: true
      service:
        type: string
        description: The service field of the request body.
        optional: true
    output:
      description: OK
      type: object
  get_customer_families_list:
    title: List customer families
    description: List customer families Calls GET /v1/CustomerFamilies on the provider's API.
    category: customer_families
    request:
      method: GET
      path: /v1/CustomerFamilies
      query:
        PageIndex: ${page_index}
        PageSize: ${page_size}
        OrderBy: ${order_by}
        query: ${query}
    input:
      page_index:
        type: integer
        description: The PageIndex parameter.
        optional: true
      page_size:
        type: integer
        description: The PageSize parameter.
        optional: true
      order_by:
        type: string
        description: The OrderBy parameter.
        optional: true
      query:
        type: string
        description: query (full text search)
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  post_customer_families_post:
    title: Create a new customer family
    description: Create a new customer family Calls POST /v1/CustomerFamilies on the provider's API.
    category: customer_families
    request:
      method: POST
      path: /v1/CustomerFamilies
      body:
        id: ${id}
        internalId: ${internal_id}
        label: ${label}
        number: ${number}
    input:
      id:
        type: integer
        description: ID
        optional: true
      internal_id:
        type: string
        description: Internal ID
        optional: true
      label:
        type: string
        description: Label
        optional: true
      number:
        type: string
        description: Number
        optional: true
    output:
      description: OK
      type: object
  get_customers_list:
    title: List customers
    description: List customers Calls GET /v1/Customers on the provider's API.
    category: customers
    request:
      method: GET
      path: /v1/Customers
      query:
        PageIndex: ${page_index}
        PageSize: ${page_size}
        OrderBy: ${order_by}
        query: ${query}
    input:
      page_index:
        type: integer
        description: The PageIndex parameter.
        optional: true
      page_size:
        type: integer
        description: The PageSize parameter.
        optional: true
      order_by:
        type: string
        description: The OrderBy parameter.
        optional: true
      query:
        type: string
        description: query (full text search)
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  post_customers_post:
    title: Create a new customer
    description: Create a new customer Calls POST /v1/Customers on the provider's API.
    category: customers
    request:
      method: POST
      path: /v1/Customers
      body:
        active: ${active}
        billingAddress: ${billing_address}
        billingOptions: ${billing_options}
        cellPhone: ${cell_phone}
        companyInformations: ${company_informations}
        companyName: ${company_name}
        email: ${email}
        family: ${family}
        firstName: ${first_name}
        internalId: ${internal_id}
        internalNotes: ${internal_notes}
        lastName: ${last_name}
    input:
      active:
        type: boolean
        description: Active
        optional: true
      billing_address:
        type: object
        description: The billingAddress field of the request body.
        optional: true
      billing_options:
        type: object
        description: Billing options
        optional: true
      cell_phone:
        type: string
        description: Cell phone number
        optional: true
      company_informations:
        type: object
        description: The companyInformations field of the request body.
        optional: true
      company_name:
        type: string
        description: Company name
        optional: true
      email:
        type: string
        description: Email
        optional: true
      family:
        type: object
        description: Customer family
        optional: true
      first_name:
        type: string
        description: First name
        optional: true
      internal_id:
        type: string
        description: ID used in information system
        optional: true
      internal_notes:
        type: string
        description: Internal notes
        optional: true
      last_name:
        type: string
        description: Last name
        optional: true
    output:
      description: OK
      type: object
