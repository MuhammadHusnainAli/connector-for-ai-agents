# Spotify (Client Credentials) tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/spotify.com/1.0.0/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: spotify-oauth2-cc
display_name: Spotify (Client Credentials)
docs_url: https://developer.spotify.com/terms/
generated: true
generated_from: https://api.apis.guru/v2/specs/spotify.com/1.0.0/openapi.json
tools:
  get_multiple_albums:
    title: Get Several Albums
    description: Get Several Albums Get Spotify catalog information for multiple albums identified by
      their Spotify IDs. Calls GET /v1/albums on the provider's API.
    category: albums
    request:
      method: GET
      path: /v1/albums
      query:
        ids: ${ids}
        market: ${market}
    input:
      ids:
        type: string
        description: The ids parameter.
      market:
        type: string
        description: The market parameter.
        optional: true
    output:
      description: A set of albums
      type: object
    read_only: true
  get_multiple_artists:
    title: Get Several Artists
    description: Get Several Artists Get Spotify catalog information for several artists based on their
      Spotify IDs. Calls GET /v1/artists on the provider's API.
    category: artists
    request:
      method: GET
      path: /v1/artists
      query:
        ids: ${ids}
    input:
      ids:
        type: string
        description: The ids parameter.
    output:
      description: A set of artists
      type: object
    read_only: true
  get_several_audio_features:
    title: Get Tracks' Audio Features
    description: Get Tracks' Audio Features Get audio features for multiple tracks based on their Spotify
      IDs. Calls GET /v1/audio-features on the provider's API.
    category: audio_features
    request:
      method: GET
      path: /v1/audio-features
      query:
        ids: ${ids}
    input:
      ids:
        type: string
        description: The ids parameter.
    output:
      description: A set of audio features
      type: object
    read_only: true
  get_multiple_audiobooks:
    title: Get Several Audiobooks
    description: 'Get Several Audiobooks Get Spotify catalog information for several audiobooks identified
      by their Spotify IDs. Note: Audiobooks are only available for the US, UK, Ireland, New Zealand and
      Australia markets. Calls GET /v1/audiobooks on the provider''s API.'
    category: audiobooks
    request:
      method: GET
      path: /v1/audiobooks
      query:
        ids: ${ids}
        market: ${market}
    input:
      ids:
        type: string
        description: The ids parameter.
      market:
        type: string
        description: The market parameter.
        optional: true
    output:
      description: A set of audiobooks
      type: object
    read_only: true
  get_several_chapters:
    title: Get Several Chapters
    description: 'Get Several Chapters Get Spotify catalog information for several chapters identified
      by their Spotify IDs. Note: Chapters are only available for the US, UK, Ireland, New Zealand and
      Australia markets. Calls GET /v1/chapters on the provider''s API.'
    category: chapters
    request:
      method: GET
      path: /v1/chapters
      query:
        ids: ${ids}
        market: ${market}
    input:
      ids:
        type: string
        description: The ids parameter.
      market:
        type: string
        description: The market parameter.
        optional: true
    output:
      description: A set of chapters
      type: object
    read_only: true
  get_multiple_episodes:
    title: Get Several Episodes
    description: Get Several Episodes Get Spotify catalog information for several episodes based on their
      Spotify IDs. Calls GET /v1/episodes on the provider's API.
    category: episodes
    request:
      method: GET
      path: /v1/episodes
      query:
        ids: ${ids}
        market: ${market}
    input:
      ids:
        type: string
        description: The ids parameter.
      market:
        type: string
        description: The market parameter.
        optional: true
    output:
      description: A set of episodes
      type: object
    read_only: true
  get_available_markets:
    title: Get Available Markets
    description: Get Available Markets Get the list of markets where Spotify is available. Calls GET /v1/markets
      on the provider's API.
    category: markets
    request:
      method: GET
      path: /v1/markets
    input: {}
    output:
      description: A markets object with an array of country codes
      type: object
    read_only: true
  get_current_users_profile:
    title: Get Current User's Profile
    description: Get Current User's Profile Get detailed profile information about the current user (including
      the current user's username). Calls GET /v1/me on the provider's API.
    category: me
    request:
      method: GET
      path: /v1/me
    input: {}
    output:
      description: A user
      type: object
    read_only: true
  get_recommendations:
    title: Get Recommendations
    description: Get Recommendations Recommendations are generated based on the available information
      for a given seed entity and matched against similar artists and tracks. If there is sufficient information
      about the provided seeds, a list of tracks will be returned together with pool size details. For
      artists and tracks that are ver Calls GET /v1/recommendations on the provider's API.
    category: recommendations
    request:
      method: GET
      path: /v1/recommendations
      query:
        limit: ${limit}
        market: ${market}
        seed_artists: ${seed_artists}
        seed_genres: ${seed_genres}
        seed_tracks: ${seed_tracks}
        min_acousticness: ${min_acousticness}
        max_acousticness: ${max_acousticness}
        target_acousticness: ${target_acousticness}
        min_danceability: ${min_danceability}
        max_danceability: ${max_danceability}
        target_danceability: ${target_danceability}
        min_duration_ms: ${min_duration_ms}
        max_duration_ms: ${max_duration_ms}
        target_duration_ms: ${target_duration_ms}
        min_energy: ${min_energy}
        max_energy: ${max_energy}
        target_energy: ${target_energy}
        min_instrumentalness: ${min_instrumentalness}
        max_instrumentalness: ${max_instrumentalness}
        target_instrumentalness: ${target_instrumentalness}
        min_key: ${min_key}
        max_key: ${max_key}
        target_key: ${target_key}
        min_liveness: ${min_liveness}
        max_liveness: ${max_liveness}
        target_liveness: ${target_liveness}
        min_loudness: ${min_loudness}
        max_loudness: ${max_loudness}
        target_loudness: ${target_loudness}
        min_mode: ${min_mode}
        max_mode: ${max_mode}
        target_mode: ${target_mode}
        min_popularity: ${min_popularity}
        max_popularity: ${max_popularity}
        target_popularity: ${target_popularity}
        min_speechiness: ${min_speechiness}
        max_speechiness: ${max_speechiness}
        target_speechiness: ${target_speechiness}
        min_tempo: ${min_tempo}
        max_tempo: ${max_tempo}
        target_tempo: ${target_tempo}
        min_time_signature: ${min_time_signature}
        max_time_signature: ${max_time_signature}
        target_time_signature: ${target_time_signature}
        min_valence: ${min_valence}
        max_valence: ${max_valence}
        target_valence: ${target_valence}
    input:
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      market:
        type: string
        description: The market parameter.
        optional: true
      seed_artists:
        type: string
        description: The seed_artists parameter.
      seed_genres:
        type: string
        description: The seed_genres parameter.
      seed_tracks:
        type: string
        description: The seed_tracks parameter.
      min_acousticness:
        type: number
        description: The min_acousticness parameter.
        optional: true
      max_acousticness:
        type: number
        description: The max_acousticness parameter.
        optional: true
      target_acousticness:
        type: number
        description: The target_acousticness parameter.
        optional: true
      min_danceability:
        type: number
        description: The min_danceability parameter.
        optional: true
      max_danceability:
        type: number
        description: The max_danceability parameter.
        optional: true
      target_danceability:
        type: number
        description: The target_danceability parameter.
        optional: true
      min_duration_ms:
        type: integer
        description: The min_duration_ms parameter.
        optional: true
      max_duration_ms:
        type: integer
        description: The max_duration_ms parameter.
        optional: true
      target_duration_ms:
        type: integer
        description: The target_duration_ms parameter.
        optional: true
      min_energy:
        type: number
        description: The min_energy parameter.
        optional: true
      max_energy:
        type: number
        description: The max_energy parameter.
        optional: true
      target_energy:
        type: number
        description: The target_energy parameter.
        optional: true
      min_instrumentalness:
        type: number
        description: The min_instrumentalness parameter.
        optional: true
      max_instrumentalness:
        type: number
        description: The max_instrumentalness parameter.
        optional: true
      target_instrumentalness:
        type: number
        description: The target_instrumentalness parameter.
        optional: true
      min_key:
        type: integer
        description: The min_key parameter.
        optional: true
      max_key:
        type: integer
        description: The max_key parameter.
        optional: true
      target_key:
        type: integer
        description: The target_key parameter.
        optional: true
      min_liveness:
        type: number
        description: The min_liveness parameter.
        optional: true
      max_liveness:
        type: number
        description: The max_liveness parameter.
        optional: true
      target_liveness:
        type: number
        description: The target_liveness parameter.
        optional: true
      min_loudness:
        type: number
        description: The min_loudness parameter.
        optional: true
      max_loudness:
        type: number
        description: The max_loudness parameter.
        optional: true
      target_loudness:
        type: number
        description: The target_loudness parameter.
        optional: true
      min_mode:
        type: integer
        description: The min_mode parameter.
        optional: true
      max_mode:
        type: integer
        description: The max_mode parameter.
        optional: true
      target_mode:
        type: integer
        description: The target_mode parameter.
        optional: true
      min_popularity:
        type: integer
        description: The min_popularity parameter.
        optional: true
      max_popularity:
        type: integer
        description: The max_popularity parameter.
        optional: true
      target_popularity:
        type: integer
        description: The target_popularity parameter.
        optional: true
      min_speechiness:
        type: number
        description: The min_speechiness parameter.
        optional: true
      max_speechiness:
        type: number
        description: The max_speechiness parameter.
        optional: true
      target_speechiness:
        type: number
        description: The target_speechiness parameter.
        optional: true
      min_tempo:
        type: number
        description: The min_tempo parameter.
        optional: true
      max_tempo:
        type: number
        description: The max_tempo parameter.
        optional: true
      target_tempo:
        type: number
        description: The target_tempo parameter.
        optional: true
      min_time_signature:
        type: integer
        description: The min_time_signature parameter.
        optional: true
      max_time_signature:
        type: integer
        description: The max_time_signature parameter.
        optional: true
      target_time_signature:
        type: integer
        description: The target_time_signature parameter.
        optional: true
      min_valence:
        type: number
        description: The min_valence parameter.
        optional: true
      max_valence:
        type: number
        description: The max_valence parameter.
        optional: true
      target_valence:
        type: number
        description: The target_valence parameter.
        optional: true
    output:
      description: A set of recommendations
      type: object
    read_only: true
  search:
    title: Search for Item
    description: 'Search for Item Get Spotify catalog information about albums, artists, playlists, tracks,
      shows, episodes or audiobooks that match a keyword string. Note: Audiobooks are only available for
      the US, UK, Ireland, New Zealand and Australia markets. Calls GET /v1/search on the provider''s
      API.'
    category: search
    request:
      method: GET
      path: /v1/search
      query:
        q: ${q}
        type: ${type}
        market: ${market}
        limit: ${limit}
        offset: ${offset}
        include_external: ${include_external}
    input:
      q:
        type: string
        description: The q parameter.
      type:
        type: array
        description: The type parameter.
        items:
          type: string
      market:
        type: string
        description: The market parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      offset:
        type: integer
        description: The offset parameter.
        optional: true
        default: 0
      include_external:
        type: string
        description: The include_external parameter.
        optional: true
        enum:
        - audio
    output:
      description: Search response
      type: object
    read_only: true
  get_multiple_shows:
    title: Get Several Shows
    description: Get Several Shows Get Spotify catalog information for several shows based on their Spotify
      IDs. Calls GET /v1/shows on the provider's API.
    category: shows
    request:
      method: GET
      path: /v1/shows
      query:
        market: ${market}
        ids: ${ids}
    input:
      market:
        type: string
        description: The market parameter.
        optional: true
      ids:
        type: string
        description: The ids parameter.
    output:
      description: A set of shows
      type: object
    read_only: true
  get_several_tracks:
    title: Get Several Tracks
    description: Get Several Tracks Get Spotify catalog information for multiple tracks based on their
      Spotify IDs. Calls GET /v1/tracks on the provider's API.
    category: tracks
    request:
      method: GET
      path: /v1/tracks
      query:
        market: ${market}
        ids: ${ids}
    input:
      market:
        type: string
        description: The market parameter.
        optional: true
      ids:
        type: string
        description: The ids parameter.
    output:
      description: A set of tracks
      type: object
    read_only: true
  get_categories:
    title: Get Several Browse Categories
    description: Get Several Browse Categories Get a list of categories used to tag items in Spotify (on,
      for example, the Spotify player’s “Browse” tab). Calls GET /v1/browse/categories on the provider's
      API.
    category: browse
    request:
      method: GET
      path: /v1/browse/categories
      query:
        country: ${country}
        locale: ${locale}
        limit: ${limit}
        offset: ${offset}
    input:
      country:
        type: string
        description: The country parameter.
        optional: true
      locale:
        type: string
        description: The locale parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      offset:
        type: integer
        description: The offset parameter.
        optional: true
        default: 0
    output:
      description: A paged set of categories
      type: object
    read_only: true
  get_featured_playlists:
    title: Get Featured Playlists
    description: Get Featured Playlists Get a list of Spotify featured playlists (shown, for example,
      on a Spotify player's 'Browse' tab). Calls GET /v1/browse/featured-playlists on the provider's API.
    category: browse
    request:
      method: GET
      path: /v1/browse/featured-playlists
      query:
        country: ${country}
        locale: ${locale}
        timestamp: ${timestamp}
        limit: ${limit}
        offset: ${offset}
    input:
      country:
        type: string
        description: The country parameter.
        optional: true
      locale:
        type: string
        description: The locale parameter.
        optional: true
      timestamp:
        type: string
        description: The timestamp parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      offset:
        type: integer
        description: The offset parameter.
        optional: true
        default: 0
    output:
      description: A paged set of playlists
      type: object
    read_only: true
