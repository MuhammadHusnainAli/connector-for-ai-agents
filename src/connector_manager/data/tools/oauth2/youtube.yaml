# YouTube tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/googleapis.com/youtube/v3/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: youtube
display_name: YouTube
docs_url: https://developers.google.com/youtube/
generated: true
generated_from: https://api.apis.guru/v2/specs/googleapis.com/youtube/v3/openapi.json
tools:
  youtube_abuse_reports_insert:
    title: Youtube abuse reports insert
    description: Inserts a new resource into this collection. Calls POST /youtube/v3/abuseReports on the
      provider's API.
    category: youtube
    request:
      method: POST
      path: /youtube/v3/abuseReports
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
      body:
        abuseTypes: ${abuse_types}
        description: ${description}
        relatedEntities: ${related_entities}
        subject: ${subject}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter serves two purposes in this operation. It identifies the properties
          that the write operation will set as well as the properties that the API response will include.
        items:
          type: string
      abuse_types:
        type: array
        description: The abuseTypes field of the request body.
        optional: true
        items:
          type: object
      description:
        type: string
        description: The description field of the request body.
        optional: true
      related_entities:
        type: array
        description: The relatedEntities field of the request body.
        optional: true
        items:
          type: object
      subject:
        type: object
        description: The subject field of the request body.
        optional: true
    output:
      description: Successful response
      type: object
  youtube_activities_list:
    title: Youtube activities list
    description: Retrieves a list of resources, possibly filtered. Calls GET /youtube/v3/activities on
      the provider's API.
    category: youtube
    request:
      method: GET
      path: /youtube/v3/activities
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        channelId: ${channel_id}
        home: ${home}
        maxResults: ${max_results}
        mine: ${mine}
        pageToken: ${page_token}
        publishedAfter: ${published_after}
        publishedBefore: ${published_before}
        regionCode: ${region_code}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter specifies a comma-separated list of one or more activity resource
          properties that the API response will include. If the parameter identifies a property that contains
          child properties, the child properties will be included in the response. For example, in an
          activity resource, the snippet property contains other properties that identify the type of
          activity, a display title for t
        items:
          type: string
      channel_id:
        type: string
        description: The channelId parameter.
        optional: true
      home:
        type: boolean
        description: The home parameter.
        optional: true
      max_results:
        type: integer
        description: The maxResults parameter specifies the maximum number of items that should be returned
          in the result set.
        optional: true
      mine:
        type: boolean
        description: The mine parameter.
        optional: true
      page_token:
        type: string
        description: The pageToken parameter identifies a specific page in the result set that should
          be returned. In an API response, the nextPageToken and prevPageToken properties identify other
          pages that could be retrieved.
        optional: true
      published_after:
        type: string
        description: The publishedAfter parameter.
        optional: true
      published_before:
        type: string
        description: The publishedBefore parameter.
        optional: true
      region_code:
        type: string
        description: The regionCode parameter.
        optional: true
    output:
      description: Successful response
      type: object
    read_only: true
  youtube_captions_delete:
    title: Youtube captions delete
    description: Deletes a resource. Calls DELETE /youtube/v3/captions on the provider's API.
    category: youtube
    request:
      method: DELETE
      path: /youtube/v3/captions
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        id: ${id}
        onBehalfOf: ${on_behalf_of}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      id:
        type: string
        description: The id parameter.
      on_behalf_of:
        type: string
        description: ID of the Google+ Page for the channel that the request is be on behalf of
        optional: true
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
    output:
      description: Successful response
      type: object
    destructive: true
  youtube_captions_list:
    title: Youtube captions list
    description: Retrieves a list of resources, possibly filtered. Calls GET /youtube/v3/captions on the
      provider's API.
    category: youtube
    request:
      method: GET
      path: /youtube/v3/captions
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        videoId: ${video_id}
        id: ${id}
        onBehalfOf: ${on_behalf_of}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter specifies a comma-separated list of one or more caption resource
          parts that the API response will include. The part names that you can include in the parameter
          value are id and snippet.
        items:
          type: string
      video_id:
        type: string
        description: Returns the captions for the specified video.
      id:
        type: array
        description: Returns the captions with the given IDs for Stubby or Apiary.
        optional: true
        items:
          type: string
      on_behalf_of:
        type: string
        description: ID of the Google+ Page for the channel that the request is on behalf of.
        optional: true
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
    output:
      description: Successful response
      type: object
    read_only: true
  youtube_captions_insert:
    title: Youtube captions insert
    description: Inserts a new resource into this collection. Calls POST /youtube/v3/captions on the provider's
      API.
    category: youtube
    request:
      method: POST
      path: /youtube/v3/captions
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        onBehalfOf: ${on_behalf_of}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
        sync: ${sync}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter specifies the caption resource parts that the API response will
          include. Set the parameter value to snippet.
        items:
          type: string
      on_behalf_of:
        type: string
        description: ID of the Google+ Page for the channel that the request is be on behalf of
        optional: true
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
      sync:
        type: boolean
        description: Extra parameter to allow automatically syncing the uploaded caption/transcript with
          the audio.
        optional: true
    output:
      description: Successful response
      type: object
  youtube_captions_update:
    title: Youtube captions update
    description: Updates an existing resource. Calls PUT /youtube/v3/captions on the provider's API.
    category: youtube
    request:
      method: PUT
      path: /youtube/v3/captions
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        onBehalfOf: ${on_behalf_of}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
        sync: ${sync}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter specifies a comma-separated list of one or more caption resource
          parts that the API response will include. The part names that you can include in the parameter
          value are id and snippet.
        items:
          type: string
      on_behalf_of:
        type: string
        description: ID of the Google+ Page for the channel that the request is on behalf of.
        optional: true
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
      sync:
        type: boolean
        description: Extra parameter to allow automatically syncing the uploaded caption/transcript with
          the audio.
        optional: true
    output:
      description: Successful response
      type: object
  youtube_channel_sections_delete:
    title: Youtube channel sections delete
    description: Deletes a resource. Calls DELETE /youtube/v3/channelSections on the provider's API.
    category: youtube
    request:
      method: DELETE
      path: /youtube/v3/channelSections
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        id: ${id}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      id:
        type: string
        description: The id parameter.
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
    output:
      description: Successful response
      type: object
    destructive: true
  youtube_channel_sections_list:
    title: Youtube channel sections list
    description: Retrieves a list of resources, possibly filtered. Calls GET /youtube/v3/channelSections
      on the provider's API.
    category: youtube
    request:
      method: GET
      path: /youtube/v3/channelSections
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        channelId: ${channel_id}
        hl: ${hl}
        id: ${id}
        mine: ${mine}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter specifies a comma-separated list of one or more channelSection
          resource properties that the API response will include. The part names that you can include
          in the parameter value are id, snippet, and contentDetails. If the parameter identifies a property
          that contains child properties, the child properties will be included in the response. For example,
          in a channelSection resourc
        items:
          type: string
      channel_id:
        type: string
        description: Return the ChannelSections owned by the specified channel ID.
        optional: true
      hl:
        type: string
        description: Return content in specified language
        optional: true
      id:
        type: array
        description: Return the ChannelSections with the given IDs for Stubby or Apiary.
        optional: true
        items:
          type: string
      mine:
        type: boolean
        description: Return the ChannelSections owned by the authenticated user.
        optional: true
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
    output:
      description: Successful response
      type: object
    read_only: true
  youtube_channel_sections_insert:
    title: Youtube channel sections insert
    description: Inserts a new resource into this collection. Calls POST /youtube/v3/channelSections on
      the provider's API.
    category: youtube
    request:
      method: POST
      path: /youtube/v3/channelSections
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
        onBehalfOfContentOwnerChannel: ${on_behalf_of_content_owner_channel}
      body:
        contentDetails: ${content_details}
        etag: ${etag}
        id: ${id}
        kind: ${kind}
        localizations: ${localizations}
        snippet: ${snippet}
        targeting: ${targeting}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter serves two purposes in this operation. It identifies the properties
          that the write operation will set as well as the properties that the API response will include.
          The part names that you can include in the parameter value are snippet and contentDetails.
        items:
          type: string
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
      on_behalf_of_content_owner_channel:
        type: string
        description: 'This parameter can only be used in a properly authorized request. Note: This parameter
          is intended exclusively for YouTube content partners. The onBehalfOfContentOwnerChannel parameter
          specifies the YouTube channel ID of the channel to which a video is being added. This parameter
          is required when a request specifies a value for the onBehalfOfContentOwner parameter, and it
          can only be used in conju'
        optional: true
      content_details:
        type: object
        description: Details about a channelsection, including playlists and channels.
        optional: true
      etag:
        type: string
        description: Etag of this resource.
        optional: true
      id:
        type: string
        description: The ID that YouTube uses to uniquely identify the channel section.
        optional: true
      kind:
        type: string
        description: 'Identifies what kind of resource this is. Value: the fixed string "youtubechannelSection".'
        optional: true
      localizations:
        type: object
        description: Localizations for different languages
        optional: true
      snippet:
        type: object
        description: Basic details about a channel section, including title, style and position.
        optional: true
      targeting:
        type: object
        description: ChannelSection targeting setting.
        optional: true
    output:
      description: Successful response
      type: object
  youtube_channel_sections_update:
    title: Youtube channel sections update
    description: Updates an existing resource. Calls PUT /youtube/v3/channelSections on the provider's
      API.
    category: youtube
    request:
      method: PUT
      path: /youtube/v3/channelSections
      query:
        $.xgafv: ${xgafv}
        access_token: ${access_token}
        alt: ${alt}
        callback: ${callback}
        fields: ${fields}
        key: ${key}
        oauth_token: ${oauth_token}
        prettyPrint: ${pretty_print}
        quotaUser: ${quota_user}
        upload_protocol: ${upload_protocol}
        uploadType: ${upload_type}
        part: ${part}
        onBehalfOfContentOwner: ${on_behalf_of_content_owner}
      body:
        contentDetails: ${content_details}
        etag: ${etag}
        id: ${id}
        kind: ${kind}
        localizations: ${localizations}
        snippet: ${snippet}
        targeting: ${targeting}
    input:
      xgafv:
        type: string
        description: V1 error format.
        optional: true
        enum:
        - '1'
        - '2'
      access_token:
        type: string
        description: OAuth access token.
        optional: true
      alt:
        type: string
        description: Data format for response.
        optional: true
        enum:
        - json
        - media
        - proto
      callback:
        type: string
        description: JSONP
        optional: true
      fields:
        type: string
        description: Selector specifying which fields to include in a partial response.
        optional: true
      key:
        type: string
        description: API key. Your API key identifies your project and provides you with API access, quota,
          and reports. Required unless you provide an OAuth 2.0 token.
        optional: true
      oauth_token:
        type: string
        description: OAuth 2.0 token for the current user.
        optional: true
      pretty_print:
        type: boolean
        description: Returns response with indentations and line breaks.
        optional: true
      quota_user:
        type: string
        description: Available to use for quota purposes for server-side applications. Can be any arbitrary
          string assigned to a user, but should not exceed 40 characters.
        optional: true
      upload_protocol:
        type: string
        description: Upload protocol for media (e.g. "raw", "multipart").
        optional: true
      upload_type:
        type: string
        description: Legacy upload protocol for media (e.g. "media", "multipart").
        optional: true
      part:
        type: array
        description: The part parameter serves two purposes in this operation. It identifies the properties
          that the write operation will set as well as the properties that the API response will include.
          The part names that you can include in the parameter value are snippet and contentDetails.
        items:
          type: string
      on_behalf_of_content_owner:
        type: string
        description: 'Note: This parameter is intended exclusively for YouTube content partners. The onBehalfOfContentOwner
          parameter indicates that the request''s authorization credentials identify a YouTube CMS user
          who is acting on behalf of the content owner specified in the parameter value. This parameter
          is intended for YouTube content partners that own and manage many different YouTube channels.
          It allows content'
        optional: true
      content_details:
        type: object
        description: Details about a channelsection, including playlists and channels.
        optional: true
      etag:
        type: string
        description: Etag of this resource.
        optional: true
      id:
        type: string
        description: The ID that YouTube uses to uniquely identify the channel section.
        optional: true
      kind:
        type: string
        description: 'Identifies what kind of resource this is. Value: the fixed string "youtubechannelSection".'
        optional: true
      localizations:
        type: object
        description: Localizations for different languages
        optional: true
      snippet:
        type: object
        description: Basic details about a channel section, including title, style and position.
        optional: true
      targeting:
        type: object
        description: ChannelSection targeting setting.
        optional: true
    output:
      description: Successful response
      type: object
