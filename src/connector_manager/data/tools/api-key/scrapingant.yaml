# ScrapingAnt tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.scrapingant.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: scrapingant
display_name: ScrapingAnt
docs_url: https://api.scrapingant.com/openapi.json
generated: true
generated_from: https://api.scrapingant.com/openapi.json
tools:
  scrapingant_general_request_v2_general_post:
    title: Scrapingant General Request
    description: Scrapingant General Request Calls DELETE /v2/general on the provider's API.
    category: general
    request:
      method: DELETE
      path: /v2/general
      query:
        x-api-key: ${x_api_key}
        url: ${url}
        browser: ${browser}
        return_page_source: ${return_page_source}
        cookies: ${cookies}
        js_snippet: ${js_snippet}
        proxy_type: ${proxy_type}
        proxy_country: ${proxy_country}
        wait_for_selector: ${wait_for_selector}
        block_resource: ${block_resource}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
      url:
        type: string
        description: URL to scrape
      browser:
        type: boolean
        description: 'Enables using headless browser for scraping. Default: true'
        optional: true
      return_page_source:
        type: boolean
        description: 'Enables returning data returned by the server and unaltered by the browser. Default:
          false. When true - JS won''t be rendered. This feature works only with browser=true'
        optional: true
      cookies:
        type: string
        description: Cookies to pass with a scraping request to the target site.
        optional: true
      js_snippet:
        type: string
        description: Base64 encoded JS snippet to run once page being loaded in the ScrapingAnt browser.
          This feature works only with browser=true.
        optional: true
      proxy_type:
        type: string
        description: 'Specifies proxy type to make request from. Default: datacenter. one of: datacenter,
          residential'
        optional: true
      proxy_country:
        type: string
        description: Specifies proxy country to make request from. If not specified, the request will
          be made from a random country.
        optional: true
      wait_for_selector:
        type: string
        description: The CSS selector of the element our service will wait for before returning result.
          This feature works only with browser=true
        optional: true
      block_resource:
        type: string
        description: 'Block resource types. Prevents cloud browser from loading specified resource types.
          Can be repeated in case several resource types should be blocked. This feature works only with
          browser=true. Available resource types: document, stylesheet, image, media, font, script, texttrack,
          xhr, fetch, eventsource, websocket, manifest, other'
        optional: true
    output:
      description: Successful response. ScrapingAnt web scraping API endpoint returns plain html content
        of the scraped web page
      type: object
    destructive: true
  scrapingant_general_request_v2_general_post_alt:
    title: Scrapingant General Request
    description: Scrapingant General Request Calls GET /v2/general on the provider's API.
    category: general
    request:
      method: GET
      path: /v2/general
      query:
        x-api-key: ${x_api_key}
        url: ${url}
        browser: ${browser}
        return_page_source: ${return_page_source}
        cookies: ${cookies}
        js_snippet: ${js_snippet}
        proxy_type: ${proxy_type}
        proxy_country: ${proxy_country}
        wait_for_selector: ${wait_for_selector}
        block_resource: ${block_resource}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
      url:
        type: string
        description: URL to scrape
      browser:
        type: boolean
        description: 'Enables using headless browser for scraping. Default: true'
        optional: true
      return_page_source:
        type: boolean
        description: 'Enables returning data returned by the server and unaltered by the browser. Default:
          false. When true - JS won''t be rendered. This feature works only with browser=true'
        optional: true
      cookies:
        type: string
        description: Cookies to pass with a scraping request to the target site.
        optional: true
      js_snippet:
        type: string
        description: Base64 encoded JS snippet to run once page being loaded in the ScrapingAnt browser.
          This feature works only with browser=true.
        optional: true
      proxy_type:
        type: string
        description: 'Specifies proxy type to make request from. Default: datacenter. one of: datacenter,
          residential'
        optional: true
      proxy_country:
        type: string
        description: Specifies proxy country to make request from. If not specified, the request will
          be made from a random country.
        optional: true
      wait_for_selector:
        type: string
        description: The CSS selector of the element our service will wait for before returning result.
          This feature works only with browser=true
        optional: true
      block_resource:
        type: string
        description: 'Block resource types. Prevents cloud browser from loading specified resource types.
          Can be repeated in case several resource types should be blocked. This feature works only with
          browser=true. Available resource types: document, stylesheet, image, media, font, script, texttrack,
          xhr, fetch, eventsource, websocket, manifest, other'
        optional: true
    output:
      description: Successful response. ScrapingAnt web scraping API endpoint returns plain html content
        of the scraped web page
      type: object
    read_only: true
  scrapingant_general_request_v2_general_post_alt_alt:
    title: Scrapingant General Request
    description: Scrapingant General Request Calls PATCH /v2/general on the provider's API.
    category: general
    request:
      method: PATCH
      path: /v2/general
      query:
        x-api-key: ${x_api_key}
        url: ${url}
        browser: ${browser}
        return_page_source: ${return_page_source}
        cookies: ${cookies}
        js_snippet: ${js_snippet}
        proxy_type: ${proxy_type}
        proxy_country: ${proxy_country}
        wait_for_selector: ${wait_for_selector}
        block_resource: ${block_resource}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
      url:
        type: string
        description: URL to scrape
      browser:
        type: boolean
        description: 'Enables using headless browser for scraping. Default: true'
        optional: true
      return_page_source:
        type: boolean
        description: 'Enables returning data returned by the server and unaltered by the browser. Default:
          false. When true - JS won''t be rendered. This feature works only with browser=true'
        optional: true
      cookies:
        type: string
        description: Cookies to pass with a scraping request to the target site.
        optional: true
      js_snippet:
        type: string
        description: Base64 encoded JS snippet to run once page being loaded in the ScrapingAnt browser.
          This feature works only with browser=true.
        optional: true
      proxy_type:
        type: string
        description: 'Specifies proxy type to make request from. Default: datacenter. one of: datacenter,
          residential'
        optional: true
      proxy_country:
        type: string
        description: Specifies proxy country to make request from. If not specified, the request will
          be made from a random country.
        optional: true
      wait_for_selector:
        type: string
        description: The CSS selector of the element our service will wait for before returning result.
          This feature works only with browser=true
        optional: true
      block_resource:
        type: string
        description: 'Block resource types. Prevents cloud browser from loading specified resource types.
          Can be repeated in case several resource types should be blocked. This feature works only with
          browser=true. Available resource types: document, stylesheet, image, media, font, script, texttrack,
          xhr, fetch, eventsource, websocket, manifest, other'
        optional: true
    output:
      description: Successful response. ScrapingAnt web scraping API endpoint returns plain html content
        of the scraped web page
      type: object
  scrapingant_general_request_v2_general_post_alt_alt_alt:
    title: Scrapingant General Request
    description: Scrapingant General Request Calls POST /v2/general on the provider's API.
    category: general
    request:
      method: POST
      path: /v2/general
      query:
        x-api-key: ${x_api_key}
        url: ${url}
        browser: ${browser}
        return_page_source: ${return_page_source}
        cookies: ${cookies}
        js_snippet: ${js_snippet}
        proxy_type: ${proxy_type}
        proxy_country: ${proxy_country}
        wait_for_selector: ${wait_for_selector}
        block_resource: ${block_resource}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
      url:
        type: string
        description: URL to scrape
      browser:
        type: boolean
        description: 'Enables using headless browser for scraping. Default: true'
        optional: true
      return_page_source:
        type: boolean
        description: 'Enables returning data returned by the server and unaltered by the browser. Default:
          false. When true - JS won''t be rendered. This feature works only with browser=true'
        optional: true
      cookies:
        type: string
        description: Cookies to pass with a scraping request to the target site.
        optional: true
      js_snippet:
        type: string
        description: Base64 encoded JS snippet to run once page being loaded in the ScrapingAnt browser.
          This feature works only with browser=true.
        optional: true
      proxy_type:
        type: string
        description: 'Specifies proxy type to make request from. Default: datacenter. one of: datacenter,
          residential'
        optional: true
      proxy_country:
        type: string
        description: Specifies proxy country to make request from. If not specified, the request will
          be made from a random country.
        optional: true
      wait_for_selector:
        type: string
        description: The CSS selector of the element our service will wait for before returning result.
          This feature works only with browser=true
        optional: true
      block_resource:
        type: string
        description: 'Block resource types. Prevents cloud browser from loading specified resource types.
          Can be repeated in case several resource types should be blocked. This feature works only with
          browser=true. Available resource types: document, stylesheet, image, media, font, script, texttrack,
          xhr, fetch, eventsource, websocket, manifest, other'
        optional: true
    output:
      description: Successful response. ScrapingAnt web scraping API endpoint returns plain html content
        of the scraped web page
      type: object
  scrapingant_general_request_v2_general_post_alt_alt_alt_alt:
    title: Scrapingant General Request
    description: Scrapingant General Request Calls PUT /v2/general on the provider's API.
    category: general
    request:
      method: PUT
      path: /v2/general
      query:
        x-api-key: ${x_api_key}
        url: ${url}
        browser: ${browser}
        return_page_source: ${return_page_source}
        cookies: ${cookies}
        js_snippet: ${js_snippet}
        proxy_type: ${proxy_type}
        proxy_country: ${proxy_country}
        wait_for_selector: ${wait_for_selector}
        block_resource: ${block_resource}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
      url:
        type: string
        description: URL to scrape
      browser:
        type: boolean
        description: 'Enables using headless browser for scraping. Default: true'
        optional: true
      return_page_source:
        type: boolean
        description: 'Enables returning data returned by the server and unaltered by the browser. Default:
          false. When true - JS won''t be rendered. This feature works only with browser=true'
        optional: true
      cookies:
        type: string
        description: Cookies to pass with a scraping request to the target site.
        optional: true
      js_snippet:
        type: string
        description: Base64 encoded JS snippet to run once page being loaded in the ScrapingAnt browser.
          This feature works only with browser=true.
        optional: true
      proxy_type:
        type: string
        description: 'Specifies proxy type to make request from. Default: datacenter. one of: datacenter,
          residential'
        optional: true
      proxy_country:
        type: string
        description: Specifies proxy country to make request from. If not specified, the request will
          be made from a random country.
        optional: true
      wait_for_selector:
        type: string
        description: The CSS selector of the element our service will wait for before returning result.
          This feature works only with browser=true
        optional: true
      block_resource:
        type: string
        description: 'Block resource types. Prevents cloud browser from loading specified resource types.
          Can be repeated in case several resource types should be blocked. This feature works only with
          browser=true. Available resource types: document, stylesheet, image, media, font, script, texttrack,
          xhr, fetch, eventsource, websocket, manifest, other'
        optional: true
    output:
      description: Successful response. ScrapingAnt web scraping API endpoint returns plain html content
        of the scraped web page
      type: object
  scrapingant_usage_v2_usage_get:
    title: Scrapingant Usage
    description: Scrapingant Usage Get current status of API credits usage Calls GET /v2/usage on the
      provider's API.
    category: usage
    request:
      method: GET
      path: /v2/usage
      query:
        x-api-key: ${x_api_key}
    input:
      x_api_key:
        type: string
        description: API token, required for authentication. You can find your API token on the dashboard
          page in the user's admin panel.
    output:
      description: Successful Response
      type: object
    read_only: true
