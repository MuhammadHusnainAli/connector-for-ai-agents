# Atlas.so tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.atlas.so/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: atlas-so
display_name: Atlas.so
docs_url: https://api.atlas.so/openapi.json
generated: true
generated_from: https://api.atlas.so/openapi.json
tools:
  list_api_v1_accounts_get:
    title: List all accounts
    description: List all accounts Lists all accounts Calls GET /v1/accounts on the provider's API.
    category: accounts
    request:
      method: GET
      path: /v1/accounts
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
    output:
      description: Successful Response
      type: object
    read_only: true
  list_canned_responses_v1_canned_responses_get:
    title: List all canned responses
    description: List all canned responses Lists all canned responses Calls GET /v1/canned_responses on
      the provider's API.
    category: canned_responses
    request:
      method: GET
      path: /v1/canned_responses
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
    output:
      description: Successful Response
      type: object
    read_only: true
  create_api_v1_canned_responses_post:
    title: Create canned response
    description: Create canned response Creates canned response Calls POST /v1/canned_responses on the
      provider's API.
    category: canned_responses
    request:
      method: POST
      path: /v1/canned_responses
      body:
        name: ${name}
        content: ${content}
    input:
      name:
        type: string
        description: The name field of the request body.
      content:
        type: string
        description: The content field of the request body.
    output:
      description: Successful Response
      type: object
  list_conversations_v1_conversations_get:
    title: List all conversations
    description: List all conversations Lists all conversations Calls GET /v1/conversations on the provider's
      API.
    category: conversations
    request:
      method: GET
      path: /v1/conversations
      query:
        cursor: ${cursor}
        limit: ${limit}
        assignedAgentId: ${assigned_agent_id}
        status: ${status}
        customerId: ${customer_id}
        startDate: ${start_date}
        endDate: ${end_date}
        subject: ${subject}
        assignedToZeus: ${assigned_to_zeus}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      assigned_agent_id:
        type: string
        description: The assignedAgentId parameter.
        optional: true
      status:
        type: string
        description: The status parameter.
        optional: true
      customer_id:
        type: string
        description: The customerId parameter.
        optional: true
      start_date:
        type: string
        description: The startDate parameter.
        optional: true
      end_date:
        type: string
        description: The endDate parameter.
        optional: true
      subject:
        type: string
        description: The subject parameter.
        optional: true
      assigned_to_zeus:
        type: string
        description: The assignedToZeus parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  create_conversation_v1_conversations_post:
    title: Create conversation
    description: Create conversation Creates conversation Calls POST /v1/conversations on the provider's
      API.
    category: conversations
    request:
      method: POST
      path: /v1/conversations
      body:
        customerId: ${customer_id}
        startedAt: ${started_at}
        subject: ${subject}
        messages: ${messages}
        status: ${status}
        priority: ${priority}
        tags: ${tags}
        customFields: ${custom_fields}
        assignedAgentId: ${assigned_agent_id}
        assignedTeamId: ${assigned_team_id}
    input:
      customer_id:
        type: string
        description: The customerId field of the request body.
      started_at:
        type: string
        description: The startedAt field of the request body.
        optional: true
      subject:
        type: string
        description: The subject field of the request body.
        optional: true
      messages:
        type: array
        description: The messages field of the request body.
        items:
          type: object
      status:
        type: string
        description: The status field of the request body.
        optional: true
      priority:
        type: string
        description: The priority field of the request body.
        optional: true
      tags:
        type: string
        description: The tags field of the request body.
        optional: true
      custom_fields:
        type: string
        description: The customFields field of the request body.
        optional: true
      assigned_agent_id:
        type: string
        description: The assignedAgentId field of the request body.
        optional: true
      assigned_team_id:
        type: string
        description: The assignedTeamId field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_csat_surveys_v1_csat_surveys_get:
    title: List CSAT surveys
    description: List CSAT surveys Lists CSAT surveys sent to customers. Filters startdate and enddate
      apply to when the survey was requested, not when the response was received. Use hasresponse to filter
      by whether the customer has responded. Calls GET /v1/csat-surveys on the provider's API.
    category: csat_surveys
    request:
      method: GET
      path: /v1/csat-surveys
      query:
        cursor: ${cursor}
        limit: ${limit}
        startDate: ${start_date}
        endDate: ${end_date}
        customerId: ${customer_id}
        conversationId: ${conversation_id}
        hasResponse: ${has_response}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
      start_date:
        type: string
        description: The startDate parameter.
        optional: true
      end_date:
        type: string
        description: The endDate parameter.
        optional: true
      customer_id:
        type: string
        description: The customerId parameter.
        optional: true
      conversation_id:
        type: string
        description: The conversationId parameter.
        optional: true
      has_response:
        type: string
        description: The hasResponse parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  list_api_v1_custom_fields_get:
    title: List all custom fields
    description: List all custom fields Lists all custom fields Calls GET /v1/custom-fields on the provider's
      API.
    category: custom_fields
    request:
      method: GET
      path: /v1/custom-fields
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
    output:
      description: Successful Response
      type: object
    read_only: true
  create_api_v1_custom_fields_post:
    title: Create custom field
    description: Create custom field Creates custom field Calls POST /v1/custom-fields on the provider's
      API.
    category: custom_fields
    request:
      method: POST
      path: /v1/custom-fields
      body:
        key: ${key}
        companyId: ${company_id}
        displayName: ${display_name}
        description: ${description}
        category: ${category}
        fieldMetadata: ${field_metadata}
        dynamicFieldMetadata: ${dynamic_field_metadata}
        type: ${type}
        required: ${required}
        editableBy: ${editable_by}
        active: ${active}
        rules: ${rules}
    input:
      key:
        type: string
        description: The key field of the request body.
        optional: true
      company_id:
        type: string
        description: The companyId field of the request body.
        optional: true
      display_name:
        type: string
        description: The displayName field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
        optional: true
      category:
        type: string
        description: The category field of the request body.
        optional: true
      field_metadata:
        type: string
        description: The fieldMetadata field of the request body.
        optional: true
      dynamic_field_metadata:
        type: string
        description: The dynamicFieldMetadata field of the request body.
        optional: true
      type:
        type: string
        description: The type field of the request body.
        optional: true
      required:
        type: string
        description: The required field of the request body.
        optional: true
      editable_by:
        type: string
        description: The editableBy field of the request body.
        optional: true
      active:
        type: string
        description: The active field of the request body.
        optional: true
      rules:
        type: string
        description: The rules field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_api_v1_customers_get:
    title: List all customers
    description: List all customers Lists all customers Calls GET /v1/customers on the provider's API.
    category: customers
    request:
      method: GET
      path: /v1/customers
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: integer
        description: The cursor parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
    output:
      description: Successful Response
      type: object
    read_only: true
