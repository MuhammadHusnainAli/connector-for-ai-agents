# Ahrefs tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://docs.ahrefs.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: ahrefs
display_name: Ahrefs
docs_url: https://ahrefs.com/terms
generated: true
generated_from: https://docs.ahrefs.com/openapi.json
tools:
  batch_analysis_batch_analysis:
    title: Batch Analysis
    description: Batch Analysis Calls POST /v3/batch-analysis/batch-analysis on Ahrefs's API.
    category: batch_analysis
    request:
      method: POST
      path: /v3/batch-analysis/batch-analysis
      body:
        select: ${select}
        order_by: ${order_by}
        top_countries: ${top_countries}
        country: ${country}
        volume_mode: ${volume_mode}
        targets: ${targets}
        output: ${output}
    input:
      select:
        type: array
        description: A list of fields to return. - ahrefsrank - backlinks - backlinksdofollow - backlinksinternal
          - backlinksnofollow - backlinksredirect - domainrating - index - ip - linkeddomains - linkeddomainsdofollow
          - mode - orgcost (10 units) - orgkeywords - orgkeywords1120 - orgkeywords13 - orgkeywords2150
          - orgkeywords410 - orgkeywords51plus - orgtraffic (10 units) - orgtraffictopbycountry (10 units
          per count
        items:
          type: string
      order_by:
        type: array
        description: 'A list of fields to order the results by, with optional direction. Format: fieldname:asc
          or fieldname:desc (default desc). - ahrefsrank - backlinks - backlinksdofollow - backlinksinternal
          - backlinksnofollow - backlinksredirect - domainrating - index - ip - linkeddomains - linkeddomainsdofollow
          - mode - orgcost (10 units) - orgkeywords - orgkeywords1120 - orgkeywords13 - orgkeywords2150
          - orgkeywo'
        optional: true
        items:
          type: string
      top_countries:
        type: integer
        description: 'The number of countries to return in the orgtraffictopbycountry field. Max: 10.'
        optional: true
      country:
        type: string
        description: A two-letter country code (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      volume_mode:
        type: string
        description: 'The search volume calculation mode: monthly or average. It affects volume, traffic,
          and traffic value.'
        optional: true
        enum:
        - monthly
        - average
      targets:
        type: array
        description: A list of targets to do batch analysis.
        items:
          type: object
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - php
    output:
      description: OK
      type: object
  brand_radar_ai_responses:
    title: AI Responses
    description: 'AI Responses Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls GET /v3/brand-radar/ai-responses on Ahrefs''s API.'
    category: brand_radar
    request:
      method: GET
      path: /v3/brand-radar/ai-responses
      query:
        select: ${select}
        where: ${where}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        order_by: ${order_by}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        competitors: ${competitors}
        brand: ${brand}
        output: ${output}
    input:
      select:
        type: string
        description: A comma-separated list of fields to return. - country - datasource - lastupdated
          - links (10 units) - question - response - searchqueries - tags - volume (10 units)
      where:
        type: string
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
        default: 1000
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
        default: ask_volume
      country:
        type: string
        description: A comma-separated list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      order_by:
        type: string
        description: A column to order the results by.
        optional: true
        enum:
        - relevance
        - volume
        default: relevance
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: string
        description: A comma-separated list of chatbot models. All models can be combined with each other.
          claude module supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords
          models report AI Overviews / AI Mode visibility derived from Google search queries (keywords)
          rather than prompts.
        enum:
        - chatgpt
        - google_ai_overviews
        - google_ai_mode
        - gemini
        - perplexity
        - copilot
        - claude
        - grok
        - google_ai_overviews_keywords
        - google_ai_mode_keywords
      market:
        type: string
        description: A comma-separated list of the niche markets of your brands. Deprecated on 2026-05-18,
          this parameter will have no effect shortly after this date.
        optional: true
        default: ''
      competitors:
        type: string
        description: A comma-separated list of competitors of your brands.
        optional: true
        default: ''
      brand:
        type: string
        description: A comma-separated list of brands to search for. At least one of brand, competitors,
          market or where should not be empty.
        optional: true
        default: ''
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - php
    output:
      description: OK
      type: object
    read_only: true
  brand_radar_ai_responses_post:
    title: AI Responses
    description: 'AI Responses Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls POST /v3/brand-radar/ai-responses on Ahrefs''s API.'
    category: brand_radar
    request:
      method: POST
      path: /v3/brand-radar/ai-responses
      body:
        brand_filter: ${brand_filter}
        volume_range: ${volume_range}
        select: ${select}
        where: ${where}
        tags_filter: ${tags_filter}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        order_by: ${order_by}
        report_id: ${report_id}
        prompts: ${prompts}
    input:
      brand_filter:
        type: object
        description: 'A filter expression for your brand''s visibility, mirroring the "Your brand" filter.
          brandname matches whether a response mentions your brand: "mentioned" or "notmentioned". pagestatus
          matches how the AI used your pages: "cited" (the AI retrieved pages from your site and referenced
          them in the answer), "foundbutnotcited" (the AI retrieved pages from your site as potential
          sources but did not refere'
        optional: true
      volume_range:
        type: object
        description: The volume range to filter by.
        optional: true
      select:
        type: array
        description: A list of fields to return. - country - datasource - lastupdated - links (10 units)
          - question - response - searchqueries - tags - volume (10 units)
        items:
          type: string
      where:
        type: object
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      tags_filter:
        type: object
        description: 'A filter expression for prompt tags. Requires reportid. Uses filter syntax with
          the following restrictions: the only valid field name is "tag"; the only valid operator are:
          "eq", "neq", "substring", "isubstring", "phrasematch", "iphrasematch", "prefix", "suffix", "empty";
          maximum nesting depth of and, or is 2.'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
      country:
        type: array
        description: A list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        items:
          type: string
      order_by:
        type: string
        description: A column to order the results by.
        optional: true
        enum:
        - relevance
        - volume
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
    output:
      description: OK
      type: object
  brand_radar_citations_history:
    title: Overview history - Citations
    description: 'Overview history - Citations Requests to this endpoint consume API units based on the
      prompts parameter: requests returning only custom prompt data are free, while requests including
      Ahrefs prompt data follow standard API unit pricing. Every entity provided in brands (and competitors,
      when applicable) must include at least one Calls POST /v3/brand-radar/citations-history on Ahrefs''s
      API.'
    category: brand_radar
    request:
      method: POST
      path: /v3/brand-radar/citations-history
      body:
        where: ${where}
        tags_filter: ${tags_filter}
        date_to: ${date_to}
        date_from: ${date_from}
        search_volume_type: ${search_volume_type}
        country: ${country}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        brands: ${brands}
        output: ${output}
    input:
      where:
        type: object
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      tags_filter:
        type: object
        description: 'A filter expression for prompt tags. Requires reportid. Uses filter syntax with
          the following restrictions: the only valid field name is "tag"; the only valid operator are:
          "eq", "neq", "substring", "isubstring", "phrasematch", "iphrasematch", "prefix", "suffix", "empty";
          maximum nesting depth of and, or is 2.'
        optional: true
      date_to:
        type: string
        description: The end date of the historical period in YYYY-MM-DD format.
        optional: true
      date_from:
        type: string
        description: The start date of the historical period in YYYY-MM-DD format.
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
      country:
        type: array
        description: A list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        items:
          type: string
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (market, country, filters). If country or filters are provided, they override the ones
          in the report. You can find it in the URL of your Brand Radar report in Ahrefs: https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: array
        description: A list of chatbot models. All models can be combined with each other. claude module
          supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords models
          report AI Overviews / AI Mode visibility derived from Google search queries (keywords) rather
          than prompts.
        items:
          type: string
      market:
        type: array
        description: A list of the niche markets of your brands. Deprecated on 2026-05-18, this parameter
          will have no effect shortly after this date.
        optional: true
        items:
          type: string
      brands:
        type: array
        description: A list of brand names and websites to search for.
        optional: true
        items:
          type: object
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - csv
        - xml
        - php
    output:
      description: OK
      type: object
  brand_radar_citations_overview:
    title: Overview - Citations
    description: 'Overview - Citations Requests to this endpoint consume API units based on the prompts
      parameter: requests returning only custom prompt data are free, while requests including Ahrefs
      prompt data follow standard API unit pricing. Every entity provided in brands (and competitors,
      when applicable) must include at least one Calls POST /v3/brand-radar/citations-overview on Ahrefs''s
      API.'
    category: brand_radar
    request:
      method: POST
      path: /v3/brand-radar/citations-overview
      body:
        where: ${where}
        select: ${select}
        tags_filter: ${tags_filter}
        search_volume_type: ${search_volume_type}
        country: ${country}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        competitors: ${competitors}
        brands: ${brands}
        output: ${output}
    input:
      where:
        type: object
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      select:
        type: array
        description: A list of fields to return. - brand - notrackedbrands - onlycompetitorsbrands - onlytargetbrand
          - targetandcompetitorsbrands - total
        items:
          type: string
      tags_filter:
        type: object
        description: 'A filter expression for prompt tags. Requires reportid. Uses filter syntax with
          the following restrictions: the only valid field name is "tag"; the only valid operator are:
          "eq", "neq", "substring", "isubstring", "phrasematch", "iphrasematch", "prefix", "suffix", "empty";
          maximum nesting depth of and, or is 2.'
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
      country:
        type: array
        description: A list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        items:
          type: string
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: array
        description: A list of chatbot models. All models can be combined with each other. claude module
          supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords models
          report AI Overviews / AI Mode visibility derived from Google search queries (keywords) rather
          than prompts.
        items:
          type: string
      market:
        type: array
        description: A list of the niche markets of your brands. Deprecated on 2026-05-18, this parameter
          will have no effect shortly after this date.
        optional: true
        items:
          type: string
      competitors:
        type: array
        description: A list of competitor names and websites to search for.
        optional: true
        items:
          type: object
      brands:
        type: array
        description: A list of brand names and websites to search for.
        optional: true
        items:
          type: object
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - csv
        - xml
        - php
    output:
      description: OK
      type: object
  brand_radar_cited_domains:
    title: Cited Domains
    description: 'Cited Domains Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls GET /v3/brand-radar/cited-domains on Ahrefs''s API.'
    category: brand_radar
    request:
      method: GET
      path: /v3/brand-radar/cited-domains
      query:
        select: ${select}
        where: ${where}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        competitors: ${competitors}
        brand: ${brand}
        output: ${output}
    input:
      select:
        type: string
        description: A comma-separated list of fields to return. - domain - pages - responses
      where:
        type: string
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
        default: 1000
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
        default: ask_volume
      country:
        type: string
        description: A comma-separated list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: string
        description: A comma-separated list of chatbot models. All models can be combined with each other.
          claude module supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords
          models report AI Overviews / AI Mode visibility derived from Google search queries (keywords)
          rather than prompts.
        enum:
        - chatgpt
        - google_ai_overviews
        - google_ai_mode
        - gemini
        - perplexity
        - copilot
        - claude
        - grok
        - google_ai_overviews_keywords
        - google_ai_mode_keywords
      market:
        type: string
        description: A comma-separated list of the niche markets of your brands. Deprecated on 2026-05-18,
          this parameter will have no effect shortly after this date.
        optional: true
        default: ''
      competitors:
        type: string
        description: A comma-separated list of competitors of your brands.
        optional: true
        default: ''
      brand:
        type: string
        description: A comma-separated list of brands to search for. At least one of brand, competitors,
          market or where should not be empty.
        optional: true
        default: ''
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - php
    output:
      description: OK
      type: object
    read_only: true
  brand_radar_cited_domains_post:
    title: Cited Domains
    description: 'Cited Domains Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls POST /v3/brand-radar/cited-domains on Ahrefs''s API.'
    category: brand_radar
    request:
      method: POST
      path: /v3/brand-radar/cited-domains
      body:
        brand_filter: ${brand_filter}
        volume_range: ${volume_range}
        select: ${select}
        where: ${where}
        tags_filter: ${tags_filter}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
    input:
      brand_filter:
        type: object
        description: 'A filter expression for your brand''s visibility, mirroring the "Your brand" filter.
          brandname matches whether a response mentions your brand: "mentioned" or "notmentioned". pagestatus
          matches how the AI used your pages: "cited" (the AI retrieved pages from your site and referenced
          them in the answer), "foundbutnotcited" (the AI retrieved pages from your site as potential
          sources but did not refere'
        optional: true
      volume_range:
        type: object
        description: The volume range to filter by.
        optional: true
      select:
        type: array
        description: A list of fields to return. - domain - pages - responses
        items:
          type: string
      where:
        type: object
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      tags_filter:
        type: object
        description: 'A filter expression for prompt tags. Requires reportid. Uses filter syntax with
          the following restrictions: the only valid field name is "tag"; the only valid operator are:
          "eq", "neq", "substring", "isubstring", "phrasematch", "iphrasematch", "prefix", "suffix", "empty";
          maximum nesting depth of and, or is 2.'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
      country:
        type: array
        description: A list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        items:
          type: string
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: array
        description: A list of chatbot models. All models can be combined with each other. claude module
          supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords models
          report AI Overviews / AI Mode visibility derived from Google search queries (keywords) rather
          than prompts.
        items:
          type: string
    output:
      description: OK
      type: object
  brand_radar_cited_pages:
    title: Cited Pages
    description: 'Cited Pages Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls GET /v3/brand-radar/cited-pages on Ahrefs''s API.'
    category: brand_radar
    request:
      method: GET
      path: /v3/brand-radar/cited-pages
      query:
        select: ${select}
        where: ${where}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        tracked_urls: ${tracked_urls}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        competitors: ${competitors}
        brand: ${brand}
        output: ${output}
    input:
      select:
        type: string
        description: A comma-separated list of fields to return. - responses - url
      where:
        type: string
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
        default: 1000
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
        default: ask_volume
      country:
        type: string
        description: A comma-separated list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      tracked_urls:
        type: string
        description: A comma-separated list of tracked page URLs. When provided, the response includes
          rows with zero citations for any tracked URLs that have no data.
        optional: true
        default: ''
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: string
        description: A comma-separated list of chatbot models. All models can be combined with each other.
          claude module supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords
          models report AI Overviews / AI Mode visibility derived from Google search queries (keywords)
          rather than prompts.
        enum:
        - chatgpt
        - google_ai_overviews
        - google_ai_mode
        - gemini
        - perplexity
        - copilot
        - claude
        - grok
        - google_ai_overviews_keywords
        - google_ai_mode_keywords
      market:
        type: string
        description: A comma-separated list of the niche markets of your brands. Deprecated on 2026-05-18,
          this parameter will have no effect shortly after this date.
        optional: true
        default: ''
      competitors:
        type: string
        description: A comma-separated list of competitors of your brands.
        optional: true
        default: ''
      brand:
        type: string
        description: A comma-separated list of brands to search for. At least one of brand, competitors,
          market or where should not be empty.
        optional: true
        default: ''
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - php
    output:
      description: OK
      type: object
    read_only: true
  brand_radar_cited_pages_post:
    title: Cited Pages
    description: 'Cited Pages Requests to this endpoint consume API units based on the prompts parameter:
      requests returning only custom prompt data are free, while requests including Ahrefs prompt data
      follow standard API unit pricing. Calls POST /v3/brand-radar/cited-pages on Ahrefs''s API.'
    category: brand_radar
    request:
      method: POST
      path: /v3/brand-radar/cited-pages
      body:
        brand_filter: ${brand_filter}
        volume_range: ${volume_range}
        select: ${select}
        where: ${where}
        tags_filter: ${tags_filter}
        limit: ${limit}
        date: ${date}
        search_volume_type: ${search_volume_type}
        country: ${country}
        tracked_urls: ${tracked_urls}
        report_id: ${report_id}
        prompts: ${prompts}
    input:
      brand_filter:
        type: object
        description: 'A filter expression for your brand''s visibility, mirroring the "Your brand" filter.
          brandname matches whether a response mentions your brand: "mentioned" or "notmentioned". pagestatus
          matches how the AI used your pages: "cited" (the AI retrieved pages from your site and referenced
          them in the answer), "foundbutnotcited" (the AI retrieved pages from your site as potential
          sources but did not refere'
        optional: true
      volume_range:
        type: object
        description: The volume range to filter by.
        optional: true
      select:
        type: array
        description: A list of fields to return. - responses - url
        items:
          type: string
      where:
        type: object
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      tags_filter:
        type: object
        description: 'A filter expression for prompt tags. Requires reportid. Uses filter syntax with
          the following restrictions: the only valid field name is "tag"; the only valid operator are:
          "eq", "neq", "substring", "isubstring", "phrasematch", "iphrasematch", "prefix", "suffix", "empty";
          maximum nesting depth of and, or is 2.'
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
      date:
        type: string
        description: The date to search for in YYYY-MM-DD format.
        optional: true
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
      country:
        type: array
        description: A list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        items:
          type: string
      tracked_urls:
        type: array
        description: A list of tracked page URLs. When provided, the response includes rows with zero
          citations for any tracked URLs that have no data.
        optional: true
        items:
          type: string
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (brand, competitors, market, country, filters). If country or filters are provided, they
          override the ones in the report. You can find it in the URL of your Brand Radar report in Ahrefs:
          https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
    output:
      description: OK
      type: object
  brand_radar_impressions_history:
    title: Overview history - Impressions
    description: 'Overview history - Impressions Requests to this endpoint consume API units based on
      the prompts parameter: requests returning only custom prompt data are free, while requests including
      Ahrefs prompt data follow standard API unit pricing. Calls GET /v3/brand-radar/impressions-history
      on Ahrefs''s API.'
    category: brand_radar
    request:
      method: GET
      path: /v3/brand-radar/impressions-history
      query:
        where: ${where}
        date_to: ${date_to}
        date_from: ${date_from}
        search_volume_type: ${search_volume_type}
        country: ${country}
        report_id: ${report_id}
        prompts: ${prompts}
        data_source: ${data_source}
        market: ${market}
        brand: ${brand}
        output: ${output}
    input:
      where:
        type: string
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). citeddomain: The domain of a page
          that was used to generate the response. type: domain citeddomainsubdomains: The domain of a
          page that was used to generate the response. Any subdomain of the given domain will also match.
          type: string citedurlexact: The URL'
        optional: true
      date_to:
        type: string
        description: The end date of the historical period in YYYY-MM-DD format.
        optional: true
      date_from:
        type: string
        description: The start date of the historical period in YYYY-MM-DD format.
      search_volume_type:
        type: string
        description: AI visibility reports are switching to AI adjusted volume. It better estimates demand
          for AI responses across chatbots and AI search surfaces. It is calculated by adjusting Google
          search volume for each AI platform’s estimated usage compared with Google. This param will be
          deprecated on August 31, 2026; all requests will use new AI adjusted volume. askvolume - New
          calculation (default). Estimates
        optional: true
        enum:
        - ask_volume
        - keyword_volume
        default: ask_volume
      country:
        type: string
        description: A comma-separated list of two-letter country codes (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      report_id:
        type: string
        description: 'The ID of the report to use. If one is given, other parameters are taken from the
          report (market, country, filters). If country or filters are provided, they override the ones
          in the report. You can find it in the URL of your Brand Radar report in Ahrefs: https://app.ahrefs.com/brand-radar/reports/reportid/...'
        optional: true
      prompts:
        type: string
        description: The type of prompts to use. If not specified, both will be used. Custom prompts require
          a reportid to be provided.
        optional: true
        enum:
        - ahrefs
        - custom
      data_source:
        type: string
        description: A comma-separated list of chatbot models. All models can be combined with each other.
          claude module supports only custom prompts. The googleaioverviewskeywords and googleaimodekeywords
          models report AI Overviews / AI Mode visibility derived from Google search queries (keywords)
          rather than prompts.
        enum:
        - chatgpt
        - google_ai_overviews
        - google_ai_mode
        - gemini
        - perplexity
        - copilot
        - claude
        - grok
        - google_ai_overviews_keywords
        - google_ai_mode_keywords
      market:
        type: string
        description: A comma-separated list of the niche markets of your brands. Deprecated on 2026-05-18,
          this parameter will have no effect shortly after this date.
        optional: true
        default: ''
      brand:
        type: string
        description: The brand to search for.
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - csv
        - xml
        - php
    output:
      description: OK
      type: object
    read_only: true
  gsc_anonymous_queries:
    title: Anonymous queries
    description: Anonymous queries Calls GET /v3/gsc/anonymous-queries on Ahrefs's API.
    category: gsc
    request:
      method: GET
      path: /v3/gsc/anonymous-queries
      query:
        timeout: ${timeout}
        limit: ${limit}
        order_by: ${order_by}
        where: ${where}
        select: ${select}
        project_id: ${project_id}
        date_from: ${date_from}
        country: ${country}
        output: ${output}
    input:
      timeout:
        type: integer
        description: A manual timeout duration in seconds.
        optional: true
      limit:
        type: integer
        description: The number of results to return.
        optional: true
        default: 1000
      order_by:
        type: string
        description: A comma-separated list of columns to order results by, with optional direction. See
          response schema for valid column identifiers.
        optional: true
      where:
        type: string
        description: 'The filter expression. The following column identifiers are recognized (this differs
          from the identifiers recognized by the select parameter). keyword type: string url type: string'
        optional: true
      select:
        type: string
        description: A comma-separated list of columns to return. See response schema for valid column
          identifiers.
      project_id:
        type: integer
        description: Project ID
      date_from:
        type: string
        description: The start date of the historical period in YYYY-MM-DD format.
      country:
        type: string
        description: A two-letter country code (ISO 3166-1 alpha-2).
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - csv
        - xml
        - php
    output:
      description: OK
      type: object
    read_only: true
  gsc_ctr_by_position:
    title: CTR by position
    description: CTR by position Requests to this endpoint are free and do not consume any API units.
      Calls GET /v3/gsc/ctr-by-position on Ahrefs's API.
    category: gsc
    request:
      method: GET
      path: /v3/gsc/ctr-by-position
      query:
        project_id: ${project_id}
        portfolio_id: ${portfolio_id}
        date_from: ${date_from}
        date_to: ${date_to}
        country: ${country}
        device: ${device}
        output: ${output}
    input:
      project_id:
        type: integer
        description: Project ID. Required when portfolioid is not provided.
        optional: true
      portfolio_id:
        type: integer
        description: Portfolio ID. When provided, aggregates data across all GSC-connected projects in
          the portfolio.
        optional: true
      date_from:
        type: string
        description: The start date of the historical period in YYYY-MM-DD format.
      date_to:
        type: string
        description: The end date of the historical period in YYYY-MM-DD format.
        optional: true
      country:
        type: string
        description: A two-letter country code (ISO 3166-1 alpha-2).
        optional: true
        enum:
        - ad
        - ae
        - af
        - ag
        - ai
        - al
        - am
        - ao
        - ar
        - as
        - at
        - au
        - aw
        - az
        - ba
        - bb
        - bd
        - be
        - bf
        - bg
      device:
        type: string
        description: Filter by device type (DESKTOP, MOBILE, TABLET). If not specified, all devices are
          included.
        optional: true
        enum:
        - desktop
        - mobile
        - tablet
      output:
        type: string
        description: The output format.
        optional: true
        enum:
        - json
        - csv
        - xml
        - php
    output:
      description: OK
      type: object
    read_only: true
