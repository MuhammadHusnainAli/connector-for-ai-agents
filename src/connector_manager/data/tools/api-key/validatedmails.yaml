# ValidatedMails tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.validatedmails.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: validatedmails
display_name: ValidatedMails
docs_url: https://api.validatedmails.com/openapi.json
generated: true
generated_from: https://api.validatedmails.com/openapi.json
tools:
  root_get:
    title: Root
    description: Root Calls GET / on ValidatedMails's API. Generated from the provider's published OpenAPI
      specification.
    category: api
    request:
      method: GET
      path: /
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  keys_list_api_keys_get:
    title: Api Keys List
    description: Api Keys List Calls GET /api-keys on ValidatedMails's API. Generated from the provider's
      published OpenAPI specification.
    category: api_keys
    request:
      method: GET
      path: /api-keys
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  keys_create_api_keys_post:
    title: Api Keys Create
    description: Api Keys Create Calls POST /api-keys on ValidatedMails's API.
    category: api_keys
    request:
      method: POST
      path: /api-keys
      body:
        name: ${name}
    input:
      name:
        type: string
        description: The name field of the request body.
    output:
      description: Successful Response
      type: object
  credits_get_credits_get:
    title: Credits Get
    description: Credits Get Calls GET /credits on ValidatedMails's API. Generated from the provider's
      published OpenAPI specification.
    category: credits
    request:
      method: GET
      path: /credits
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  health_health_get:
    title: Health
    description: Health Calls GET /health on ValidatedMails's API. Generated from the provider's published
      OpenAPI specification.
    category: health
    request:
      method: GET
      path: /health
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  validate_validate_get:
    title: Validate
    description: Validate Calls GET /validate on ValidatedMails's API. Generated from the provider's published
      OpenAPI specification.
    category: validate
    request:
      method: GET
      path: /validate
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  admin_health_admin_health_get:
    title: Admin Health
    description: Admin Health Calls GET /admin/health on ValidatedMails's API.
    category: admin
    request:
      method: GET
      path: /admin/health
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  admin_users_list_admin_users_get:
    title: Admin Users List
    description: Admin Users List Calls GET /admin/users on ValidatedMails's API.
    category: admin
    request:
      method: GET
      path: /admin/users
      query:
        limit: ${limit}
        cursor: ${cursor}
        q: ${q}
    input:
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      q:
        type: string
        description: The q parameter.
        optional: true
        default: ''
    output:
      description: Successful Response
      type: object
    read_only: true
  keys_me_api_keys_me_get:
    title: Api Keys Me
    description: Api Keys Me Calls GET /api-keys/me on ValidatedMails's API. Generated from the provider's
      published OpenAPI specification.
    category: api_keys
    request:
      method: GET
      path: /api-keys/me
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  auth_complete_auth_complete_post:
    title: Auth Complete
    description: Auth Complete Calls POST /auth/complete on ValidatedMails's API.
    category: auth
    request:
      method: POST
      path: /auth/complete
      body:
        token: ${token}
    input:
      token:
        type: string
        description: The token field of the request body.
    output:
      description: Successful Response
      type: object
  auth_me_auth_me_get:
    title: Auth Me
    description: Auth Me Calls GET /auth/me on ValidatedMails's API. Generated from the provider's published
      OpenAPI specification.
    category: auth
    request:
      method: GET
      path: /auth/me
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  auth_start_auth_start_post:
    title: Auth Start
    description: Auth Start Calls POST /auth/start on ValidatedMails's API. Generated from the provider's
      published OpenAPI specification.
    category: auth
    request:
      method: POST
      path: /auth/start
      body:
        email: ${email}
        cap_token: ${cap_token}
    input:
      email:
        type: string
        description: The email field of the request body.
      cap_token:
        type: string
        description: The cap_token field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
