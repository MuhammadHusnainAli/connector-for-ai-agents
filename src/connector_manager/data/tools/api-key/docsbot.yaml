# DocsBot tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.docsbot.ai/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: docsbot
display_name: DocsBot
docs_url: https://docsbot.ai/legal/terms-of-service/
generated: true
generated_from: https://api.docsbot.ai/openapi.json
tools:
  get_get:
    title: Get
    description: Get Calls GET / on DocsBot's API. Generated from the provider's published OpenAPI specification.
    category: api
    request:
      method: GET
      path: /
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  list_libraries_that_can_be_searched_libraries_get:
    title: List Libraries That Can Be Searched
    description: List Libraries That Can Be Searched Calls GET /libraries on DocsBot's API.
    category: libraries
    request:
      method: GET
      path: /libraries
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  create_bots_ask:
    title: Question And Answer (Legacy)
    description: Question And Answer (Legacy) Calls POST /teams/{team_id}/bots/{bot_id}/ask on DocsBot's
      API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/ask
      body:
        question: ${question}
        history: ${history}
        format: ${format}
        full_source: ${full_source}
        metadata: ${metadata}
        testing: ${testing}
        autocut: ${autocut}
        context_items: ${context_items}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      question:
        type: string
        description: The question field of the request body.
      history:
        type: array
        description: The history field of the request body.
        optional: true
        items:
          type: array
      format:
        type: string
        description: The format field of the request body.
        optional: true
      full_source:
        type: string
        description: The full_source field of the request body.
        optional: true
      metadata:
        type: string
        description: The metadata field of the request body.
        optional: true
      testing:
        type: string
        description: The testing field of the request body.
        optional: true
      autocut:
        type: string
        description: The autocut field of the request body.
        optional: true
      context_items:
        type: integer
        description: The context_items field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  create_bots_chat:
    title: Chat Or Question And Answer
    description: Chat Or Question And Answer Calls POST /teams/{team_id}/bots/{bot_id}/chat on DocsBot's
      API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/chat
      body:
        question: ${question}
        history: ${history}
        format: ${format}
        full_source: ${full_source}
        metadata: ${metadata}
        testing: ${testing}
        autocut: ${autocut}
        context_items: ${context_items}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      question:
        type: string
        description: The question field of the request body.
      history:
        type: array
        description: The history field of the request body.
        optional: true
        items:
          type: array
      format:
        type: string
        description: The format field of the request body.
        optional: true
      full_source:
        type: string
        description: The full_source field of the request body.
        optional: true
      metadata:
        type: string
        description: The metadata field of the request body.
        optional: true
      testing:
        type: string
        description: The testing field of the request body.
        optional: true
      autocut:
        type: string
        description: The autocut field of the request body.
        optional: true
      context_items:
        type: integer
        description: The context_items field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  create_bots_chat_agent:
    title: Chat Or Question And Answer Using Retreival Agent
    description: Chat Or Question And Answer Using Retreival Agent Calls POST /teams/{team_id}/bots/{bot_id}/chat-agent
      on DocsBot's API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/chat-agent
      body:
        question: ${question}
        history: ${history}
        format: ${format}
        full_source: ${full_source}
        metadata: ${metadata}
        testing: ${testing}
        autocut: ${autocut}
        context_items: ${context_items}
        audio: ${audio}
        conversationId: ${conversation_id}
        channel: ${channel}
        stream: ${stream}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      question:
        type: string
        description: The question field of the request body.
        optional: true
      history:
        type: string
        description: Ignore this field, not used in agent conversations
        optional: true
      format:
        type: string
        description: The format field of the request body.
        optional: true
      full_source:
        type: string
        description: The full_source field of the request body.
        optional: true
      metadata:
        type: string
        description: The metadata field of the request body.
        optional: true
      testing:
        type: string
        description: The testing field of the request body.
        optional: true
      autocut:
        type: string
        description: The autocut field of the request body.
        optional: true
      context_items:
        type: integer
        description: The context_items field of the request body.
        optional: true
      audio:
        type: string
        description: Audio user message as a data URL string or an object with base64 data and format.
        optional: true
      conversation_id:
        type: string
        description: Unique identifier for the conversation, pass a new UUID to start a new conversation
      channel:
        type: string
        description: Internal conversation channel persisted on the conversation object.
        optional: true
      stream:
        type: boolean
        description: Whether to stream the response with SSE or return it all at once
        optional: true
    output:
      description: Successful Response
      type: object
  create_bots_fetch:
    title: Fetch Document
    description: Fetch Document Calls POST /teams/{team_id}/bots/{bot_id}/fetch on DocsBot's API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/fetch
      body:
        fileId: ${file_id}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      file_id:
        type: string
        description: Returns the reconstructed document for this file ID.
    output:
      description: Successful Response
      type: object
  create_bots_research:
    title: Start Deep Research
    description: Start Deep Research Calls POST /teams/{team_id}/bots/{bot_id}/research on DocsBot's API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/research
      body:
        question: ${question}
        model: ${model}
        web_search: ${web_search}
        code_interpreter: ${code_interpreter}
        docs_search: ${docs_search}
        question_history: ${question_history}
        metadata: ${metadata}
        tags: ${tags}
        include_untagged: ${include_untagged}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      question:
        type: string
        description: The question field of the request body.
      model:
        type: string
        description: Deep research model tier. gpt-5.6-luna is the default faster, cheaper option; gpt-5.4-mini
          remains available for legacy requests; gpt-5.5 / gpt-5.6-terra are balanced; gpt-5.6-sol is
          most capable. Legacy values o3/o4-mini are accepted as aliases.
        optional: true
        enum:
        - gpt-5.4-mini
        - gpt-5.5
        - gpt-5.6-sol
        - gpt-5.6-terra
        - gpt-5.6-luna
        - o3
        - o4-mini
      web_search:
        type: boolean
        description: Enable web search tool for deep research
        optional: true
      code_interpreter:
        type: boolean
        description: Enable code interpreter tool
        optional: true
      docs_search:
        type: boolean
        description: Enable documentation search tool (default true)
        optional: true
      question_history:
        type: boolean
        description: Enable question history tool for firestore search (default false)
        optional: true
      metadata:
        type: string
        description: Optional user metadata. Keys 'name' and 'email' are recognized.
        optional: true
      tags:
        type: string
        description: The tags field of the request body.
        optional: true
      include_untagged:
        type: boolean
        description: The include_untagged field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  create_bots_search:
    title: Semantic Search
    description: Semantic Search Calls POST /teams/{team_id}/bots/{bot_id}/search on DocsBot's API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/search
      body:
        query: ${query}
        sourceId: ${source_id}
        fileId: ${file_id}
        use_glossary: ${use_glossary}
        top_k: ${top_k}
        autocut: ${autocut}
        alpha: ${alpha}
        tags: ${tags}
        include_untagged: ${include_untagged}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      query:
        type: string
        description: The query field of the request body.
      source_id:
        type: string
        description: The sourceId field of the request body.
        optional: true
      file_id:
        type: string
        description: The fileId field of the request body.
        optional: true
      use_glossary:
        type: boolean
        description: The use_glossary field of the request body.
        optional: true
      top_k:
        type: string
        description: The top_k field of the request body.
        optional: true
      autocut:
        type: string
        description: The autocut field of the request body.
        optional: true
      alpha:
        type: string
        description: Hybrid search results can favor the keyword component or the semantic component.
          To change the relative weights of the keyword and vector components, set the alpha value in
          your query. An alpha of 1 is a pure semantic vector search. An alpha of 0 is a pure keyword
          search.
        optional: true
      tags:
        type: string
        description: The tags field of the request body.
        optional: true
      include_untagged:
        type: boolean
        description: The include_untagged field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  create_bots_voice:
    title: Create Widget Voice Webrtc Session
    description: Create Widget Voice Webrtc Session Exchange a browser SDP offer for a DocsBot-controlled
      Realtime answer. Calls POST /teams/{team_id}/bots/{bot_id}/voice on DocsBot's API.
    category: teams
    request:
      method: POST
      path: /teams/${team_id}/bots/${bot_id}/voice
      query:
        conversation_id: ${conversation_id}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      conversation_id:
        type: string
        description: The conversation_id parameter.
        optional: true
    output:
      description: Successful Response
      type: object
  update_bots_conversations:
    title: Update Metadata Of Conversation
    description: Update Metadata Of Conversation Update metadata of Conversation. Calls PUT /teams/{team_id}/bots/{bot_id}/conversations/{conv_id}
      on DocsBot's API.
    category: teams
    request:
      method: PUT
      path: /teams/${team_id}/bots/${bot_id}/conversations/${conv_id}
      body:
        metadata: ${metadata}
        fullChange: ${full_change}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      conv_id:
        type: string
        description: The conv_id parameter.
      metadata:
        type: object
        description: metadata of conversation
      full_change:
        type: string
        description: Update full metadata.
        optional: true
    output:
      description: Successful Response
      type: object
  update_bots_rate:
    title: Rate An Answer
    description: Rate An Answer Calls PUT /teams/{team_id}/bots/{bot_id}/rate/{answer_id} on DocsBot's
      API.
    category: teams
    request:
      method: PUT
      path: /teams/${team_id}/bots/${bot_id}/rate/${answer_id}
      body:
        rating: ${rating}
    input:
      team_id:
        type: string
        description: The team_id parameter.
      bot_id:
        type: string
        description: The bot_id parameter.
      answer_id:
        type: string
        description: The answer_id parameter.
      rating:
        type: integer
        description: The rating field of the request body.
    output:
      description: Successful Response
      type: object
