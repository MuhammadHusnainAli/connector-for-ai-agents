# TestLocally tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://testlocal.ly/swagger.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: testlocally
display_name: TestLocally
docs_url: https://testlocal.ly/swagger.json
generated: true
generated_from: https://testlocal.ly/swagger.json
tools:
  v0_me:
    title: V0 me
    description: Debug information about the currently used token Calls GET /api/v0/me on the provider's
      API.
    category: api
    request:
      method: GET
      path: /api/v0/me
    input: {}
    output:
      description: Your token information
      type: object
    read_only: true
  v0_servers:
    title: V0 servers
    description: List of servers. Use any server’s name property when requesting a new test Calls GET
      /api/v0/servers on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v0/servers
    input: {}
    output:
      description: List of servers
      type: object
    read_only: true
  v0_recent_tests:
    title: V0 recent tests
    description: Lists recent tests matching criteria Calls GET /api/v0/tests on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v0/tests
      query:
        start: ${start}
        end: ${end}
        owners[]: ${owners}
        locations[]: ${locations}
        target: ${target}
        scheduled: ${scheduled}
        browser: ${browser}
        viewport: ${viewport}
        page: ${page}
        limit: ${limit}
    input:
      start:
        type: string
        description: start time specification
        optional: true
      end:
        type: string
        description: end time specification
        optional: true
      owners:
        type: array
        description: a list of user ids
        optional: true
        items:
          type: string
      locations:
        type: array
        description: list of locations
        optional: true
        items:
          type: string
      target:
        type: string
        description: The target parameter.
        optional: true
      scheduled:
        type: string
        description: values 'yes', 'no', or a specific schedule id
        optional: true
      browser:
        type: string
        description: The browser parameter.
        optional: true
      viewport:
        type: string
        description: The viewport parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 20
    output:
      description: List of tests matching criteria
      type: object
    read_only: true
  v0_request_test:
    title: V0 request test
    description: Creates new test Calls POST /api/v0/tests on the provider's API.
    category: api
    request:
      method: POST
      path: /api/v0/tests
      body:
        target: ${target}
        browser: ${browser}
        viewport: ${viewport}
        servers: ${servers}
        headers: ${headers}
        skipCompression: ${skip_compression}
        ignoreSslErrors: ${ignore_ssl_errors}
        clicks: ${clicks}
    input:
      target:
        type: string
        description: The target field of the request body.
      browser:
        type: string
        description: The browser field of the request body.
        optional: true
        enum:
        - chromium
        - chrome
        - firefox
        - msedge
        - webkit
      viewport:
        type: string
        description: The viewport field of the request body.
        optional: true
        enum:
        - desktop
        - tablet
        - phone
      servers:
        type: array
        description: The servers field of the request body.
        items:
          type: string
      headers:
        type: object
        description: The headers field of the request body.
        optional: true
      skip_compression:
        type: boolean
        description: The skipCompression field of the request body.
        optional: true
      ignore_ssl_errors:
        type: boolean
        description: The ignoreSslErrors field of the request body.
        optional: true
      clicks:
        type: array
        description: The clicks field of the request body.
        optional: true
        items:
          type: string
    output:
      description: Test successfully created
      type: object
  v0_test_results:
    title: V0 test results
    description: Get a single test details Calls GET /api/v0/tests/{id} on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v0/tests/${id}
    input:
      id:
        type: string
        description: The id parameter.
    output:
      description: Success
      type: object
    read_only: true
