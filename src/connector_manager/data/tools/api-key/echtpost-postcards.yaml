# Echtpost Postcards tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.echtpost.de/v2/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: echtpost-postcards
display_name: Echtpost Postcards
docs_url: https://api.echtpost.de/v2/openapi.json
generated: true
generated_from: https://api.echtpost.de/v2/openapi.json
tools:
  list_cards:
    title: List cards
    description: List cards Calls GET /v2/cards on Echtpost Postcards's API. Generated from the provider's
      published OpenAPI specification.
    category: cards
    request:
      method: GET
      path: /v2/cards
      query:
        page: ${page}
        per_page: ${per_page}
        status: ${status}
        deliver_at: ${deliver_at}
        motive_id: ${motive_id}
        sort: ${sort}
        direction: ${direction}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
      per_page:
        type: integer
        description: The per_page parameter.
        optional: true
      status:
        type: string
        description: One or more of pending, scheduled, sent, canceled. Comma-separated.
        optional: true
      deliver_at:
        type: string
        description: Filter cards by delivery date (ISO 8601, e.g. 2026-05-12).
        optional: true
      motive_id:
        type: integer
        description: The motive_id parameter.
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - created_at
        - deliver_at
      direction:
        type: string
        description: The direction parameter.
        optional: true
        enum:
        - asc
        - desc
    output:
      description: OK
      type: object
    read_only: true
  create_card:
    title: Create a card (custom)
    description: Create a card (custom) Calls POST /v2/cards on Echtpost Postcards's API.
    category: cards
    request:
      method: POST
      path: /v2/cards
      body:
        motive_id: ${motive_id}
        content: ${content}
        content_ps: ${content_ps}
        content_vertical: ${content_vertical}
        deliver_at: ${deliver_at}
        qr_code_url: ${qr_code_url}
        deduplicate_recipients: ${deduplicate_recipients}
        recipient_ids: ${recipient_ids}
        group_ids: ${group_ids}
        recipients: ${recipients}
        font: ${font}
        notification: ${notification}
    input:
      motive_id:
        type: integer
        description: The motive_id field of the request body.
      content:
        type: string
        description: Card text. Use {anrede} as a per-recipient salutation placeholder (casing ignored).
      content_ps:
        type: string
        description: Optional P.S., printed as a single footer line.
        optional: true
      content_vertical:
        type: string
        description: The content_vertical field of the request body.
        optional: true
      deliver_at:
        type: string
        description: 'Delivery date. Accepts: ISO 8601 date (YYYY-MM-DD), today, asap (alias for today),
          tomorrow, or N-days-from-now (e.g. 5-days-from-now). Must resolve to a weekday that is not a
          printer holiday. Omitting this field schedules the card for the next available business day.'
        optional: true
      qr_code_url:
        type: string
        description: The qr_code_url field of the request body.
        optional: true
      deduplicate_recipients:
        type: boolean
        description: The deduplicate_recipients field of the request body.
        optional: true
      recipient_ids:
        type: array
        description: The recipient_ids field of the request body.
        optional: true
        items:
          type: integer
      group_ids:
        type: array
        description: The group_ids field of the request body.
        optional: true
        items:
          type: integer
      recipients:
        type: array
        description: The recipients field of the request body.
        optional: true
        items:
          type: object
      font:
        type: object
        description: The font field of the request body.
        optional: true
      notification:
        type: object
        description: The notification field of the request body.
        optional: true
    output:
      description: Created
      type: object
  list_contacts:
    title: List contacts
    description: List contacts Calls GET /v2/contacts on Echtpost Postcards's API.
    category: contacts
    request:
      method: GET
      path: /v2/contacts
      query:
        page: ${page}
        per_page: ${per_page}
        country_code: ${country_code}
        group_id: ${group_id}
        external_id: ${external_id}
        search: ${search}
        sort: ${sort}
        direction: ${direction}
        include: ${include}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
      per_page:
        type: integer
        description: The per_page parameter.
        optional: true
      country_code:
        type: string
        description: The country_code parameter.
        optional: true
      group_id:
        type: integer
        description: The group_id parameter.
        optional: true
      external_id:
        type: string
        description: The external_id parameter.
        optional: true
      search:
        type: string
        description: The search parameter.
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - last_name
        - created_at
        - updated_at
      direction:
        type: string
        description: The direction parameter.
        optional: true
        enum:
        - asc
        - desc
      include:
        type: string
        description: Comma-separated. Allowed groups, cards.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_credits:
    title: Account balance and pricing
    description: Account balance and pricing Calls GET /v2/credits on Echtpost Postcards's API.
    category: credits
    request:
      method: GET
      path: /v2/credits
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  list_groups:
    title: List groups
    description: List groups Calls GET /v2/groups on Echtpost Postcards's API.
    category: groups
    request:
      method: GET
      path: /v2/groups
      query:
        page: ${page}
        per_page: ${per_page}
        sort: ${sort}
        direction: ${direction}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
      per_page:
        type: integer
        description: The per_page parameter.
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - name
        - created_at
        - updated_at
      direction:
        type: string
        description: The direction parameter.
        optional: true
        enum:
        - asc
        - desc
    output:
      description: OK
      type: object
    read_only: true
  create_group:
    title: Create group
    description: Create group Calls POST /v2/groups on Echtpost Postcards's API.
    category: groups
    request:
      method: POST
      path: /v2/groups
      body:
        name: ${name}
        external_id: ${external_id}
    input:
      name:
        type: string
        description: The name field of the request body.
      external_id:
        type: string
        description: The external_id field of the request body.
        optional: true
    output:
      description: Created
      type: object
  get_me:
    title: Current account and API key metadata
    description: Current account and API key metadata Calls GET /v2/me on Echtpost Postcards's API.
    category: me
    request:
      method: GET
      path: /v2/me
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  list_motives:
    title: List available motives
    description: List available motives Calls GET /v2/motives on Echtpost Postcards's API.
    category: motives
    request:
      method: GET
      path: /v2/motives
      query:
        page: ${page}
        per_page: ${per_page}
        search: ${search}
        sort: ${sort}
        direction: ${direction}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
      per_page:
        type: integer
        description: The per_page parameter.
        optional: true
      search:
        type: string
        description: Search across name, description, content, and searchterms.
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - name
        - created_at
      direction:
        type: string
        description: The direction parameter.
        optional: true
        enum:
        - asc
        - desc
    output:
      description: OK
      type: object
    read_only: true
  list_templates:
    title: List templates
    description: List templates Calls GET /v2/templates on Echtpost Postcards's API.
    category: templates
    request:
      method: GET
      path: /v2/templates
      query:
        page: ${page}
        per_page: ${per_page}
        source: ${source}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
      per_page:
        type: integer
        description: The per_page parameter.
        optional: true
      source:
        type: string
        description: Defaults to regular.
        optional: true
        enum:
        - regular
        - ai
        - letter
    output:
      description: OK
      type: object
    read_only: true
  create_card_from_template:
    title: Create a card from a template
    description: Create a card from a template Calls POST /v2/cards/from_template on Echtpost Postcards's
      API.
    category: cards
    request:
      method: POST
      path: /v2/cards/from_template
      body:
        template_id: ${template_id}
        deliver_at: ${deliver_at}
        recipient_ids: ${recipient_ids}
        group_ids: ${group_ids}
        recipients: ${recipients}
        simulate: ${simulate}
    input:
      template_id:
        type: integer
        description: The template_id field of the request body.
      deliver_at:
        type: string
        description: 'Delivery date. Accepts: ISO 8601 date (YYYY-MM-DD), today, asap (alias for today),
          tomorrow, or N-days-from-now (e.g. 5-days-from-now). Must resolve to a weekday that is not a
          printer holiday. Omitting this field schedules the card for the next available business day.'
        optional: true
      recipient_ids:
        type: array
        description: The recipient_ids field of the request body.
        optional: true
        items:
          type: integer
      group_ids:
        type: array
        description: The group_ids field of the request body.
        optional: true
        items:
          type: integer
      recipients:
        type: array
        description: The recipients field of the request body.
        optional: true
        items:
          type: object
      simulate:
        type: string
        description: Sandbox only. Instead of creating the card, force a simulated error so API clients
          can test their error handling. insufficientfunds returns HTTP 402 with the normal insufficient-funds
          body. Ignored with a live key.
        optional: true
        enum:
        - insufficient_funds
    output:
      description: Created
      type: object
  preview_card_fit:
    title: Check whether card content fits at a given font
    description: Check whether card content fits at a given font Calls POST /v2/cards/preview_fit on Echtpost
      Postcards's API.
    category: cards
    request:
      method: POST
      path: /v2/cards/preview_fit
      body:
        content: ${content}
        sample_greeting: ${sample_greeting}
        font: ${font}
    input:
      content:
        type: string
        description: Plain text in the same shape as POST /v2/cards content; use JSON line breaks (\n)
          between paragraphs.
      sample_greeting:
        type: string
        description: Optional greeting prefix to prepend for fit-check.
        optional: true
      font:
        type: object
        description: Optional. Omitted family defaults to architectsdaughter; omitted size defaults to
          the middle allowed value for the family. Only these fields affect the fit check (no color).
        optional: true
    output:
      description: OK
      type: object
  cancel_card:
    title: Cancel card (async)
    description: Cancel card (async) Calls DELETE /v2/cards/{id} on Echtpost Postcards's API.
    category: cards
    request:
      method: DELETE
      path: /v2/cards/${id}
    input:
      id:
        type: string
        description: The id parameter.
    output:
      description: Accepted — cancellation queued
      type: object
    destructive: true
