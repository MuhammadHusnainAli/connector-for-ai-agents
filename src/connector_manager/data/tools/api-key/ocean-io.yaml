# Ocean.io tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.ocean.io/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: ocean-io
display_name: Ocean.io
docs_url: https://api.ocean.io/openapi.json
generated: true
generated_from: https://api.ocean.io/openapi.json
tools:
  get_data_fields_public:
    title: Get data fields
    description: Get data fields Provides a list of all industries and industry categories, technologies
      and regions searchable by Ocean.io. Calling this endpoint does not consume any credits. Calls GET
      /v2/data-fields on the provider's API.
    category: data_fields
    request:
      method: GET
      path: /v2/data-fields
      query:
        apiToken: ${api_token}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  create_segmentation:
    title: Create segmentation
    description: Create segmentation Create a new segmentation from a list of company domains. Runs asynchronously
      — poll Get Segmentation until status is SUCCESSFUL to read the resulting segments. Calls POST /v2/segmentation
      on the provider's API.
    category: segmentation
    request:
      method: POST
      path: /v2/segmentation
      query:
        apiToken: ${api_token}
      body:
        domains: ${domains}
        leadScoringFeatures: ${lead_scoring_features}
        numberOfSegmentsMode: ${number_of_segments_mode}
        positiveDomains: ${positive_domains}
        negativeDomains: ${negative_domains}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      domains:
        type: array
        description: Company domains to segment. Provide a representative set of the companies you want
          to group, typically your customers or best-fit accounts.
        items:
          type: string
      lead_scoring_features:
        type: array
        description: Company attributes to consider when grouping and scoring companies (for example industries,
          technologies, companysize). Omit to use the default feature set.
        optional: true
        items:
          type: string
      number_of_segments_mode:
        type: integer
        description: Bias toward fewer or more segments. Omit to let the service choose the number that
          best fits the data.
        optional: true
      positive_domains:
        type: array
        description: Companies that are a good fit. The segmentation leans toward similar companies.
        optional: true
        items:
          type: string
      negative_domains:
        type: array
        description: Companies that are a poor fit. The segmentation leans away from similar companies.
        optional: true
        items:
          type: string
    output:
      description: Successful Response
      type: object
  auto_complete_company:
    title: Autocomplete companies
    description: Autocomplete companies Auto complete the name search Calls POST /v2/autocomplete/companies
      on the provider's API.
    category: autocomplete
    request:
      method: POST
      path: /v2/autocomplete/companies
      query:
        apiToken: ${api_token}
      body:
        name: ${name}
        countryFilters: ${country_filters}
        excludeDomains: ${exclude_domains}
        forLookalikeSearch: ${for_lookalike_search}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      name:
        type: string
        description: Beginning of the name or the domain of a company
      country_filters:
        type: array
        description: List of countries to filter for the autocompletion
        optional: true
        items:
          type: string
      exclude_domains:
        type: array
        description: List of domains to exclude from the results
        optional: true
        items:
          type: string
      for_lookalike_search:
        type: boolean
        description: 'When false, returns all matching companies without domain validation or status filters.
          WARNING: Do not set this parameter to false if the autocomplete results will be used as input
          for a lookalike search.'
        optional: true
    output:
      description: Successful Response
      type: object
  auto_complete_job_titles:
    title: Autocomplete job titles
    description: Autocomplete job titles Auto complete the job title search. Calls POST /v2/autocomplete/job-titles
      on the provider's API.
    category: autocomplete
    request:
      method: POST
      path: /v2/autocomplete/job-titles
      query:
        apiToken: ${api_token}
      body:
        query: ${query}
        limit: ${limit}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      query:
        type: string
        description: Beginning of the job title
      limit:
        type: integer
        description: Number of job titles to return. Default is 5 if not specified, maximum is 15.
        optional: true
    output:
      description: Successful Response
      type: object
  auto_complete_keywords:
    title: Autocomplete keywords
    description: Autocomplete keywords Auto complete the keyword search. Calls POST /v2/autocomplete/keywords
      on the provider's API.
    category: autocomplete
    request:
      method: POST
      path: /v2/autocomplete/keywords
      query:
        apiToken: ${api_token}
      body:
        query: ${query}
        limit: ${limit}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      query:
        type: string
        description: Beginning of the keyword
      limit:
        type: integer
        description: Number of keywords to return. Default is 5 if not specified, maximum is 15.
        optional: true
    output:
      description: Successful Response
      type: object
  auto_complete_locations:
    title: Autocomplete locations
    description: Autocomplete locations Auto complete the location search Calls POST /v2/autocomplete/locations
      on the provider's API.
    category: autocomplete
    request:
      method: POST
      path: /v2/autocomplete/locations
      query:
        apiToken: ${api_token}
      body:
        query: ${query}
        limit: ${limit}
        locationType: ${location_type}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      query:
        type: string
        description: Beginning of the location
      limit:
        type: integer
        description: Number of locations to return. Default is 5 if not specified, maximum is 15.
        optional: true
      location_type:
        type: string
        description: The locationType field of the request body.
        optional: true
        enum:
        - companies
        - people
    output:
      description: Successful Response
      type: object
  auto_complete_skills:
    title: Autocomplete skills
    description: Autocomplete skills Auto complete the skill search. Calls POST /v2/autocomplete/skills
      on the provider's API.
    category: autocomplete
    request:
      method: POST
      path: /v2/autocomplete/skills
      query:
        apiToken: ${api_token}
      body:
        query: ${query}
        limit: ${limit}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      query:
        type: string
        description: Beginning of the skill name
      limit:
        type: integer
        description: Number of skills to return. Default is 5 if not specified, maximum is 15.
        optional: true
    output:
      description: Successful Response
      type: object
  get_credit_balance:
    title: Get credit balance
    description: Get credit balance Get credit balance Calls GET /v2/credits/balance on the provider's
      API.
    category: credits
    request:
      method: GET
      path: /v2/credits/balance
      query:
        apiToken: ${api_token}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  enrich_companies:
    title: Enrich companies
    description: 'Enrich companies Match companies with our database and enrich it with additional information.
      Webhook result: Documentation Calls POST /v2/enrich/companies on the provider''s API.'
    category: enrich
    request:
      method: POST
      path: /v2/enrich/companies
      query:
        apiToken: ${api_token}
      body:
        companyDataMapping: ${company_data_mapping}
        webhookUrl: ${webhook_url}
        fields: ${fields}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      company_data_mapping:
        type: object
        description: A dictionary where each key is unique user-defined ID for a company, and each value
          is the data for that company to be enriched. This ID will be included in the webhook response
          together with the enriched company data, allowing the user to match the response to the original
          request.
      webhook_url:
        type: string
        description: Url of the webhook the enriched companies should be sent to, when completed.
      fields:
        type: array
        description: Fields to return in the Company objects. Only requesting the fields that are needed
          will use less bandwidth and result in faster responses. If not specified, all fields are returned
          that can be seen in the example.
        optional: true
        items:
          type: string
    output:
      description: Successful Response
      type: object
  search_companies_v3:
    title: Lookalike companies search
    description: Lookalike companies search Search companies using filters. Looking for the legacy endpoint?
      Access it here Calls POST /v3/search/companies on the provider's API.
    category: search
    request:
      method: POST
      path: /v3/search/companies
      query:
        apiToken: ${api_token}
      body:
        size: ${size}
        searchAfter: ${search_after}
        companiesFilters: ${companies_filters}
        peopleFilters: ${people_filters}
        fields: ${fields}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      size:
        type: integer
        description: Number of companies to return. The maximum value is 10,000. To get more than 10,000
          results, use searchAfter.
        optional: true
      search_after:
        type: string
        description: Use searchAfter returned by the previous request to get the next page
        optional: true
      companies_filters:
        type: object
        description: The companiesFilters field of the request body.
        optional: true
      people_filters:
        type: object
        description: The peopleFilters field of the request body.
        optional: true
      fields:
        type: array
        description: Fields to return in the Company objects. Only requesting the fields that are needed
          will use less bandwidth and result in faster responses. If not specified, all fields are returned
          that can be seen in the example.
        optional: true
        items:
          type: string
    output:
      description: Successful Response
      type: object
  search_people_v3:
    title: Lookalike people search
    description: 'Lookalike people search Search people using filters and/or people Ids Looking for the
      legacy endpoint? Access it here Webhook result: Email results Phone results Calls POST /v3/search/people
      on the provider''s API.'
    category: search
    request:
      method: POST
      path: /v3/search/people
      query:
        apiToken: ${api_token}
      body:
        size: ${size}
        peopleFilters: ${people_filters}
        companiesFilters: ${companies_filters}
        peoplePerCompany: ${people_per_company}
        jobTitleThreshold: ${job_title_threshold}
        searchAfter: ${search_after}
        fields: ${fields}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      size:
        type: integer
        description: Number of people to return. The maximum value is 10,000.
        optional: true
      people_filters:
        type: object
        description: The peopleFilters field of the request body.
        optional: true
      companies_filters:
        type: object
        description: The companiesFilters field of the request body.
        optional: true
      people_per_company:
        type: integer
        description: Number of people to return per company. If you want to return all people from a company,
          don't set this value.
        optional: true
      job_title_threshold:
        type: number
        description: Threshold for job title similarity. The value must be between 0 and 1. Default is
          0.8
        optional: true
      search_after:
        type: string
        description: Use searchAfter returned by the previous request to get the next page.
        optional: true
      fields:
        type: array
        description: 'Fields to return in the Person objects. Only requesting the fields that are needed
          will use less bandwidth and result in faster responses. If not specified, the following default
          fields are returned: id, domain, name, firstName, lastName, country, state, location, linkedinUrl,
          seniorities, departments, photo, jobTitle, jobTitleEnglish, currentJobDescription, experiences,
          summary, skills, headline,'
        optional: true
        items:
          type: string
    output:
      description: Successful Response
      type: object
  preview_search_companies_v3:
    title: Preview - Lookalike companies
    description: Preview - Lookalike companies Preview search companies using filters. Only available
      for enterprise API customers. Calls POST /v3/search/companies/preview on the provider's API.
    category: search
    request:
      method: POST
      path: /v3/search/companies/preview
      query:
        apiToken: ${api_token}
      body:
        size: ${size}
        companiesFilters: ${companies_filters}
        peopleFilters: ${people_filters}
    input:
      api_token:
        type: string
        description: The apiToken parameter.
        optional: true
      size:
        type: integer
        description: Number of companies to return. The maximum value is 50 and the minimum is 5
        optional: true
      companies_filters:
        type: object
        description: The companiesFilters field of the request body.
        optional: true
      people_filters:
        type: object
        description: The peopleFilters field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
