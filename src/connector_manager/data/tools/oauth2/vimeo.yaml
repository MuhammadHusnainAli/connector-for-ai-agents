# Vimeo (OAuth) tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/vimeo.com/3.4/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: vimeo
display_name: Vimeo (OAuth)
docs_url: https://api.apis.guru/v2/specs/vimeo.com/3.4/openapi.json
generated: true
generated_from: https://api.apis.guru/v2/specs/vimeo.com/3.4/openapi.json
tools:
  get_endpoints:
    title: Get an API specification
    description: Get an API specification Calls GET / on the provider's API.
    category: api
    request:
      method: GET
      path: /
      query:
        openapi: ${openapi}
    input:
      openapi:
        type: boolean
        description: Return an OpenAPI specification.
        optional: true
    output:
      description: Standard request.
      type: object
    read_only: true
  get_categories:
    title: Get all categories
    description: Get all categories This method gets all existing categories. Calls GET /categories on
      the provider's API.
    category: categories
    request:
      method: GET
      path: /categories
      query:
        direction: ${direction}
        page: ${page}
        per_page: ${per_page}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - last_video_featured_time
        - name
    output:
      description: The categories were returned.
      type: object
    read_only: true
  get_channels:
    title: Get all channels
    description: Get all channels Calls GET /channels on the provider's API.
    category: channels
    request:
      method: GET
      path: /channels
      query:
        direction: ${direction}
        filter: ${filter}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      filter:
        type: string
        description: The attribute by which to filter the results.
        optional: true
        enum:
        - featured
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: 'The way to sort the results. Option descriptions: relevant - Relevant sorting is
          available only for search queries.'
        optional: true
        enum:
        - alphabetical
        - date
        - followers
        - relevant
        - videos
    output:
      description: The channels were returned.
      type: object
    read_only: true
  get_content_ratings:
    title: Get all content ratings
    description: Get all content ratings Calls GET /contentratings on the provider's API.
    category: contentratings
    request:
      method: GET
      path: /contentratings
    input: {}
    output:
      description: The content ratings were returned.
      type: object
    read_only: true
  get_cc_licenses:
    title: Get all Creative Commons licenses
    description: Get all Creative Commons licenses Calls GET /creativecommons on the provider's API.
    category: creativecommons
    request:
      method: GET
      path: /creativecommons
    input: {}
    output:
      description: The Creative Commons licenses were returned.
      type: object
    read_only: true
  get_groups:
    title: Get all groups
    description: Get all groups Calls GET /groups on the provider's API.
    category: groups
    request:
      method: GET
      path: /groups
      query:
        direction: ${direction}
        filter: ${filter}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      filter:
        type: string
        description: The attribute by which to filter the results.
        optional: true
        enum:
        - featured
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: 'The way to sort the results. Option descriptions: relevant - Relevant sorting is
          available only for search queries.'
        optional: true
        enum:
        - alphabetical
        - date
        - followers
        - relevant
        - videos
    output:
      description: The groups were returned.
      type: object
    read_only: true
  get_languages:
    title: Get all languages
    description: Get all languages Calls GET /languages on the provider's API.
    category: languages
    request:
      method: GET
      path: /languages
      query:
        filter: ${filter}
    input:
      filter:
        type: string
        description: 'The attribute by which to filter the results. Option descriptions: texttracks -
          Only return text track supported languages'
        optional: true
        enum:
        - texttracks
    output:
      description: The languages were returned.
      type: object
    read_only: true
  get_user_alt1:
    title: Get a user
    description: Get a user Calls GET /me on the provider's API.
    category: me
    request:
      method: GET
      path: /me
    input: {}
    output:
      description: The user was returned.
      type: object
    read_only: true
  search_users:
    title: Search for users
    description: Search for users Calls GET /users on the provider's API.
    category: users
    request:
      method: GET
      path: /users
      query:
        direction: ${direction}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - date
        - followers
        - relevant
        - videos
    output:
      description: The search results were returned.
      type: object
    read_only: true
  search_videos:
    title: Search for videos
    description: Search for videos Calls GET /videos on the provider's API.
    category: videos
    request:
      method: GET
      path: /videos
      query:
        direction: ${direction}
        filter: ${filter}
        links: ${links}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
        uris: ${uris}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      filter:
        type: string
        description: The attribute by which to filter the results. CC and related filters target videos
          with the corresponding Creative Commons licenses. For more information, see our Creative Commons
          page.
        optional: true
        enum:
        - CC
        - CC-BY
        - CC-BY-NC
        - CC-BY-NC-ND
        - CC-BY-NC-SA
        - CC-BY-ND
        - CC-BY-SA
        - CC0
        - categories
        - duration
        - in-progress
        - minimum_likes
        - trending
        - upload_date
      links:
        type: string
        description: A comma-separated list of video URLs to find.
        optional: true
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: Search query.
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - comments
        - date
        - duration
        - likes
        - plays
        - relevant
      uris:
        type: string
        description: The comma-separated list of videos to find.
        optional: true
    output:
      description: The search results were returned.
      type: object
    read_only: true
  get_albums_alt1:
    title: Get all the albums that belong to a user
    description: Get all the albums that belong to a user Calls GET /me/albums on the provider's API.
    category: me
    request:
      method: GET
      path: /me/albums
      query:
        direction: ${direction}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - date
        - duration
        - videos
    output:
      description: The albums were returned.
      type: object
    read_only: true
  get_appearances_alt1:
    title: Get all the videos in which a user appears
    description: Get all the videos in which a user appears Calls GET /me/appearances on the provider's
      API.
    category: me
    request:
      method: GET
      path: /me/appearances
      query:
        direction: ${direction}
        filter: ${filter}
        filter_embeddable: ${filter_embeddable}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      filter:
        type: string
        description: The attribute by which to filter the results.
        optional: true
        enum:
        - embeddable
      filter_embeddable:
        type: boolean
        description: Whether to filter the results by embeddable videos (true) or non-embeddable videos
          (false). Required only if filter is embeddable.
        optional: true
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - comments
        - date
        - duration
        - likes
        - plays
    output:
      description: The videos were returned.
      type: object
    read_only: true
  get_category_subscriptions_alt1:
    title: Get all the categories that a user follows
    description: Get all the categories that a user follows Calls GET /me/categories on the provider's
      API.
    category: me
    request:
      method: GET
      path: /me/categories
      query:
        direction: ${direction}
        page: ${page}
        per_page: ${per_page}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - date
        - name
    output:
      description: The categories were returned.
      type: object
    read_only: true
  get_channel_subscriptions_alt1:
    title: Get all the channels to which a user subscribes
    description: Get all the channels to which a user subscribes Calls GET /me/channels on the provider's
      API.
    category: me
    request:
      method: GET
      path: /me/channels
      query:
        direction: ${direction}
        filter: ${filter}
        page: ${page}
        per_page: ${per_page}
        query: ${query}
        sort: ${sort}
    input:
      direction:
        type: string
        description: The sort direction of the results.
        optional: true
        enum:
        - asc
        - desc
      filter:
        type: string
        description: The attribute by which to filter the results.
        optional: true
        enum:
        - moderated
      page:
        type: number
        description: The page number of the results to show.
        optional: true
      per_page:
        type: number
        description: The number of items to show on each page of results, up to a maximum of 100.
        optional: true
      query:
        type: string
        description: The search query to use to filter the results.
        optional: true
      sort:
        type: string
        description: The way to sort the results.
        optional: true
        enum:
        - alphabetical
        - date
        - followers
        - videos
    output:
      description: The channels were returned.
      type: object
    read_only: true
