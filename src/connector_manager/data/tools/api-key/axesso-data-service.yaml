# Axesso Data Service tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/axesso.de/1.0.0/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: axesso-data-service
display_name: Axesso Data Service
docs_url: http://api-doc.axesso.de/
generated: true
generated_from: https://api.apis.guru/v2/specs/axesso.de/1.0.0/openapi.json
tools:
  request_buy_recommendation:
    title: request buy recommendations to a given product
    description: request buy recommendations to a given product Calls GET /amz/amazon-lookup-buy-recommendations
      on the provider's API.
    category: amz
    request:
      method: GET
      path: /amz/amazon-lookup-buy-recommendations
      query:
        url: ${url}
    input:
      url:
        type: string
        description: The url of the requested product.
    output:
      description: successful operation
      type: object
    read_only: true
  request_product:
    title: lookup product information
    description: lookup product information Calls GET /amz/amazon-lookup-product on the provider's API.
    category: amz
    request:
      method: GET
      path: /amz/amazon-lookup-product
      query:
        url: ${url}
        size: ${size}
    input:
      url:
        type: string
        description: The url of the requested product.
      size:
        type: string
        description: Size parameter if available.
        optional: true
    output:
      description: successful operation
      type: object
    read_only: true
  keyword_search:
    title: fetch results auf a keyword search on Amazon
    description: fetch results auf a keyword search on Amazon Calls GET /amz/amazon-search-by-keyword
      on the provider's API.
    category: amz
    request:
      method: GET
      path: /amz/amazon-search-by-keyword
      query:
        keyword: ${keyword}
        domainCode: ${domain_code}
        sortBy: ${sort_by}
        numberOfProducts: ${number_of_products}
    input:
      keyword:
        type: string
        description: keyword to search
      domain_code:
        type: string
        description: domain for the search
      sort_by:
        type: string
        description: sort option
        optional: true
        default: relevanceblender
      number_of_products:
        type: integer
        description: number of the results (max 20)
        optional: true
        default: '20'
    output:
      description: successful operation
      type: object
    read_only: true
  sort_options:
    title: request available sort options to use in keyword search
    description: request available sort options to use in keyword search Calls GET /amz/sort-options on
      the provider's API.
    category: amz
    request:
      method: GET
      path: /amz/sort-options
    input: {}
    output:
      description: successful operation
      type: object
    read_only: true
