# Pro Ledger tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.pro-ledger.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: pro-ledger
display_name: Pro Ledger
docs_url: https://pro-ledger.com/terms-of-use
generated: true
generated_from: https://api.pro-ledger.com/openapi.json
tools:
  hello_world_get:
    title: Hello World
    description: Hello World Calls GET / on the provider's API.
    category: api
    request:
      method: GET
      path: /
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  check_api_key_api_v1_auth_check_api_key_get:
    title: Check Api Key
    description: Check Api Key This endpoint serves as a validation mechanism for API keys, allowing clients
      to verify the authenticity and validity of the provided API key. The primary purpose is to ensure
      secure access to protected resources and maintain the integrity of the authentication process. Calls
      GET /api/v1/auth/check_api_key on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/auth/check_api_key
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  create_record_create_new_record:
    title: Create New Record
    description: Create New Record Create a new record in the Pro Ledger platform. Calls POST /api/v1/record/create_new_record
      on the provider's API.
    category: api
    request:
      method: POST
      path: /api/v1/record/create_new_record
      query:
        transactionType: ${transaction_type}
      body:
        rec: ${rec}
        unit: ${unit}
        class: ${class}
        source: ${source}
        category: ${category}
        subcategory: ${subcategory}
        description: ${description}
        total: ${total}
        taxes: ${taxes}
        account: ${account}
        taxIncluded: ${tax_included}
    input:
      transaction_type:
        type: string
        description: The transactionType parameter.
        optional: true
        default: Income
      rec:
        type: string
        description: The rec field of the request body.
        enum:
        - Select
        - Done
        - No Receipt
        - Foreign $$
      unit:
        type: string
        description: The unit field of the request body.
        optional: true
      class:
        type: string
        description: The class field of the request body.
      source:
        type: string
        description: The source field of the request body.
      category:
        type: string
        description: The category field of the request body.
      subcategory:
        type: string
        description: The subcategory field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
        optional: true
      total:
        type: string
        description: The total field of the request body.
      taxes:
        type: array
        description: The taxes field of the request body.
        optional: true
        items:
          type: string
      account:
        type: string
        description: The account field of the request body.
      tax_included:
        type: string
        description: The taxIncluded field of the request body.
        enum:
        - Incl. Tax
        - Excl. Tax
    output:
      description: Successful Response
      type: object
  get_accounts_api_v1_record_get_accounts_get:
    title: Get Accounts
    description: Get Accounts Get accounts setup information Calls GET /api/v1/record/get_accounts on
      the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_accounts
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  get_categories_api_v1_record_get_categories_get:
    title: Get Categories
    description: Get Categories Get categories setup information Calls GET /api/v1/record/get_categories
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_categories
      query:
        transactionType: ${transaction_type}
    input:
      transaction_type:
        type: string
        description: The transactionType parameter.
        optional: true
        default: Income
    output:
      description: Successful Response
      type: object
    read_only: true
  get_classes_api_v1_record_get_classes_get:
    title: Get Classes
    description: Get Classes Get classes setup information Calls GET /api/v1/record/get_classes on the
      provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_classes
      query:
        transactionType: ${transaction_type}
    input:
      transaction_type:
        type: string
        description: The transactionType parameter.
        optional: true
        default: Income
    output:
      description: Successful Response
      type: object
    read_only: true
  get_record_get_income_categories:
    title: Get Income Categories
    description: Get Income Categories Get categories setup information Calls GET /api/v1/record/get_income_categories
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_income_categories
      query:
        transactionType: ${transaction_type}
    input:
      transaction_type:
        type: string
        description: The transactionType parameter.
        optional: true
        default: Income
    output:
      description: Successful Response
      type: object
    read_only: true
  get_record_get_subcategories:
    title: Get Subcategories
    description: Get Subcategories Get expenses subcategories setup information Calls GET /api/v1/record/get_subcategories
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_subcategories
      query:
        transactionType: ${transaction_type}
        category: ${category}
    input:
      transaction_type:
        type: string
        description: The transactionType parameter.
        optional: true
        default: Income
      category:
        type: string
        description: The category parameter.
    output:
      description: Successful Response
      type: object
    read_only: true
  get_record_get_tax_collection:
    title: Get Tax Collection
    description: Get Tax Collection Get tax collection setup information Calls GET /api/v1/record/get_tax_collection
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/record/get_tax_collection
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
