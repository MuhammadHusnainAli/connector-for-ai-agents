# Stripe App tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/stripe.com/2022-11-15/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: stripe-app
display_name: Stripe App
docs_url: https://stripe.com/us/terms/
generated: true
generated_from: https://api.apis.guru/v2/specs/stripe.com/2022-11-15/openapi.json
tools:
  get_account:
    title: Get account
    description: Retrieves the details of an account. Calls GET /v1/account on the provider's API.
    category: account
    request:
      method: GET
      path: /v1/account
      query:
        expand: ${expand}
    input:
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
    output:
      description: Successful response.
      type: object
    read_only: true
  get_accounts:
    title: Get accounts
    description: Returns a list of accounts connected to your platform via Connect . If you’re not a platform,
      the list is empty. Calls GET /v1/accounts on the provider's API.
    category: accounts
    request:
      method: GET
      path: /v1/accounts
      query:
        created: ${created}
        ending_before: ${ending_before}
        expand: ${expand}
        limit: ${limit}
        starting_after: ${starting_after}
    input:
      created:
        type: string
        description: The created parameter.
        optional: true
      ending_before:
        type: string
        description: A cursor for use in pagination. endingbefore is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, starting with
          objbar, your subsequent call can include endingbefore=objbar in order to fetch the previous
          page of the list.
        optional: true
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: A limit on the number of objects to be returned. Limit can range between 1 and 100,
          and the default is 10.
        optional: true
      starting_after:
        type: string
        description: A cursor for use in pagination. startingafter is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, ending with objfoo,
          your subsequent call can include startingafter=objfoo in order to fetch the next page of the
          list.
        optional: true
    output:
      description: Successful response.
      type: object
    read_only: true
  get_application_fees:
    title: Get application fees
    description: Returns a list of application fees you’ve previously collected. The application fees
      are returned in sorted order, with the most recent fees appearing first. Calls GET /v1/application_fees
      on the provider's API.
    category: application_fees
    request:
      method: GET
      path: /v1/application_fees
      query:
        charge: ${charge}
        created: ${created}
        ending_before: ${ending_before}
        expand: ${expand}
        limit: ${limit}
        starting_after: ${starting_after}
    input:
      charge:
        type: string
        description: Only return application fees for the charge specified by this charge ID.
        optional: true
      created:
        type: string
        description: The created parameter.
        optional: true
      ending_before:
        type: string
        description: A cursor for use in pagination. endingbefore is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, starting with
          objbar, your subsequent call can include endingbefore=objbar in order to fetch the previous
          page of the list.
        optional: true
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: A limit on the number of objects to be returned. Limit can range between 1 and 100,
          and the default is 10.
        optional: true
      starting_after:
        type: string
        description: A cursor for use in pagination. startingafter is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, ending with objfoo,
          your subsequent call can include startingafter=objfoo in order to fetch the next page of the
          list.
        optional: true
    output:
      description: Successful response.
      type: object
    read_only: true
  get_balance:
    title: Get balance
    description: Retrieves the current account balance, based on the authentication that was used to make
      the request. For a sample request, see Accounting for negative balances . Calls GET /v1/balance
      on the provider's API.
    category: balance
    request:
      method: GET
      path: /v1/balance
      query:
        expand: ${expand}
    input:
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
    output:
      description: Successful response.
      type: object
    read_only: true
  get_balance_transactions:
    title: Get balance transactions
    description: Returns a list of transactions that have contributed to the Stripe account balance (e.g.,
      charges, transfers, and so forth). The transactions are returned in sorted order, with the most
      recent transactions appearing first. Note that this endpoint was previously called “Balance history”
      and used the Calls GET /v1/balance_transactions on the provider's API.
    category: balance_transactions
    request:
      method: GET
      path: /v1/balance_transactions
      query:
        created: ${created}
        currency: ${currency}
        ending_before: ${ending_before}
        expand: ${expand}
        limit: ${limit}
        payout: ${payout}
        source: ${source}
        starting_after: ${starting_after}
        type: ${type}
    input:
      created:
        type: string
        description: The created parameter.
        optional: true
      currency:
        type: string
        description: Only return transactions in a certain currency. Three-letter ISO currency code, in
          lowercase. Must be a supported currency.
        optional: true
      ending_before:
        type: string
        description: A cursor for use in pagination. endingbefore is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, starting with
          objbar, your subsequent call can include endingbefore=objbar in order to fetch the previous
          page of the list.
        optional: true
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: A limit on the number of objects to be returned. Limit can range between 1 and 100,
          and the default is 10.
        optional: true
      payout:
        type: string
        description: For automatic Stripe payouts only, only returns transactions that were paid out on
          the specified payout ID.
        optional: true
      source:
        type: string
        description: Only returns the original transaction.
        optional: true
      starting_after:
        type: string
        description: A cursor for use in pagination. startingafter is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, ending with objfoo,
          your subsequent call can include startingafter=objfoo in order to fetch the next page of the
          list.
        optional: true
      type:
        type: string
        description: 'Only returns transactions of the given type. One of: adjustment, advance, advancefunding,
          anticipationrepayment, applicationfee, applicationfeerefund, charge, connectcollectiontransfer,
          contribution, issuingauthorizationhold, issuingauthorizationrelease, issuingdispute, issuingtransaction,
          payment, paymentfailurerefund, paymentrefund, payout, payoutcancel, payoutfailure, refund, refundfailure,
          res'
        optional: true
    output:
      description: Successful response.
      type: object
    read_only: true
  get_charges:
    title: Get charges
    description: Returns a list of charges you’ve previously created. The charges are returned in sorted
      order, with the most recent charges appearing first. Calls GET /v1/charges on the provider's API.
    category: charges
    request:
      method: GET
      path: /v1/charges
      query:
        created: ${created}
        customer: ${customer}
        ending_before: ${ending_before}
        expand: ${expand}
        limit: ${limit}
        payment_intent: ${payment_intent}
        starting_after: ${starting_after}
        transfer_group: ${transfer_group}
    input:
      created:
        type: string
        description: The created parameter.
        optional: true
      customer:
        type: string
        description: Only return charges for the customer specified by this customer ID.
        optional: true
      ending_before:
        type: string
        description: A cursor for use in pagination. endingbefore is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, starting with
          objbar, your subsequent call can include endingbefore=objbar in order to fetch the previous
          page of the list.
        optional: true
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: A limit on the number of objects to be returned. Limit can range between 1 and 100,
          and the default is 10.
        optional: true
      payment_intent:
        type: string
        description: Only return charges that were created by the PaymentIntent specified by this PaymentIntent
          ID.
        optional: true
      starting_after:
        type: string
        description: A cursor for use in pagination. startingafter is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, ending with objfoo,
          your subsequent call can include startingafter=objfoo in order to fetch the next page of the
          list.
        optional: true
      transfer_group:
        type: string
        description: Only return charges for this transfer group.
        optional: true
    output:
      description: Successful response.
      type: object
    read_only: true
  get_country_specs:
    title: Get country specs
    description: Lists all Country Spec objects available in the API. Calls GET /v1/country_specs on the
      provider's API.
    category: country_specs
    request:
      method: GET
      path: /v1/country_specs
      query:
        ending_before: ${ending_before}
        expand: ${expand}
        limit: ${limit}
        starting_after: ${starting_after}
    input:
      ending_before:
        type: string
        description: A cursor for use in pagination. endingbefore is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, starting with
          objbar, your subsequent call can include endingbefore=objbar in order to fetch the previous
          page of the list.
        optional: true
      expand:
        type: array
        description: Specifies which fields in the response should be expanded.
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: A limit on the number of objects to be returned. Limit can range between 1 and 100,
          and the default is 10.
        optional: true
      starting_after:
        type: string
        description: A cursor for use in pagination. startingafter is an object ID that defines your place
          in the list. For instance, if you make a list request and receive 100 objects, ending with objfoo,
          your subsequent call can include startingafter=objfoo in order to fetch the next page of the
          list.
        optional: true
    output:
      description: Successful response.
      type: object
    read_only: true
