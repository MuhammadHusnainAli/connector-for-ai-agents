# Hail tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.hail.so/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: hail
display_name: Hail
docs_url: https://api.hail.so/openapi.json
generated: true
generated_from: https://api.hail.so/openapi.json
tools:
  list_calls_calls_get:
    title: List Calls
    description: List Calls Calls GET /calls on the provider's API.
    category: calls
    request:
      method: GET
      path: /calls
      query:
        cursor: ${cursor}
        limit: ${limit}
        status: ${status}
        to: ${to}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      status:
        type: string
        description: The status parameter.
        optional: true
      to:
        type: string
        description: The to parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  create_call_calls_post:
    title: Create Call
    description: Create Call Calls POST /calls on the provider's API.
    category: calls
    request:
      method: POST
      path: /calls
      body:
        recipient_consent: ${recipient_consent}
        consent_source: ${consent_source}
        consent_obtained_at: ${consent_obtained_at}
        message_type: ${message_type}
        to: ${to}
        from: ${from}
        system_prompt: ${system_prompt}
        llm: ${llm}
        first_message: ${first_message}
        ai_disclosure: ${ai_disclosure}
        voice_config: ${voice_config}
        conversation_id: ${conversation_id}
    input:
      recipient_consent:
        type: boolean
        description: Attestation that you have obtained the lawful consent required to contact this recipient.
          Hail does not verify consent itself — you are responsible for a lawful basis under TCPA/ePrivacy/PECR/CAN-SPAM/GDPR
          as applicable. Rejected (422) if not true.
      consent_source:
        type: string
        description: Where/how consent was obtained (e.g. 'signup form', 'prior customer relationship').
          Required (non-empty) when messagetype is 'marketing'.
        optional: true
      consent_obtained_at:
        type: string
        description: When consent was obtained, if known.
        optional: true
      message_type:
        type: string
        description: '''marketing'' additionally requires a non-empty consentsource. Use ''informational''
          for transactional/service communications.'
        optional: true
        enum:
        - marketing
        - informational
      to:
        type: string
        description: The to field of the request body.
      from:
        type: string
        description: The from field of the request body.
        optional: true
      system_prompt:
        type: string
        description: The system_prompt field of the request body.
        optional: true
      llm:
        type: string
        description: The llm field of the request body.
        optional: true
      first_message:
        type: string
        description: The first_message field of the request body.
        optional: true
      ai_disclosure:
        type: boolean
        description: Speak the AI self-disclosure line ('Hi, this is an AI assistant calling on behalf
          of ...') as the first thing on the call. Enabled by default. Disable only if you have verified
          the disclosure is not required for this call — 47 CFR 64.1200(b)(1) requires identifying the
          initiating business at the start of artificial-voice calls in the US, and several jurisdictions
          have AI bot-disclosure laws. Hail
        optional: true
      voice_config:
        type: object
        description: The voice_config field of the request body.
        optional: true
      conversation_id:
        type: string
        description: The conversation_id field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_contacts_contacts_get:
    title: List Contacts
    description: List Contacts Calls GET /contacts on the provider's API.
    category: contacts
    request:
      method: GET
      path: /contacts
      query:
        q: ${q}
        cursor: ${cursor}
        limit: ${limit}
    input:
      q:
        type: string
        description: The q parameter.
        optional: true
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 100
    output:
      description: Successful Response
      type: object
    read_only: true
  create_contact_contacts_post:
    title: Create Contact
    description: Create Contact Calls POST /contacts on the provider's API.
    category: contacts
    request:
      method: POST
      path: /contacts
      body:
        name: ${name}
        phone_e164: ${phone_e164}
        email: ${email}
    input:
      name:
        type: string
        description: The name field of the request body.
      phone_e164:
        type: string
        description: The phone_e164 field of the request body.
        optional: true
      email:
        type: string
        description: The email field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_email_domains_email_domains_get:
    title: List Email Domains
    description: List Email Domains Calls GET /email-domains on the provider's API.
    category: email_domains
    request:
      method: GET
      path: /email-domains
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
    output:
      description: Successful Response
      type: object
    read_only: true
  create_email_domain_email_domains_post:
    title: Create Email Domain
    description: Create Email Domain Calls POST /email-domains on the provider's API.
    category: email_domains
    request:
      method: POST
      path: /email-domains
      body:
        kind: ${kind}
        domain: ${domain}
        local_prefix_user: ${local_prefix_user}
        local_prefix_org: ${local_prefix_org}
    input:
      kind:
        type: string
        description: The kind field of the request body.
        enum:
        - hail_mail
        - custom
      domain:
        type: string
        description: The domain field of the request body.
        optional: true
      local_prefix_user:
        type: string
        description: The local_prefix_user field of the request body.
        optional: true
      local_prefix_org:
        type: string
        description: The local_prefix_org field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_emails_emails_get:
    title: List Emails
    description: List Emails Calls GET /emails on the provider's API.
    category: emails
    request:
      method: GET
      path: /emails
      query:
        cursor: ${cursor}
        limit: ${limit}
        status: ${status}
        direction: ${direction}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      status:
        type: string
        description: The status parameter.
        optional: true
      direction:
        type: string
        description: The direction parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  create_email_emails_post:
    title: Create Email
    description: Create Email Calls POST /emails on the provider's API.
    category: emails
    request:
      method: POST
      path: /emails
      body:
        recipient_consent: ${recipient_consent}
        consent_source: ${consent_source}
        consent_obtained_at: ${consent_obtained_at}
        message_type: ${message_type}
        from: ${from}
        from_name: ${from_name}
        to: ${to}
        cc: ${cc}
        bcc: ${bcc}
        reply_to: ${reply_to}
        subject: ${subject}
        body_text: ${body_text}
    input:
      recipient_consent:
        type: boolean
        description: Attestation that you have obtained the lawful consent required to contact this recipient.
          Hail does not verify consent itself — you are responsible for a lawful basis under TCPA/ePrivacy/PECR/CAN-SPAM/GDPR
          as applicable. Rejected (422) if not true.
      consent_source:
        type: string
        description: Where/how consent was obtained (e.g. 'signup form', 'prior customer relationship').
          Required (non-empty) when messagetype is 'marketing'.
        optional: true
      consent_obtained_at:
        type: string
        description: When consent was obtained, if known.
        optional: true
      message_type:
        type: string
        description: '''marketing'' additionally requires a non-empty consentsource. Use ''informational''
          for transactional/service communications.'
        optional: true
        enum:
        - marketing
        - informational
      from:
        type: string
        description: The from field of the request body.
        optional: true
      from_name:
        type: string
        description: The from_name field of the request body.
        optional: true
      to:
        type: array
        description: The to field of the request body.
        items:
          type: string
      cc:
        type: string
        description: The cc field of the request body.
        optional: true
      bcc:
        type: string
        description: The bcc field of the request body.
        optional: true
      reply_to:
        type: string
        description: The reply_to field of the request body.
        optional: true
      subject:
        type: string
        description: The subject field of the request body.
      body_text:
        type: string
        description: The body_text field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  list_events_events_get:
    title: List Events
    description: List Events Calls GET /events on the provider's API.
    category: events
    request:
      method: GET
      path: /events
      query:
        cursor: ${cursor}
        limit: ${limit}
        id: ${id}
        kind: ${kind}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 100
      id:
        type: string
        description: The id parameter.
        optional: true
      kind:
        type: string
        description: The kind parameter.
        optional: true
    output:
      description: Successful Response
      type: object
    read_only: true
  healthz_healthz_get:
    title: Healthz
    description: Healthz Calls GET /healthz on the provider's API.
    category: healthz
    request:
      method: GET
      path: /healthz
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  list_numbers_numbers_get:
    title: List Numbers
    description: List Numbers Calls GET /numbers on the provider's API.
    category: numbers
    request:
      method: GET
      path: /numbers
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
    output:
      description: Successful Response
      type: object
    read_only: true
  acquire_number_numbers_post:
    title: Acquire Number
    description: Acquire Number Calls POST /numbers on the provider's API.
    category: numbers
    request:
      method: POST
      path: /numbers
      body:
        country_code: ${country_code}
        number_type: ${number_type}
    input:
      country_code:
        type: string
        description: The country_code field of the request body.
      number_type:
        type: string
        description: The number_type field of the request body.
        optional: true
        enum:
        - local
        - mobile
        - toll_free
        - national
    output:
      description: Successful Response
      type: object
