# Svix tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/svix.com/1.4/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: svix
display_name: Svix
docs_url: https://api.apis.guru/v2/specs/svix.com/1.4/openapi.json
generated: true
generated_from: https://api.apis.guru/v2/specs/svix.com/1.4/openapi.json
tools:
  list_applications_api_v1_app_get:
    title: List Applications
    description: List Applications List of all the organization's applications. Calls GET /api/v1/app/
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/app/
      query:
        iterator: ${iterator}
        limit: ${limit}
        order: ${order}
    input:
      iterator:
        type: string
        description: The iterator parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      order:
        type: string
        description: The order parameter.
        optional: true
        default: descending
    output:
      description: Successful Response
      type: object
    read_only: true
  create_application_api_v1_app_post:
    title: Create Application
    description: Create Application Create a new application. Calls POST /api/v1/app/ on the provider's
      API.
    category: api
    request:
      method: POST
      path: /api/v1/app/
      query:
        get_if_exists: ${get_if_exists}
      body:
        metadata: ${metadata}
        name: ${name}
        rateLimit: ${rate_limit}
        uid: ${uid}
    input:
      get_if_exists:
        type: boolean
        description: Get an existing application, or create a new one if doesn't exist. It's two separate
          functions in the libs.
        optional: true
        default: false
      metadata:
        type: object
        description: The metadata field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
      rate_limit:
        type: integer
        description: The rateLimit field of the request body.
        optional: true
      uid:
        type: string
        description: Optional unique identifier for the application
        optional: true
    output:
      description: OK
      type: object
  list_background_tasks_api_v1_background_task_get:
    title: List Background Tasks
    description: List Background Tasks List background tasks executed in the past 90 days. Calls GET /api/v1/background-task/
      on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/background-task/
      query:
        iterator: ${iterator}
        limit: ${limit}
        order: ${order}
    input:
      iterator:
        type: string
        description: The iterator parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      order:
        type: string
        description: The order parameter.
        optional: true
        default: descending
    output:
      description: Successful Response
      type: object
    read_only: true
  list_event_types_api_v1_event_type_get:
    title: List Event Types
    description: List Event Types Return the list of event types. Calls GET /api/v1/event-type/ on the
      provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/event-type/
      query:
        iterator: ${iterator}
        limit: ${limit}
        with_content: ${with_content}
        include_archived: ${include_archived}
    input:
      iterator:
        type: string
        description: The iterator parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 50
      with_content:
        type: boolean
        description: The with_content parameter.
        optional: true
        default: false
      include_archived:
        type: boolean
        description: The include_archived parameter.
        optional: true
        default: false
    output:
      description: Successful Response
      type: object
    read_only: true
  create_event_type_api_v1_event_type_post:
    title: Create Event Type
    description: Create Event Type Create new or unarchive existing event type. Unarchiving an event type
      will allow endpoints to filter on it and messages to be sent with it. Endpoints filtering on the
      event type before archival will continue to filter on it. This operation does not preserve the description
      and schemas. Calls POST /api/v1/event-type/ on the provider's API.
    category: api
    request:
      method: POST
      path: /api/v1/event-type/
      body:
        archived: ${archived}
        description: ${description}
        featureFlag: ${feature_flag}
        name: ${name}
        schemas: ${schemas}
    input:
      archived:
        type: boolean
        description: The archived field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
      feature_flag:
        type: string
        description: The featureFlag field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
      schemas:
        type: object
        description: The schema for the event type for a specific version as a JSON schema.
        optional: true
    output:
      description: Successful Response
      type: object
  health_api_v1_health_get:
    title: Health
    description: Health Verify the API server is up and running. Calls GET /api/v1/health/ on the provider's
      API.
    category: api
    request:
      method: GET
      path: /api/v1/health/
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  delete_application_api_v1_app_app_id_delete:
    title: Delete Application
    description: Delete Application Delete an application. Calls DELETE /api/v1/app/{app_id}/ on the provider's
      API.
    category: api
    request:
      method: DELETE
      path: /api/v1/app/${app_id}/
    input:
      app_id:
        type: string
        description: The app_id parameter.
    output:
      description: Successful Response
      type: object
    destructive: true
  get_application_api_v1_app_app_id_get:
    title: Get Application
    description: Get Application Get an application. Calls GET /api/v1/app/{app_id}/ on the provider's
      API.
    category: api
    request:
      method: GET
      path: /api/v1/app/${app_id}/
    input:
      app_id:
        type: string
        description: The app_id parameter.
    output:
      description: Successful Response
      type: object
    read_only: true
  update_application_api_v1_app_app_id_put:
    title: Update Application
    description: Update Application Update an application. Calls PUT /api/v1/app/{app_id}/ on the provider's
      API.
    category: api
    request:
      method: PUT
      path: /api/v1/app/${app_id}/
      body:
        metadata: ${metadata}
        name: ${name}
        rateLimit: ${rate_limit}
        uid: ${uid}
    input:
      app_id:
        type: string
        description: The app_id parameter.
      metadata:
        type: object
        description: The metadata field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
      rate_limit:
        type: integer
        description: The rateLimit field of the request body.
        optional: true
      uid:
        type: string
        description: Optional unique identifier for the application
        optional: true
    output:
      description: Successful Response
      type: object
