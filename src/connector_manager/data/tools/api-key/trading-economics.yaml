# Trading Economics tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.tradingeconomics.com/swagger/v1/swagger.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: trading-economics
display_name: Trading Economics
docs_url: https://api.tradingeconomics.com/swagger/v1/swagger.json
generated: true
generated_from: https://api.tradingeconomics.com/swagger/v1/swagger.json
tools:
  get_calendar:
    title: Get calendar
    description: Calls GET /calendar on the provider's API, as described by its OpenAPI specification.
    category: calendar
    request:
      method: GET
      path: /calendar
      query:
        limit: ${limit}
        importance: ${importance}
        lang: ${lang}
        values: ${values}
        f: ${f}
    input:
      limit:
        type: integer
        description: The limit parameter.
        optional: true
      importance:
        type: integer
        description: 1 = low, 2 = medium, 3 = high
        optional: true
      lang:
        type: string
        description: e.g. en, es, fr, de, pt
        optional: true
      values:
        type: string
        description: e.g. True
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_changes:
    title: Get changes
    description: Calls GET /changes on the provider's API, as described by its OpenAPI specification.
    category: changes
    request:
      method: GET
      path: /changes
      query:
        f: ${f}
    input:
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_comtrade:
    title: Get comtrade
    description: Calls GET /comtrade on the provider's API, as described by its OpenAPI specification.
    category: comtrade
    request:
      method: GET
      path: /comtrade
      query:
        f: ${f}
    input:
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_country:
    title: Get country
    description: Calls GET /country on the provider's API, as described by its OpenAPI specification.
    category: country
    request:
      method: GET
      path: /country
      query:
        f: ${f}
    input:
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_credit_ratings:
    title: Get credit ratings
    description: Calls GET /credit-ratings on the provider's API, as described by its OpenAPI specification.
    category: credit_ratings
    request:
      method: GET
      path: /credit-ratings
      query:
        f: ${f}
    input:
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_dividends:
    title: Get dividends
    description: Calls GET /dividends on the provider's API, as described by its OpenAPI specification.
    category: dividends
    request:
      method: GET
      path: /dividends
      query:
        d1: ${d1}
        d2: ${d2}
        f: ${f}
    input:
      d1:
        type: string
        description: Start date in yyyy-MM-dd format. eg. 2024-01-01
        optional: true
      d2:
        type: string
        description: End date in yyyy-MM-dd format. eg. 2025-01-01
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_earnings:
    title: Get earnings
    description: Calls GET /earnings on the provider's API, as described by its OpenAPI specification.
    category: earnings
    request:
      method: GET
      path: /earnings
      query:
        d1: ${d1}
        d2: ${d2}
        type: ${type}
        f: ${f}
    input:
      d1:
        type: string
        description: Start date in yyyy-MM-dd format. eg. 2024-01-01
        optional: true
      d2:
        type: string
        description: End date in yyyy-MM-dd format. eg. 2025-01-01
        optional: true
      type:
        type: string
        description: 'Trade type: export or import'
        optional: true
        default: any
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_earnings_revenues:
    title: Get earnings revenues
    description: Calls GET /earnings-revenues on the provider's API, as described by its OpenAPI specification.
    category: earnings_revenues
    request:
      method: GET
      path: /earnings-revenues
      query:
        d1: ${d1}
        d2: ${d2}
        f: ${f}
    input:
      d1:
        type: string
        description: Start date in yyyy-MM-dd format. eg. 2024-01-01
        optional: true
      d2:
        type: string
        description: End date in yyyy-MM-dd format. eg. 2025-01-01
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_eurostat:
    title: Get eurostat
    description: Calls GET /eurostat on the provider's API, as described by its OpenAPI specification.
    category: eurostat
    request:
      method: GET
      path: /eurostat
      query:
        category: ${category}
        category_group: ${category_group}
        f: ${f}
    input:
      category:
        type: string
        description: Category name
        optional: true
      category_group:
        type: string
        description: Category group name, e.g. interest rates, inflation rate
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_indicators:
    title: Get indicators
    description: Calls GET /indicators on the provider's API, as described by its OpenAPI specification.
    category: indicators
    request:
      method: GET
      path: /indicators
      query:
        calendar: ${calendar}
        country: ${country}
        f: ${f}
    input:
      calendar:
        type: integer
        description: Set to 1 to list calendar indicators
        optional: true
      country:
        type: string
        description: Country name, e.g. European Union, Denmark, world
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_ipo:
    title: Get ipo
    description: Calls GET /ipo on the provider's API, as described by its OpenAPI specification.
    category: ipo
    request:
      method: GET
      path: /ipo
      query:
        d1: ${d1}
        d2: ${d2}
        f: ${f}
    input:
      d1:
        type: string
        description: Start date in yyyy-MM-dd format. eg. 2024-01-01
        optional: true
      d2:
        type: string
        description: End date in yyyy-MM-dd format. eg. 2025-01-01
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
  get_news:
    title: Get news
    description: Calls GET /news on the provider's API, as described by its OpenAPI specification.
    category: news
    request:
      method: GET
      path: /news
      query:
        type: ${type}
        d1: ${d1}
        d2: ${d2}
        f: ${f}
    input:
      type:
        type: string
        description: 'Trade type: export or import'
        optional: true
      d1:
        type: string
        description: Start date in yyyy-MM-dd format. eg. 2024-01-01
        optional: true
      d2:
        type: string
        description: End date in yyyy-MM-dd format. eg. 2025-01-01
        optional: true
      f:
        type: string
        description: 'Response format: json (default), csv, or html'
        optional: true
        enum:
        - json
        - csv
        - html
        default: json
    output:
      description: OK
      type: object
    read_only: true
