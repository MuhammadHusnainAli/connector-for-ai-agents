# Serply tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.serply.io/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: serply
display_name: Serply
docs_url: https://api.serply.io/openapi.json
generated: true
generated_from: https://api.serply.io/openapi.json
tools:
  ping_ping_post:
    title: Ping
    description: Ping Calls GET /ping on the provider's API.
    category: ping
    request:
      method: GET
      path: /ping
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  proxy_api_path_post:
    title: Proxy Api
    description: Proxy Api Main API endpoint that proxies all requests after authentication. Calls DELETE
      /{path} on the provider's API.
    category: api
    request:
      method: DELETE
      path: /${path}
    input:
      path:
        type: string
        description: The path parameter.
    output:
      description: Successful Response
      type: object
    destructive: true
  proxy_api_path_post_alt:
    title: Proxy Api
    description: Proxy Api Main API endpoint that proxies all requests after authentication. Calls GET
      /{path} on the provider's API.
    category: api
    request:
      method: GET
      path: /${path}
    input:
      path:
        type: string
        description: The path parameter.
    output:
      description: Successful Response
      type: object
    read_only: true
  proxy_api_path_post_alt_alt:
    title: Proxy Api
    description: Proxy Api Main API endpoint that proxies all requests after authentication. Calls PATCH
      /{path} on the provider's API.
    category: api
    request:
      method: PATCH
      path: /${path}
    input:
      path:
        type: string
        description: The path parameter.
    output:
      description: Successful Response
      type: object
  proxy_api_path_post_alt_alt_alt:
    title: Proxy Api
    description: Proxy Api Main API endpoint that proxies all requests after authentication. Calls POST
      /{path} on the provider's API.
    category: api
    request:
      method: POST
      path: /${path}
    input:
      path:
        type: string
        description: The path parameter.
    output:
      description: Successful Response
      type: object
  proxy_api_path_post_alt_alt_alt_alt:
    title: Proxy Api
    description: Proxy Api Main API endpoint that proxies all requests after authentication. Calls PUT
      /{path} on the provider's API.
    category: api
    request:
      method: PUT
      path: /${path}
    input:
      path:
        type: string
        description: The path parameter.
    output:
      description: Successful Response
      type: object
  get_webhooks_api_v1_webhooks_get:
    title: Get Webhooks
    description: Get Webhooks Calls GET /api/v1/webhooks on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/webhooks
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
