# BuiltWith tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.builtwith.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: builtwith
display_name: BuiltWith
docs_url: https://api.builtwith.com/x402-api
generated: true
generated_from: https://api.builtwith.com/openapi.json
tools:
  ask_for_websites:
    title: Find websites with a natural-language query
    description: Find websites with a natural-language query Run an uncommitted BuiltWith Ask query and
      return matching websites for research and qualification. This route does not create a persistent
      List report. Calls POST /agent/ask on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/ask
      body:
        query: ${query}
        meta: ${meta}
    input:
      query:
        type: string
        description: Natural-language description of the websites to find.
      meta:
        type: boolean
        description: Include additional BuiltWith metadata when available.
        optional: true
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  get_domain_technology_changes:
    title: Get technology additions and removals for a domain
    description: Get technology additions and removals for a domain Return historical technology changes
      for one domain, optionally constrained by a human-readable time boundary such as 'last month'. Calls
      POST /agent/changes on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/changes
      body:
        domain: ${domain}
        since: ${since}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
      since:
        type: string
        description: Optional BuiltWith time boundary, for example 'last month'.
        optional: true
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  find_company_domains:
    title: Find domains associated with a company name
    description: Find domains associated with a company name Resolve a company or brand name to associated
      website domains before running domain-level research. Calls POST /agent/company-domains on the provider's
      API.
    category: agent
    request:
      method: POST
      path: /agent/company-domains
      body:
        company: ${company}
    input:
      company:
        type: string
        description: Company or brand name.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  get_company_identifiers:
    title: Get public company identifiers for a domain
    description: Get public company identifiers for a domain Return public company registration, tax,
      and VAT identifiers associated with one domain when available. Calls POST /agent/company-identifiers
      on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/company-identifiers
      body:
        domain: ${domain}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  lookup_domain_technologies:
    title: Look up a domain's technologies and metadata
    description: Look up a domain's technologies and metadata Return BuiltWith technology detections and
      metadata for one domain. Useful for company research, stack analysis, competitive intelligence,
      and sales qualification. Calls GET /agent/domain on the provider's API.
    category: agent
    request:
      method: GET
      path: /agent/domain
      query:
        domain: ${domain}
        liveOnly: ${live_only}
    input:
      domain:
        type: string
        description: The domain parameter.
      live_only:
        type: boolean
        description: The liveOnly parameter.
        optional: true
        default: false
    output:
      description: BuiltWith result returned after payment settlement
      type: object
    read_only: true
  get_domain_keywords:
    title: Get keyword data for a domain
    description: Get keyword data for a domain Return keywords and topical signals associated with one
      website domain. Calls POST /agent/keywords on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/keywords
      body:
        domain: ${domain}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  recommend_technologies:
    title: Recommend technologies for a domain
    description: Recommend technologies for a domain Return technology recommendations inferred from a
      domain's existing stack and comparable sites. Calls POST /agent/recommendations on the provider's
      API.
    category: agent
    request:
      method: POST
      path: /agent/recommendations
      body:
        domain: ${domain}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  get_domain_redirects:
    title: Get live and historical redirects for a domain
    description: Get live and historical redirects for a domain Return redirect destinations observed
      for a domain, useful for brand, migration, acquisition, and asset research. Calls POST /agent/redirects
      on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/redirects
      body:
        domain: ${domain}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
  find_related_websites:
    title: Find websites related to a domain
    description: Find websites related to a domain Find domains related through shared identifiers, infrastructure,
      analytics, ownership signals, and other BuiltWith relationship evidence. Calls POST /agent/relationships
      on the provider's API.
    category: agent
    request:
      method: POST
      path: /agent/relationships
      body:
        domain: ${domain}
    input:
      domain:
        type: string
        description: Domain name to research, for example example.com. Do not include a URL path.
    output:
      description: BuiltWith result returned after payment settlement
      type: object
