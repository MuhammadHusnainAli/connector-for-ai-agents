# Lumin PDF tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://developers.luminpdf.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: lumin-pdf
display_name: Lumin PDF
docs_url: https://developers.luminpdf.com/openapi.json
generated: true
generated_from: https://developers.luminpdf.com/openapi.json
tools:
  create_agreements:
    title: Create Agreement
    description: Create Agreement Create a new AgreementGen document from a AgreementGen template. Calls
      POST /agreements on Lumin PDF's API.
    category: agreements
    request:
      method: POST
      path: /agreements
      body:
        method: ${method}
        agreement_name: ${agreement_name}
        agreement_data: ${agreement_data}
    input:
      method:
        type: string
        description: Must be set to template.
        enum:
        - template
      agreement_name:
        type: string
        description: Human-friendly title of the agreement (1-255 chars). Defaults to input name or "New
          document" if not provided in the request body.
        optional: true
      agreement_data:
        type: object
        description: The agreement_data field of the request body.
    output:
      description: Returns the information of the created AgreementGen document.
      type: object
  get_agreements_file:
    title: Get Agreement File
    description: Get Agreement File Obtain a downloadable file or binary PDF data from an AgreementGen
      document. Calls GET /agreements/{agreement_id}/file on Lumin PDF's API.
    category: agreements
    request:
      method: GET
      path: /agreements/${agreement_id}/file
    input:
      agreement_id:
        type: string
        description: ID of the AgreementGen document.
    output:
      description: Returns the downloadable file or binary PDF data of the AgreementGen document.
      type: object
    read_only: true
  update_signature_request_cancel:
    title: Cancel Signature Request
    description: Cancel Signature Request Cancel a signature request. Calls PUT /signature_request/cancel/{signature_request_id}
      on Lumin PDF's API.
    category: signature_request
    request:
      method: PUT
      path: /signature_request/cancel/${signature_request_id}
    input:
      signature_request_id:
        type: string
        description: ID of the signature request.
    output:
      description: Returns the information of the cancelled signature request.
      type: object
