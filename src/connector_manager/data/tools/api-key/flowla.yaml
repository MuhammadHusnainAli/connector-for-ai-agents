# Flowla tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://docs.flowla.com/openapi/v1.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: flowla
display_name: Flowla
docs_url: https://docs.flowla.com/openapi/v1.json
generated: true
generated_from: https://docs.flowla.com/openapi/v1.json
tools:
  create_rooms:
    title: Create a room
    description: Create a room Creates a blank room or duplicates an existing room. Pass duplicateFromId
      to copy from an existing room, or omit it (and optionally supply templateId) to start fresh. Calls
      POST /rooms on Flowla's API.
    category: rooms
    request:
      method: POST
      path: /rooms
      body:
        title: ${title}
        description: ${description}
        email: ${email}
        userId: ${user_id}
        companyId: ${company_id}
        labelId: ${label_id}
        statusId: ${status_id}
        templateId: ${template_id}
        duplicateFromId: ${duplicate_from_id}
        dealIdStr: ${deal_id_str}
        currency: ${currency}
        amount: ${amount}
    input:
      title:
        type: string
        description: The display title of the room.
      description:
        type: string
        description: A short description shown in room previews.
        optional: true
      email:
        type: string
        description: Email address of the primary contact for this room.
        optional: true
      user_id:
        type: string
        description: ID of the Flowla user to assign as the room owner.
        optional: true
      company_id:
        type: string
        description: ID of the company to associate with this room.
        optional: true
      label_id:
        type: string
        description: ID of the label to apply to the room.
        optional: true
      status_id:
        type: string
        description: ID of the status to set on the room.
        optional: true
      template_id:
        type: string
        description: Template ID to initialise the room from. Ignored when duplicateFromId is set.
        optional: true
      duplicate_from_id:
        type: string
        description: ID of an existing room to duplicate. When provided, the new room is an exact copy
          of the source room.
        optional: true
      deal_id_str:
        type: string
        description: External deal or opportunity ID from your CRM.
        optional: true
      currency:
        type: string
        description: ISO 4217 currency code (e.g. USD, EUR, AED).
        optional: true
      amount:
        type: number
        description: Deal or contract amount in the specified currency.
        optional: true
    output:
      description: Created
      type: object
  get_status:
    title: Check API status
    description: Check API status Use this endpoint for monitoring and uptime checks. Calls GET /status
      on Flowla's API.
    category: status
    request:
      method: GET
      path: /status
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  get_statuses:
    title: List room statuses
    description: List room statuses Calls GET /statuses on Flowla's API. Generated from the provider's
      published OpenAPI specification.
    category: statuses
    request:
      method: GET
      path: /statuses
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  get_templates:
    title: List templates
    description: List templates Calls GET /templates on Flowla's API. Generated from the provider's published
      OpenAPI specification.
    category: templates
    request:
      method: GET
      path: /templates
      query:
        template_ids: ${template_ids}
    input:
      template_ids:
        type: string
        description: Comma-separated template IDs.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  create_templates:
    title: Create a template
    description: Create a template Calls POST /templates on Flowla's API. Generated from the provider's
      published OpenAPI specification.
    category: templates
    request:
      method: POST
      path: /templates
      body:
        title: ${title}
        description: ${description}
        room_id: ${room_id}
    input:
      title:
        type: string
        description: The title field of the request body.
      description:
        type: string
        description: The description field of the request body.
        optional: true
      room_id:
        type: string
        description: The room_id field of the request body.
        optional: true
    output:
      description: Created
      type: object
  get_users:
    title: List organization users
    description: List organization users Calls GET /users on Flowla's API. Generated from the provider's
      published OpenAPI specification.
    category: users
    request:
      method: GET
      path: /users
      query:
        cursor: ${cursor}
        limit: ${limit}
    input:
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  create_assets_bulk_upload:
    title: Bulk upload assets
    description: Bulk upload assets Upload multiple assets into the asset library in a single request.
      Calls POST /assets/bulk-upload on Flowla's API.
    category: assets
    request:
      method: POST
      path: /assets/bulk-upload
      body:
        assets: ${assets}
    input:
      assets:
        type: array
        description: The assets field of the request body.
        items:
          type: object
    output:
      description: Created
      type: object
  get_assets_search:
    title: Search assets
    description: Search assets Search the asset library by query text and optional filters. Calls GET
      /assets/search on Flowla's API.
    category: assets
    request:
      method: GET
      path: /assets/search
      query:
        q: ${q}
        folder_id: ${folder_id}
        cursor: ${cursor}
        limit: ${limit}
    input:
      q:
        type: string
        description: The q parameter.
      folder_id:
        type: string
        description: The folder_id parameter.
        optional: true
      cursor:
        type: string
        description: The cursor parameter.
        optional: true
      limit:
        type: integer
        description: The limit parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_assets_share_links:
    title: Get many asset share links
    description: Get many asset share links Resolve share links for a set of asset IDs. Calls GET /assets/share-links
      on Flowla's API.
    category: assets
    request:
      method: GET
      path: /assets/share-links
      query:
        asset_ids: ${asset_ids}
    input:
      asset_ids:
        type: string
        description: 'Comma-separated asset IDs. Example: ast01,ast02,ast03.'
    output:
      description: OK
      type: object
    read_only: true
  update_actions:
    title: Edit an action
    description: Edit an action Calls PATCH /actions/{action_id} on Flowla's API.
    category: actions
    request:
      method: PATCH
      path: /actions/${action_id}
    input:
      action_id:
        type: string
        description: The action_id parameter.
    output:
      description: OK
      type: object
  update_assets:
    title: Update asset metadata
    description: Update asset metadata Calls PATCH /assets/{asset_id} on Flowla's API.
    category: assets
    request:
      method: PATCH
      path: /assets/${asset_id}
      body:
        title: ${title}
        description: ${description}
        tags: ${tags}
        folder_id: ${folder_id}
        metadata: ${metadata}
    input:
      asset_id:
        type: string
        description: The asset_id parameter.
      title:
        type: string
        description: The title field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
        optional: true
      tags:
        type: array
        description: The tags field of the request body.
        optional: true
        items:
          type: string
      folder_id:
        type: string
        description: The folder_id field of the request body.
        optional: true
      metadata:
        type: object
        description: The metadata field of the request body.
        optional: true
    output:
      description: OK
      type: object
  delete_rooms:
    title: Delete a room
    description: Delete a room Calls DELETE /rooms/{room_id} on Flowla's API.
    category: rooms
    request:
      method: DELETE
      path: /rooms/${room_id}
    input:
      room_id:
        type: string
        description: The room_id parameter.
    output:
      description: OK
      type: object
    destructive: true
