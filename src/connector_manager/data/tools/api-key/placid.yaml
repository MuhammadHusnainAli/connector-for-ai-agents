# Placid tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.placid.app/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: placid
display_name: Placid
docs_url: https://api.placid.app/openapi.json
generated: true
generated_from: https://api.placid.app/openapi.json
tools:
  create_image:
    title: Create image
    description: Fill an existing template's text and picture layers with content and create an image;
      refetch api/openai/templates if you cant find the template Calls POST /api/openai/images on the
      provider's API.
    category: api
    request:
      method: POST
      path: /api/openai/images
      body:
        template_uuid: ${template_uuid}
        layer_modifications: ${layer_modifications}
    input:
      template_uuid:
        type: string
        description: The UUID of the template
        optional: true
      layer_modifications:
        type: array
        description: This array holds a list of template layer modifications
        optional: true
        items:
          type: object
    output:
      description: OK
      type: object
  create_pdf:
    title: Create pdf
    description: Fill an existing template's text and picture layers with content and create a pdf; refetch
      api/openai/templates if you cant find the template Calls POST /api/openai/pdfs on the provider's
      API.
    category: api
    request:
      method: POST
      path: /api/openai/pdfs
      body:
        template_uuid: ${template_uuid}
        layer_modifications: ${layer_modifications}
    input:
      template_uuid:
        type: string
        description: The UUID of the template
        optional: true
      layer_modifications:
        type: array
        description: This array holds a list of template layer modifications
        optional: true
        items:
          type: object
    output:
      description: OK
      type: object
  get_templates:
    title: List all available templates
    description: List all available templates List all templates this user has in his project Calls GET
      /api/openai/templates on the provider's API.
    category: api
    request:
      method: GET
      path: /api/openai/templates
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  create_video:
    title: Create video
    description: Fill an existing template's text and picture layers with content and create a video;
      refetch api/openai/templates if you cant find the template Calls POST /api/openai/videos on the
      provider's API.
    category: api
    request:
      method: POST
      path: /api/openai/videos
      body:
        template_uuid: ${template_uuid}
        layer_modifications: ${layer_modifications}
    input:
      template_uuid:
        type: string
        description: The UUID of the template
        optional: true
      layer_modifications:
        type: array
        description: This array holds a list of template layer modifications
        optional: true
        items:
          type: object
    output:
      description: OK
      type: object
  get_video:
    title: Get a single video
    description: Get a single video Calls GET /api/openai/videos/{id} on the provider's API.
    category: api
    request:
      method: GET
      path: /api/openai/videos/${id}
    input:
      id:
        type: string
        description: ID of the video
    output:
      description: OK
      type: object
    read_only: true
