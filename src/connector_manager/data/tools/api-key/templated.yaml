# Templated tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.templated.io/v3/api-docs
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: templated
display_name: Templated
docs_url: https://api.templated.io/v3/api-docs
generated: true
generated_from: https://api.templated.io/v3/api-docs
tools:
  auth:
    title: Auth
    description: Calls GET /auth on Templated's API. Generated from the provider's published OpenAPI specification.
    category: auth
    request:
      method: GET
      path: /auth
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  get_team_jobs_1:
    title: Get team jobs 1
    description: Calls GET /bulk-render-jobs on Templated's API. Generated from the provider's published
      OpenAPI specification.
    category: bulk_render_jobs
    request:
      method: GET
      path: /bulk-render-jobs
      query:
        page: ${page}
        limit: ${limit}
    input:
      page:
        type: integer
        description: The page parameter.
        optional: true
        default: 0
      limit:
        type: integer
        description: The limit parameter.
        optional: true
        default: 10
    output:
      description: OK
      type: object
    read_only: true
  get_all_categories:
    title: Get all categories
    description: Calls GET /categories on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: categories
    request:
      method: GET
      path: /categories
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  create_category:
    title: Create category
    description: Calls POST /categories on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: categories
    request:
      method: POST
      path: /categories
      body:
        id: ${id}
        name: ${name}
        description: ${description}
        cover: ${cover}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
        optional: true
      cover:
        type: string
        description: The cover field of the request body.
        optional: true
    output:
      description: OK
      type: object
  create_folder_1:
    title: Create folder 1
    description: Calls POST /folders on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: folders
    request:
      method: POST
      path: /folders
      body:
        id: ${id}
        name: ${name}
        userId: ${user_id}
        teamId: ${team_id}
        createdAt: ${created_at}
        updatedAt: ${updated_at}
        templateCount: ${template_count}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
        optional: true
      user_id:
        type: string
        description: The userId field of the request body.
        optional: true
      team_id:
        type: string
        description: The teamId field of the request body.
        optional: true
      created_at:
        type: string
        description: The createdAt field of the request body.
        optional: true
      updated_at:
        type: string
        description: The updatedAt field of the request body.
        optional: true
      template_count:
        type: integer
        description: The templateCount field of the request body.
        optional: true
    output:
      description: OK
      type: object
  create_team:
    title: Create team
    description: Calls POST /teams on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: teams
    request:
      method: POST
      path: /teams
      body:
        id: ${id}
        name: ${name}
        apiKey: ${api_key}
        apiQuota: ${api_quota}
        apiUsage: ${api_usage}
        templatesLimit: ${templates_limit}
        membersLimit: ${members_limit}
        stripeCustomerId: ${stripe_customer_id}
        status: ${status}
        plan: ${plan}
        billingPeriod: ${billing_period}
        allowEmbed: ${allow_embed}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
        optional: true
      api_key:
        type: string
        description: The apiKey field of the request body.
        optional: true
      api_quota:
        type: integer
        description: The apiQuota field of the request body.
        optional: true
      api_usage:
        type: integer
        description: The apiUsage field of the request body.
        optional: true
      templates_limit:
        type: integer
        description: The templatesLimit field of the request body.
        optional: true
      members_limit:
        type: integer
        description: The membersLimit field of the request body.
        optional: true
      stripe_customer_id:
        type: string
        description: The stripeCustomerId field of the request body.
        optional: true
      status:
        type: string
        description: The status field of the request body.
        optional: true
        enum:
        - FREE
        - ACTIVE
        - CANCELED
        - PAST_DUE
        - BLOCKED
      plan:
        type: string
        description: The plan field of the request body.
        optional: true
      billing_period:
        type: string
        description: The billingPeriod field of the request body.
        optional: true
        enum:
        - MONTH
        - YEAR
      allow_embed:
        type: boolean
        description: The allowEmbed field of the request body.
        optional: true
    output:
      description: OK
      type: object
  create_template_1:
    title: Create template 1
    description: Calls POST /templates on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: templates
    request:
      method: POST
      path: /templates
      body:
        isMaster: ${is_master}
        isClone: ${is_clone}
        sourceTemplateId: ${source_template_id}
        sourceTemplateName: ${source_template_name}
        id: ${id}
        type: ${type}
        name: ${name}
        description: ${description}
        width: ${width}
        height: ${height}
        userId: ${user_id}
        teamId: ${team_id}
    input:
      is_master:
        type: boolean
        description: The isMaster field of the request body.
        optional: true
      is_clone:
        type: boolean
        description: The isClone field of the request body.
        optional: true
      source_template_id:
        type: string
        description: The sourceTemplateId field of the request body.
        optional: true
      source_template_name:
        type: string
        description: The sourceTemplateName field of the request body.
        optional: true
      id:
        type: string
        description: The id field of the request body.
        optional: true
      type:
        type: string
        description: The type field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
        optional: true
      description:
        type: string
        description: The description field of the request body.
        optional: true
      width:
        type: integer
        description: The width field of the request body.
        optional: true
      height:
        type: integer
        description: The height field of the request body.
        optional: true
      user_id:
        type: string
        description: The userId field of the request body.
        optional: true
      team_id:
        type: string
        description: The teamId field of the request body.
        optional: true
    output:
      description: OK
      type: object
  get_master_templates_1:
    title: Get master templates 1
    description: Calls GET /admin/master-templates on Templated's API. Generated from the provider's published
      OpenAPI specification.
    category: admin
    request:
      method: GET
      path: /admin/master-templates
      query:
        query: ${query}
        categoryId: ${category_id}
        page: ${page}
        size: ${size}
    input:
      query:
        type: string
        description: The query parameter.
        optional: true
      category_id:
        type: string
        description: The categoryId parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
        default: 0
      size:
        type: integer
        description: The size parameter.
        optional: true
        default: 30
    output:
      description: OK
      type: object
    read_only: true
  migrate_payloads:
    title: Migrate payloads
    description: Calls POST /admin/migrate-payloads on Templated's API. Generated from the provider's
      published OpenAPI specification.
    category: admin
    request:
      method: POST
      path: /admin/migrate-payloads
      query:
        batchSize: ${batch_size}
        parallelism: ${parallelism}
    input:
      batch_size:
        type: integer
        description: The batchSize parameter.
        optional: true
        default: 10
      parallelism:
        type: integer
        description: The parallelism parameter.
        optional: true
        default: 5
    output:
      description: OK
      type: object
  get_mrr:
    title: Get mrr
    description: Calls GET /admin/mrr on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: admin
    request:
      method: GET
      path: /admin/mrr
    input: {}
    output:
      description: OK
      type: object
    read_only: true
  get_renders_grouped:
    title: Get renders grouped
    description: Calls GET /admin/renders-grouped on Templated's API. Generated from the provider's published
      OpenAPI specification.
    category: admin
    request:
      method: GET
      path: /admin/renders-grouped
      query:
        period: ${period}
        count: ${count}
    input:
      period:
        type: string
        description: The period parameter.
        optional: true
        default: hour
      count:
        type: integer
        description: The count parameter.
        optional: true
        default: 72
    output:
      description: OK
      type: object
    read_only: true
  get_admin_stats:
    title: Get admin stats
    description: Calls GET /admin/stats on Templated's API. Generated from the provider's published OpenAPI
      specification.
    category: admin
    request:
      method: GET
      path: /admin/stats
      query:
        days: ${days}
    input:
      days:
        type: integer
        description: The days parameter.
        optional: true
        default: 90
    output:
      description: OK
      type: object
    read_only: true
