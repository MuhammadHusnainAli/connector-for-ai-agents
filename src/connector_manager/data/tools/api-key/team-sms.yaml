# Team SMS tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://teamsms.io/swagger.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: team-sms
display_name: Team SMS
docs_url: https://teamsms.io
generated: true
generated_from: https://teamsms.io/swagger.json
tools:
  get_me:
    title: Who am I?
    description: Who am I? Calls GET /me on the provider's API.
    category: me
    request:
      method: GET
      path: /me
    input: {}
    output:
      description: successful operation
      type: object
    read_only: true
  phone_numbers_owned:
    title: Which are the phone numbers I owned?
    description: Which are the phone numbers I owned? Calls GET /phone-numbers-owned on the provider's
      API.
    category: phone_numbers_owned
    request:
      method: GET
      path: /phone-numbers-owned
    input: {}
    output:
      description: successful operation
      type: object
    read_only: true
  sms:
    title: List of received SMS
    description: List of received SMS Calls GET /sms on the provider's API.
    category: sms
    request:
      method: GET
      path: /sms
    input: {}
    output:
      description: successful operation
      type: object
    read_only: true
