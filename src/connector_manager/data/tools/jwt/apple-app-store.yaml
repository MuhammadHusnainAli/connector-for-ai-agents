# Apple App Store tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/apple.com/app-store-connect/1.4.1/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: apple-app-store
display_name: Apple App Store
docs_url: https://developer.apple.com/documentation/appstoreconnectapi
generated: true
generated_from: https://api.apis.guru/v2/specs/apple.com/app-store-connect/1.4.1/openapi.json
tools:
  app_categories_get_collection:
    title: App categories get collection
    description: Calls GET /v1/appCategories on the provider's API, as described by its OpenAPI specification.
    category: app_categories
    request:
      method: GET
      path: /v1/appCategories
      query:
        filter[platforms]: ${filter_platforms}
        exists[parent]: ${exists_parent}
        fields[appCategories]: ${fields_app_categories}
        limit: ${limit}
        include: ${include}
        limit[subcategories]: ${limit_subcategories}
    input:
      filter_platforms:
        type: array
        description: filter by attribute 'platforms'
        optional: true
        items:
          type: string
      exists_parent:
        type: array
        description: filter by existence or non-existence of related 'parent'
        optional: true
        items:
          type: string
      fields_app_categories:
        type: array
        description: the fields to include for returned resources of type appCategories
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: maximum resources per page
        optional: true
      include:
        type: array
        description: comma-separated list of relationships to include
        optional: true
        items:
          type: string
      limit_subcategories:
        type: integer
        description: maximum number of related subcategories returned (when they are included)
        optional: true
    output:
      description: List of AppCategories
      type: object
    read_only: true
  app_encryption_declarations_get_collection:
    title: App encryption declarations get collection
    description: Calls GET /v1/appEncryptionDeclarations on the provider's API, as described by its OpenAPI
      specification.
    category: app_encryption_declarations
    request:
      method: GET
      path: /v1/appEncryptionDeclarations
      query:
        filter[platform]: ${filter_platform}
        filter[app]: ${filter_app}
        filter[builds]: ${filter_builds}
        fields[appEncryptionDeclarations]: ${fields_app_encryption_declarations}
        limit: ${limit}
        include: ${include}
        fields[apps]: ${fields_apps}
    input:
      filter_platform:
        type: array
        description: filter by attribute 'platform'
        optional: true
        items:
          type: string
      filter_app:
        type: array
        description: filter by id(s) of related 'app'
        optional: true
        items:
          type: string
      filter_builds:
        type: array
        description: filter by id(s) of related 'builds'
        optional: true
        items:
          type: string
      fields_app_encryption_declarations:
        type: array
        description: the fields to include for returned resources of type appEncryptionDeclarations
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: maximum resources per page
        optional: true
      include:
        type: array
        description: comma-separated list of relationships to include
        optional: true
        items:
          type: string
      fields_apps:
        type: array
        description: the fields to include for returned resources of type apps
        optional: true
        items:
          type: string
    output:
      description: List of AppEncryptionDeclarations
      type: object
    read_only: true
  app_info_localizations_create_instance:
    title: App info localizations create instance
    description: Calls POST /v1/appInfoLocalizations on the provider's API, as described by its OpenAPI
      specification.
    category: app_info_localizations
    request:
      method: POST
      path: /v1/appInfoLocalizations
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appInfoLocalizations
    output:
      description: Single AppInfoLocalization
      type: object
  app_pre_orders_create_instance:
    title: App pre orders create instance
    description: Calls POST /v1/appPreOrders on the provider's API, as described by its OpenAPI specification.
    category: app_pre_orders
    request:
      method: POST
      path: /v1/appPreOrders
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
        optional: true
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appPreOrders
    output:
      description: Single AppPreOrder
      type: object
  app_preview_sets_create_instance:
    title: App preview sets create instance
    description: Calls POST /v1/appPreviewSets on the provider's API, as described by its OpenAPI specification.
    category: app_preview_sets
    request:
      method: POST
      path: /v1/appPreviewSets
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appPreviewSets
    output:
      description: Single AppPreviewSet
      type: object
  app_previews_create_instance:
    title: App previews create instance
    description: Calls POST /v1/appPreviews on the provider's API, as described by its OpenAPI specification.
    category: app_previews
    request:
      method: POST
      path: /v1/appPreviews
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appPreviews
    output:
      description: Single AppPreview
      type: object
  app_price_points_get_collection:
    title: App price points get collection
    description: Calls GET /v1/appPricePoints on the provider's API, as described by its OpenAPI specification.
    category: app_price_points
    request:
      method: GET
      path: /v1/appPricePoints
      query:
        filter[priceTier]: ${filter_price_tier}
        filter[territory]: ${filter_territory}
        fields[appPricePoints]: ${fields_app_price_points}
        limit: ${limit}
        include: ${include}
        fields[territories]: ${fields_territories}
    input:
      filter_price_tier:
        type: array
        description: filter by id(s) of related 'priceTier'
        optional: true
        items:
          type: string
      filter_territory:
        type: array
        description: filter by id(s) of related 'territory'
        optional: true
        items:
          type: string
      fields_app_price_points:
        type: array
        description: the fields to include for returned resources of type appPricePoints
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: maximum resources per page
        optional: true
      include:
        type: array
        description: comma-separated list of relationships to include
        optional: true
        items:
          type: string
      fields_territories:
        type: array
        description: the fields to include for returned resources of type territories
        optional: true
        items:
          type: string
    output:
      description: List of AppPricePoints
      type: object
    read_only: true
  app_price_tiers_get_collection:
    title: App price tiers get collection
    description: Calls GET /v1/appPriceTiers on the provider's API, as described by its OpenAPI specification.
    category: app_price_tiers
    request:
      method: GET
      path: /v1/appPriceTiers
      query:
        filter[id]: ${filter_id}
        fields[appPriceTiers]: ${fields_app_price_tiers}
        limit: ${limit}
        include: ${include}
        fields[appPricePoints]: ${fields_app_price_points}
        limit[pricePoints]: ${limit_price_points}
    input:
      filter_id:
        type: array
        description: filter by id(s)
        optional: true
        items:
          type: string
      fields_app_price_tiers:
        type: array
        description: the fields to include for returned resources of type appPriceTiers
        optional: true
        items:
          type: string
      limit:
        type: integer
        description: maximum resources per page
        optional: true
      include:
        type: array
        description: comma-separated list of relationships to include
        optional: true
        items:
          type: string
      fields_app_price_points:
        type: array
        description: the fields to include for returned resources of type appPricePoints
        optional: true
        items:
          type: string
      limit_price_points:
        type: integer
        description: maximum number of related pricePoints returned (when they are included)
        optional: true
    output:
      description: List of AppPriceTiers
      type: object
    read_only: true
  app_screenshot_sets_create_instance:
    title: App screenshot sets create instance
    description: Calls POST /v1/appScreenshotSets on the provider's API, as described by its OpenAPI specification.
    category: app_screenshot_sets
    request:
      method: POST
      path: /v1/appScreenshotSets
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appScreenshotSets
    output:
      description: Single AppScreenshotSet
      type: object
  app_screenshots_create_instance:
    title: App screenshots create instance
    description: Calls POST /v1/appScreenshots on the provider's API, as described by its OpenAPI specification.
    category: app_screenshots
    request:
      method: POST
      path: /v1/appScreenshots
      body:
        attributes: ${attributes}
        relationships: ${relationships}
        type: ${type}
    input:
      attributes:
        type: object
        description: The attributes field of the request body.
      relationships:
        type: object
        description: The relationships field of the request body.
      type:
        type: string
        description: The type field of the request body.
        enum:
        - appScreenshots
    output:
      description: Single AppScreenshot
      type: object
