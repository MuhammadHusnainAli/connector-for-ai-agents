# Alai tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://docs.getalai.com/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: alai
display_name: Alai
docs_url: https://docs.getalai.com/openapi.json
generated: true
generated_from: https://docs.getalai.com/openapi.json
tools:
  generate_presentation_api_v1_generations_post:
    title: Generate a presentation
    description: 'Generate a presentation Create a new presentation from text content. This is an async
      operation. Workflow: 1. Submit your content and options 2. Receive a generationid immediately 3.
      Poll GET /generations/{generationid} until status is completed or failed 4. Access your presentation
      via the URLs in the response Rate limit: Calls POST /api/v1/generations on the provider''s API.'
    category: api
    request:
      method: POST
      path: /api/v1/generations
      body:
        input_text: ${input_text}
        additional_instructions: ${additional_instructions}
        image_ids: ${image_ids}
        export_formats: ${export_formats}
        presentation_options: ${presentation_options}
        text_options: ${text_options}
        image_options: ${image_options}
    input:
      input_text:
        type: string
        description: Content to transform into slides. Can be plain text, markdown, or structured notes.
          Longer input typically produces more slides.
      additional_instructions:
        type: string
        description: Guidance for the AI on style, focus areas, or specific requirements not captured
          in other options.
        optional: true
      image_ids:
        type: string
        description: IDs of previously uploaded images (from POST /upload-images) to incorporate. Images
          are matched to relevant slides automatically.
        optional: true
      export_formats:
        type: array
        description: Formats to export upon completion. 'link' is a shareable web URL. 'pdf' and 'ppt'
          provide downloadable files.
        optional: true
        items:
          type: string
      presentation_options:
        type: object
        description: The presentation_options field of the request body.
        optional: true
      text_options:
        type: object
        description: The text_options field of the request body.
        optional: true
      image_options:
        type: object
        description: The image_options field of the request body.
        optional: true
    output:
      description: Successful Response
      type: object
  ping_api_v1_ping_get:
    title: Verify API key
    description: Verify API key Validates your API key and returns your user ID. Use this to test authentication
      before making other requests. Calls GET /api/v1/ping on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/ping
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  get_presentations_api_v1_presentations_get:
    title: List all presentations
    description: List all presentations Returns a list of all presentations owned by the authenticated
      user, including their IDs and titles. Calls GET /api/v1/presentations on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/presentations
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  get_themes_api_v1_themes_get:
    title: List available themes
    description: List available themes Returns theme IDs and names for all system themes and user themes
      accessible to the authenticated user. Calls GET /api/v1/themes on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/themes
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  get_vibes_api_v1_vibes_get:
    title: List available vibes
    description: List available vibes Returns vibe IDs and names for all system vibes and custom user
      vibes accessible to the authenticated user. Pass the id as presentationoptions.vibeid on a generation
      request. Calls GET /api/v1/vibes on the provider's API.
    category: api
    request:
      method: GET
      path: /api/v1/vibes
    input: {}
    output:
      description: Successful Response
      type: object
    read_only: true
  get_v1_generations:
    title: Check generation status
    description: 'Check generation status Poll this endpoint to check the status of any async operation
      (presentation generation, slide creation, export, or transcript generation). Status values: - pending
      - Queued, not yet started - inprogress - Currently processing - completed - Finished successfully;
      results available in response - faile Calls GET /api/v1/generations/{generation_id} on the provider''s
      API.'
    category: api
    request:
      method: GET
      path: /api/v1/generations/${generation_id}
    input:
      generation_id:
        type: string
        description: The generation_id parameter.
    output:
      description: Successful Response
      type: object
    read_only: true
  delete_v1_presentations:
    title: Delete Presentation
    description: Delete Presentation Calls DELETE /api/v1/presentations/{presentation_id} on the provider's
      API.
    category: api
    request:
      method: DELETE
      path: /api/v1/presentations/${presentation_id}
    input:
      presentation_id:
        type: string
        description: The presentation_id parameter.
    output:
      description: Successful Response
      type: object
    destructive: true
  create_presentations_exports:
    title: Export a presentation
    description: Export a presentation Export an existing presentation to one or more formats (PDF, PPTX).
      This is an async operation. Poll GET /generations/{generationid} until status is completed, then
      access download URLs in the formats field. Calls POST /api/v1/presentations/{presentation_id}/exports
      on the provider's API.
    category: api
    request:
      method: POST
      path: /api/v1/presentations/${presentation_id}/exports
      body:
        formats: ${formats}
    input:
      presentation_id:
        type: string
        description: The presentation_id parameter.
      formats:
        type: array
        description: 'Formats to export: ''link'' (shareable URL), ''pdf'', ''ppt'''
        optional: true
        items:
          type: string
    output:
      description: Successful Response
      type: object
