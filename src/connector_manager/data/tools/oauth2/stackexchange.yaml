# Stack Exchange tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://api.apis.guru/v2/specs/stackexchange.com/2.0/openapi.json
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: stackexchange
display_name: Stack Exchange
docs_url: https://api.stackexchange.com/
generated: true
generated_from: https://api.apis.guru/v2/specs/stackexchange.com/2.0/openapi.json
tools:
  get_answers:
    title: Get answers
    description: 'Returns all the undeleted answers in the system. The sorts accepted by this method operate
      on the follow fields of the answer object: - activity - lastactivitydate - creation - creationdate
      - votes - score activity is the default sort. It is possible to create moderately complex queries
      using sort, Calls GET /2.0/answers on the provider''s API.'
    category: answers
    request:
      method: GET
      path: /2.0/answers
      query:
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number
        optional: true
      min:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - activity
        - creation
        - votes
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_badges:
    title: Get badges
    description: Returns all the badges in the system. Badge sorts are a tad complicated. For the purposes
      of sorting (and min/max) tagbased is considered to be greater than named. This means that you can
      get a list of all tag based badges by passing min=tagbased, and conversely all the named badges
      by passing max=n Calls GET /2.0/badges on the provider's API.
    category: badges
    request:
      method: GET
      path: /2.0/badges
      query:
        inname: ${inname}
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      inname:
        type: string
        description: The inname parameter.
        optional: true
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = rank = string sort = name = string sort = type = string
        optional: true
      min:
        type: string
        description: sort = rank = string sort = name = string sort = type = string
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - rank
        - name
        - type
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_comments:
    title: Get comments
    description: Gets all the comments on the site. If you're filtering for interesting comments (by score,
      creation date, etc.) make use of the sort paramter with appropriate min and max values. If you're
      looking to query conversations between users, instead use the /users/{ids}/mentioned and /users/{ids}/comments/
      Calls GET /2.0/comments on the provider's API.
    category: comments
    request:
      method: GET
      path: /2.0/comments
      query:
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = creation = date sort = votes = number
        optional: true
      min:
        type: string
        description: sort = creation = date sort = votes = number
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - creation
        - votes
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_errors:
    title: Get errors
    description: Returns the various error codes that can be produced by the API. This method is provided
      for discovery, documentation, and testing purposes, it is not expected many applications will consume
      it during normal operation. For testing purposes, look into the /errors/{id} method which simulates
      errors gi Calls GET /2.0/errors on the provider's API.
    category: errors
    request:
      method: GET
      path: /2.0/errors
      query:
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
    input:
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_events:
    title: Get events
    description: 'Returns a stream of events that have occurred on the site. The API considers the following
      "events": - posting a question - posting an answer - posting a comment - editing a post - creating
      a user Events are only accessible for 15 minutes after they occurred, and by default only events
      in the last 5 Calls GET /2.0/events on the provider''s API.'
    category: events
    request:
      method: GET
      path: /2.0/events
      query:
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
        since: ${since}
    input:
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
      since:
        type: integer
        description: Unix date.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_inbox:
    title: Get inbox
    description: Returns a user's inbox. This method requires an accesstoken, with a scope containing
      "readinbox". This method returns a list of inbox items. Calls GET /2.0/inbox on the provider's API.
    category: inbox
    request:
      method: GET
      path: /2.0/inbox
      query:
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
    input:
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_info:
    title: Get info
    description: Returns a collection of statistics about the site. Data to facilitate per-site customization,
      discover related sites, and aggregate statistics is all returned by this method. This data is cached
      very aggressively, by design. Query sparingly, ideally no more than once an hour. This method returns
      an Calls GET /2.0/info on the provider's API.
    category: info
    request:
      method: GET
      path: /2.0/info
      query:
        site: ${site}
    input:
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_me:
    title: Get me
    description: Returns the user associated with the passed accesstoken. This method returns a user.
      Calls GET /2.0/me on the provider's API.
    category: me
    request:
      method: GET
      path: /2.0/me
      query:
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = reputation = number sort = creation = date sort = name = string sort = modified
          = date
        optional: true
      min:
        type: string
        description: sort = reputation = number sort = creation = date sort = name = string sort = modified
          = date
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - reputation
        - creation
        - name
        - modified
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_notifications:
    title: Get notifications
    description: Returns a user's notifications. This method requires an accesstoken, with a scope containing
      "readinbox". This method returns a list of notifications. Calls GET /2.0/notifications on the provider's
      API.
    category: notifications
    request:
      method: GET
      path: /2.0/notifications
      query:
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
    input:
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_posts:
    title: Get posts
    description: Fetches all posts (questions and answers) on the site. In many ways this method is the
      union of /questions and /answers, returning both sets of data albeit only the common fields. Most
      applications should use the question or answer specific methods, but /posts is available for those
      rare cases where Calls GET /2.0/posts on the provider's API.
    category: posts
    request:
      method: GET
      path: /2.0/posts
      query:
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number
        optional: true
      min:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - activity
        - creation
        - votes
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_privileges:
    title: Get privileges
    description: Returns the earnable privileges on a site. Privileges define abilities a user can earn
      (via reputation) on any Stack Exchange site. While fairly stable, over time they do change. New
      ones are introduced with new features, and the reputation requirements change as a site matures.
      This method returns Calls GET /2.0/privileges on the provider's API.
    category: privileges
    request:
      method: GET
      path: /2.0/privileges
      query:
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_questions:
    title: Get questions
    description: Gets all the questions on the site. This method allows you make fairly flexible queries
      across the entire corpus of questions on a site. For example, getting all questions asked in the
      the week of Jan 1st 2011 with scores of 10 or more is a single query with parameters sort=votes&min=10&fromdate=129
      Calls GET /2.0/questions on the provider's API.
    category: questions
    request:
      method: GET
      path: /2.0/questions
      query:
        tagged: ${tagged}
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
    input:
      tagged:
        type: string
        description: String list (semicolon delimited).
        optional: true
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = hot =
          none sort = week = none sort = month = none sort = relevance = none
        optional: true
      min:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = hot =
          none sort = week = none sort = month = none sort = relevance = none
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - activity
        - creation
        - votes
        - hot
        - week
        - month
        - relevance
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
    output:
      description: OK
      type: object
    read_only: true
  get_search:
    title: Get search
    description: Searches a site for any questions which fit the given criteria. This method is intentionally
      quite limited. For more general searching, you should use a proper internet search engine restricted
      to the domain of the site in question. At least one of tagged or intitle must be set on this method.
      notta Calls GET /2.0/search on the provider's API.
    category: search
    request:
      method: GET
      path: /2.0/search
      query:
        tagged: ${tagged}
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
        intitle: ${intitle}
        nottagged: ${nottagged}
    input:
      tagged:
        type: string
        description: String list (semicolon delimited).
        optional: true
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = relevance
          = none
        optional: true
      min:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = relevance
          = none
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - activity
        - creation
        - votes
        - relevance
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
      intitle:
        type: string
        description: The intitle parameter.
        optional: true
      nottagged:
        type: string
        description: String list (semicolon delimited).
        optional: true
    output:
      description: OK
      type: object
    read_only: true
  get_similar:
    title: Get similar
    description: Returns questions which are similar to a hypothetical one based on a title and tag combination.
      This method is roughly equivalent to a site's related questions suggestion on the ask page. This
      method is useful for correlating data outside of a Stack Exchange site with similar content within
      one. Not Calls GET /2.0/similar on the provider's API.
    category: similar
    request:
      method: GET
      path: /2.0/similar
      query:
        tagged: ${tagged}
        order: ${order}
        max: ${max}
        min: ${min}
        sort: ${sort}
        fromdate: ${fromdate}
        todate: ${todate}
        pagesize: ${pagesize}
        page: ${page}
        filter: ${filter}
        callback: ${callback}
        site: ${site}
        nottagged: ${nottagged}
        title: ${title}
    input:
      tagged:
        type: string
        description: String list (semicolon delimited).
        optional: true
      order:
        type: string
        description: The order parameter.
        optional: true
        enum:
        - desc
        - asc
      max:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = relevance
          = none
        optional: true
      min:
        type: string
        description: sort = activity = date sort = creation = date sort = votes = number sort = relevance
          = none
        optional: true
      sort:
        type: string
        description: The sort parameter.
        optional: true
        enum:
        - activity
        - creation
        - votes
        - relevance
      fromdate:
        type: integer
        description: Unix date.
        optional: true
      todate:
        type: integer
        description: Unix date.
        optional: true
      pagesize:
        type: integer
        description: The pagesize parameter.
        optional: true
      page:
        type: integer
        description: The page parameter.
        optional: true
      filter:
        type: string
        description: Discussion The Stack Exchange API allows applications to exclude almost every field
          returned. For example, if an application did not care about a user's badge counts it could exclude
          user.badgecounts whenever it calls a method that returns users. An application excludes fields
          by creating a filter (via /filter/create) and passing it to a method in the filter parameter.
          Filters are immutable and no
        optional: true
      callback:
        type: string
        description: All API responses are JSON, we do support JSONP with the callback query parameter.
        optional: true
      site:
        type: string
        description: Each of these methods operates on a single site at a time, identified by the site
          parameter. This parameter can be the full domain name (ie. "stackoverflow.com"), or a short
          form identified by apisiteparameter on the site object.
      nottagged:
        type: string
        description: String list (semicolon delimited).
        optional: true
      title:
        type: string
        description: The title parameter.
        optional: true
    output:
      description: OK
      type: object
    read_only: true
