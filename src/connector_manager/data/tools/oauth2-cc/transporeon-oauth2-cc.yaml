# Transporeon Appointment Scheduling (Client Credentials) tools -- GENERATED from the provider's OpenAPI specification.
#
#   source: https://yard-management-planning-api.dev.eu.trimble-transportation.com/v3/api-docs
#
# Every path, method, parameter and description below comes from that spec.
# Nothing here is inferred: operations the spec did not describe well enough to
# build a usable tool from were skipped rather than guessed at.
#
# This is a starting point, not a hand-authored pack. It has no scope
# annotations, and it covers a representative slice of the API rather than the
# whole surface. Replacing it with a researched pack is an improvement -- delete
# the `generated` flag once you do.

connector_id: transporeon-oauth2-cc
display_name: Transporeon Appointment Scheduling (Client Credentials)
docs_url: https://yard-management-planning-api.dev.eu.trimble-transportation.com/v3/api-docs
generated: true
generated_from: https://yard-management-planning-api.dev.eu.trimble-transportation.com/v3/api-docs
tools:
  get_unique_permissions:
    title: Get unique permissions for current user's roles
    description: Get unique permissions for current user's roles Returns all unique permissions across
      all roles assigned to the current user, deduplicated by permission code Calls GET /transportation/yard-management/v1/permissions
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/v1/permissions
    input: {}
    output:
      description: Returns list of unique permissions
      type: object
    read_only: true
  create_role_permissions:
    title: Create new Role Permissions for user's role
    description: Create new Role Permissions for user's role Calls POST /transportation/yard-management/v1/permissions
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: POST
      path: /transportation/yard-management/v1/permissions
      body:
        id: ${id}
        version: ${version}
        createdBy: ${created_by}
        createdAt: ${created_at}
        updatedBy: ${updated_by}
        updatedAt: ${updated_at}
        deleted: ${deleted}
        role: ${role}
        module: ${module}
        permissions: ${permissions}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      version:
        type: integer
        description: The version field of the request body.
        optional: true
      created_by:
        type: string
        description: The createdBy field of the request body.
        optional: true
      created_at:
        type: string
        description: The createdAt field of the request body.
        optional: true
      updated_by:
        type: string
        description: The updatedBy field of the request body.
        optional: true
      updated_at:
        type: string
        description: The updatedAt field of the request body.
        optional: true
      deleted:
        type: boolean
        description: The deleted field of the request body.
        optional: true
      role:
        type: string
        description: The role field of the request body.
      module:
        type: string
        description: The module field of the request body.
        optional: true
      permissions:
        type: array
        description: The permissions field of the request body.
        optional: true
        items:
          type: object
    output:
      description: Role Permissions created successfully
      type: object
  get_error_codes:
    title: Get all error codes
    description: Get all error codes Calls GET /transportation/yard-management/master/v1/error-codes on
      Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/master/v1/error-codes
    input: {}
    output:
      description: Returns a list of all possible error codes
      type: object
    read_only: true
  get_appointments:
    title: Get all the Appointments
    description: Get all the Appointments Calls GET /transportation/yard-management/planning/v1/appointments
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/planning/v1/appointments
      query:
        size: ${size}
        page: ${page}
        isDeleted: ${is_deleted}
        sortOrder: ${sort_order}
        sortField: ${sort_field}
        filter: ${filter}
    input:
      size:
        type: integer
        description: The size parameter.
        optional: true
        default: 10
      page:
        type: integer
        description: The page parameter.
        optional: true
        default: 1
      is_deleted:
        type: boolean
        description: The isDeleted parameter.
        optional: true
        default: false
      sort_order:
        type: string
        description: The sortOrder parameter.
        optional: true
        enum:
        - ASC
        - DESC
        default: ASC
      sort_field:
        type: string
        description: The sortField parameter.
        optional: true
        default: appointmentDate
      filter:
        type: string
        description: The filter parameter.
        optional: true
    output:
      description: Get a paginated list of Appointments
      type: object
    read_only: true
  add_appointment:
    title: Create an Appointment
    description: Create an Appointment Calls POST /transportation/yard-management/planning/v1/appointments
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: POST
      path: /transportation/yard-management/planning/v1/appointments
      body:
        id: ${id}
        version: ${version}
        createdBy: ${created_by}
        createdAt: ${created_at}
        updatedBy: ${updated_by}
        updatedAt: ${updated_at}
        deleted: ${deleted}
        code: ${code}
        yard: ${yard}
        externalReference: ${external_reference}
        source: ${source}
        type: ${type}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      version:
        type: integer
        description: The version field of the request body.
        optional: true
      created_by:
        type: string
        description: The createdBy field of the request body.
        optional: true
      created_at:
        type: string
        description: The createdAt field of the request body.
        optional: true
      updated_by:
        type: string
        description: The updatedBy field of the request body.
        optional: true
      updated_at:
        type: string
        description: The updatedAt field of the request body.
        optional: true
      deleted:
        type: boolean
        description: The deleted field of the request body.
        optional: true
      code:
        type: string
        description: The code field of the request body.
        optional: true
      yard:
        type: object
        description: The yard field of the request body.
        optional: true
      external_reference:
        type: object
        description: The externalReference field of the request body.
        optional: true
      source:
        type: string
        description: The source field of the request body.
        optional: true
        enum:
        - INDUSTRY
        - RETAIL
        - CORE
      type:
        type: string
        description: The type field of the request body.
        optional: true
        enum:
        - APPOINTMENT
        - RESERVATION
        - LOCK
    output:
      description: Appointment created successfully
      type: object
  list_catalog:
    title: List active preset sections with their embedded fields
    description: List active preset sections with their embedded fields Calls GET /transportation/yard-management/planning/v1/booking-dialog-preset-catalog
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/planning/v1/booking-dialog-preset-catalog
    input: {}
    output:
      description: Returns the full preset catalog tree
      type: object
    read_only: true
  list_2:
    title: List system datasets
    description: 'List system datasets Paginated list with RSQL filter. Example picker filter: active==true;deprecated==false;kind==APISOURCED;apiSource==CARRIER
      Calls GET /transportation/yard-management/planning/v1/booking-dialog-system-datasets on Transporeon
      Appointment Scheduling (Client Credentials)''s API.'
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/planning/v1/booking-dialog-system-datasets
      query:
        size: ${size}
        page: ${page}
        isDeleted: ${is_deleted}
        sortOrder: ${sort_order}
        sortField: ${sort_field}
        filter: ${filter}
    input:
      size:
        type: integer
        description: The size parameter.
        optional: true
        default: 20
      page:
        type: integer
        description: The page parameter.
        optional: true
        default: 1
      is_deleted:
        type: boolean
        description: The isDeleted parameter.
        optional: true
        default: false
      sort_order:
        type: string
        description: The sortOrder parameter.
        optional: true
        enum:
        - ASC
        - DESC
        default: ASC
      sort_field:
        type: string
        description: The sortField parameter.
        optional: true
        default: code
      filter:
        type: string
        description: The filter parameter.
        optional: true
    output:
      description: Returns paginated system datasets
      type: object
    read_only: true
  create_3:
    title: Create a system dataset
    description: Create a system dataset Admin endpoint. Kind invariants are enforced; code/kind/apiSource/itemKeyType
      become immutable post-create. Calls POST /transportation/yard-management/planning/v1/booking-dialog-system-datasets
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: POST
      path: /transportation/yard-management/planning/v1/booking-dialog-system-datasets
      body:
        id: ${id}
        version: ${version}
        deleted: ${deleted}
        createdAt: ${created_at}
        updatedAt: ${updated_at}
        code: ${code}
        name: ${name}
        descriptionTranslationId: ${description_translation_id}
        kind: ${kind}
        itemKeyType: ${item_key_type}
        items: ${items}
        apiSource: ${api_source}
    input:
      id:
        type: string
        description: The id field of the request body.
        optional: true
      version:
        type: integer
        description: The version field of the request body.
        optional: true
      deleted:
        type: boolean
        description: The deleted field of the request body.
        optional: true
      created_at:
        type: string
        description: The createdAt field of the request body.
        optional: true
      updated_at:
        type: string
        description: The updatedAt field of the request body.
        optional: true
      code:
        type: string
        description: The code field of the request body.
        optional: true
      name:
        type: string
        description: The name field of the request body.
        optional: true
      description_translation_id:
        type: string
        description: The descriptionTranslationId field of the request body.
        optional: true
      kind:
        type: string
        description: The kind field of the request body.
        optional: true
        enum:
        - STATIC_OPTIONS
        - API_SOURCED
      item_key_type:
        type: string
        description: The itemKeyType field of the request body.
        optional: true
        enum:
        - STRING
        - NUMBER
        - BOOLEAN
      items:
        type: array
        description: The items field of the request body.
        optional: true
        items:
          type: object
      api_source:
        type: string
        description: The apiSource field of the request body.
        optional: true
        enum:
        - CARRIER
        - DRIVER
        - VEHICLE_LICENCE_PLATE
    output:
      description: System dataset created
      type: object
  list_1:
    title: List booking dialog templates
    description: List booking dialog templates Calls GET /transportation/yard-management/planning/v1/booking-dialog-templates
      on Transporeon Appointment Scheduling (Client Credentials)'s API.
    category: yard_management
    request:
      method: GET
      path: /transportation/yard-management/planning/v1/booking-dialog-templates
      query:
        size: ${size}
        page: ${page}
        isDeleted: ${is_deleted}
        sortOrder: ${sort_order}
        sortField: ${sort_field}
        filter: ${filter}
    input:
      size:
        type: integer
        description: The size parameter.
        optional: true
        default: 20
      page:
        type: integer
        description: The page parameter.
        optional: true
        default: 1
      is_deleted:
        type: boolean
        description: The isDeleted parameter.
        optional: true
        default: false
      sort_order:
        type: string
        description: The sortOrder parameter.
        optional: true
        enum:
        - ASC
        - DESC
        default: ASC
      sort_field:
        type: string
        description: The sortField parameter.
        optional: true
        default: updatedAt
      filter:
        type: string
        description: The filter parameter.
        optional: true
    output:
      description: Returns paginated template summaries
      type: object
    read_only: true
